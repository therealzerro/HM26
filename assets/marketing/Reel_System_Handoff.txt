================================================================================
HITMASTER ZK6 — DAILY REEL SYSTEM
Content Agent Handoff · 2026-08-19 · v4.3
================================================================================
⚠ THIS FILE WAS RENAMED on 2026-07-28. It was
`assets/marketing/Reel_System_Handoff_2026-07-27.txt`; it is now
`assets/marketing/Reel_System_Handoff.txt`. The old name carried its CREATION
date, not its content date, so a file revised through v1.3 → v1.5 → v1.6 still
read "07-27" and looked nine days stale when it was current. The date is gone
from the name for good — **the version is the header line above, and nowhere
else.** Bookmark the new path; the old one no longer exists.

Read this before proposing or producing ANY reel-related asset or enhancement.
Companion docs:
  - assets/HitMaster_Brand_Rehab_Skill_Brief_v2.md  (brand law — canonical)
  - assets/marketing/AllDay_Reel_Endcard_Revision_Brief_2026-07-27.md (endcard contract)
  - docs/mkt15_phase2_copy_brief.md (public copy set — DELIVERED + the bolt request)
  - REEL_COMMANDS.txt (repo root — operator command reference)

================================================================================
STATE OF THE SYSTEM — 2026-08-19 (CONSOLIDATED SNAPSHOT, current to v4.3 — READ THIS FIRST)
================================================================================
This block is the one-pass current picture. It is REWRITTEN IN PLACE at every
version (it is live text, not an era record); the "WHAT CHANGED IN vX.Y"
blocks beneath it are the era records and are never retro-edited. Where this
block and an older note disagree, this block wins; where it and a § section
disagree, the § section has been struck or is wrong — say so in your next
audit and it will be fixed.

A. DAILY OUTPUT (operator-triggered `npm run reel:daily`, never scheduled):
   NINE reels, each auto-published to Admin → Reels with a drafted caption;
   NOTHING auto-posts. Order: verify → allday → midday → evening.
     verify          group cut, D−1 receipts (aborts on a zero-match day)
     verify_public   masked public grading cut (same beat map; tier 1)
     allday_pro · allday_free · allday_public
     midday_pro · midday_free (redacted)
     evening_pro · evening_free (redacted)
   midday_public / evening_public are UNREGISTERED by ruling (no session
   carriers; three daily public reels is a cadence risk) — do not propose
   assets for them. Kind list is enforced by a DB CHECK + KIND_UI entry (§8).
   PLUS ONE MANUAL KIND (MKT-62, 8/19): `verify_midday` — THE SAME-DAY
   MIDDAY VERIFY. NOT in reel:daily, ever: `npm run reel:verify-midday`,
   run by the operator on days the midday ledger is imported and graded in
   time (~4:30 PM ET slot, POSTING_SCHEDULE Session 3 — which now exists).
   Grades TODAY's MIDDAY board, free group only. Anatomy in C2 below.
   Last three runs: 8/17 (first coffee-Home production day; allday+midday
   re-published after MKT-57a), 8/18, 8/19 — all nine kinds, no aborts.
   Retention is keep-current-only (MKT-38): yesterday's mp4 exists only in
   git history.

B. SLATE REEL ANATOMY (allday/midday/evening, all cuts; 1080×1920 60fps;
   33.83–34.53s depending on the day's intro):
     0.0–~5.6   INTRO — slate kinds rotate over INTRO_ROTATION (TEN: standard,
                deadpan, arrival, monitors, powerup, board, guest, window,
                arrive — MKT-54; juggle — MKT-61, 8/19). Lane-spread: pro and free draw DIFFERENT
                members the same day; the walk reshuffles per cycle (MKT-49).
                Frame-one dated identity chip over the Anchor's shoulder
                (MKT-22/35). verify draws VERIFY_INTROS (anchor_intro_verify),
                public kinds draw PUBLIC_INTROS (anchor_intro_public) —
                both are SETS of one and NEVER fall into the slate rotation.
     ~5.3–8.3   STINGER — 3.0s, 0.3s crossfade into the intro smoke. SIX
                shared motions rotate (std/strike/circuit/fracture + etch/
                facet — MKT-58; imprint retired; shutter rejected, regen
                pending); verify + verify_public draw the SEAL set instead.
                Copy per kind (stinger-config.ts); lockup on the shared 1330
                band (MKT-45 — no per-kind tucks, ever).
     8.3–27.3   BODY 19.0s = 10.0s STATIC STILL of the COFFEE-MODE HOME
                (MKT-57: scope switcher · "NEXT DRAW · Nh NNm" · 2×3 rank
                tiles "#N / digits / ENERGY nn" · black space · tab bar) then
                six Pick Detail Modals tapped in rank order, 1.5s each, hard
                cuts (MKT-56). Body provenance tags: hm_reel_date,
                hm_reel_redacted, hm_reel_public — the assembler asserts them.
                MEMBER-LEGIBILITY LAYER on full-fidelity bodies ONLY (never on
                redacted/public): tile digits embossed 1.25× with glow;
                NOTATION BAND at the bottom of the black space ("EVENING · 6
                SIGNALS · RANK ORDER" + #1…#6, mono, read back from the DOM —
                abort unless six); "TODAY'S DATA DROP" stamp chip at y=1000
                (above the band); HOLD TIMER draining along the bottom edge.
                Capture-time DOM injection + encode filters — the APP IS NOT
                MODIFIED by any of it. MKT-11 promo panels (SEVEN as of
                MKT-60 8/19, served from the app-panels bucket) sit below the
                "RESOLVED IN" card in every modal (app-wide product surface;
                MKT-57a made Home's modal render them like Explore's).
     27.3–33.8  OUTRO — the endcard's FIRST 6.5s. Endcard motions rotate per
                TIER: pro = reveal/discharge/ignite/lock (MKT-47; reveal is
                bed-INELIGIBLE), free = std/alt/tide/bloom/adopen (MKT-48;
                public kinds are free-tier). Text on the shared 1240 band.
     AUDIO      intro sting → stinger impact → carrier VO from 0.4s before the
                dissolve completes (carrier-local t=0). Usable VO window =
                bodyDur+0.4 = 19.4s, INDEPENDENT of intro length. Long carrier
                (≥~19.4s) = wall-to-wall, fade at 20.5s bites first; short
                (~10s) = hum-bed from the endcard's pre-crack window. −14 LUFS.
     BEAT MAP   (carrier-local): 0–0.4 under the dissolve tail → 0.4–10.4 the
                SIX TILES (still — members notate here; the VO may read the
                board but must not say "tap in" before ~10.4) → 10.4–19.4 one
                modal every 1.5s → long-form final line may ride the smoke
                rise, never the bolt snap.

C. VERIFY ANATOMY (group + public cuts; ~23–29s, data-adaptive): fixed intro
   → seal stinger ("YESTERDAY'S RECEIPTS", headline-dominant) → body 8.6–14.0s
   built from the row selection (slate 2.5 + summary 2.0 + pan 2.0 + holds:
   2.0s straight / 1.6s box, ≤3 featured, straights first) with the persistent
   gold date ribbon → endcard FIRST 6.5s on the shared band. Carrier =
   verif_carrier (10.005s) + verif_carrier_pt2 (6.542s, MKT-43 pair) or
   signoff_short (~2.2s) or silence — resolved BY WINDOW, whole or not at all.
   Public half differs in exactly five ways (§3). The track-record screen is
   verify's live set: `?capture=1` + `day-<date>` anchors + heading text are
   RIG CONTRACT (MKT-51) — never change them without a work order.

C2. SAME-DAY MIDDAY VERIFY ANATOMY (`verify_midday`, MKT-62 — ~21–27s,
   data-adaptive; FIRST LIVE BUILD PENDING today's ledger): the daily verify
   proves the METHOD; this reel proves the GAP. A free member saw the midday
   board go up COVERED this morning; this grades it the same afternoon.
     0.0–~5.9   INTRO — `anchor_intro_rise` (the Anchor STANDS — the only beat
                in the pool where he leaves the chair). PINNED to this kind,
                never in any rotation: the standing beat is FALSE on a
                routine reel. Chip at frame one: TODAY'S MIDDAY · GRADED
                (gold, dated TODAY — the inverse of verify's chip rule).
     —          NO STINGER, by ruling (8/19): the 1-box floor day measured
                63.8% furniture WITH one = the exact number that barred
                verify's stinger for a month, and thin days are the days that
                run; the chip already identifies; intro → covered board reads
                better direct. "SAME-DAY RECEIPTS" copy withdrawn.
     body       THE COVER COMING OFF: the midday board AS THE FREE ROOM SAW
                IT (digits + set covered, NO result column — nothing was
                graded in this frame), 2.0s static hold → the cover LIFTS
                (0.5s eased dissolve, two pixel-registered boards, nothing
                moves but the cover) → the GRADED board, 2.5s push-in →
                SUMMARY BAND 2.0s = THE TIMESTAMP PAIR (PUBLISHED h:mm AM ·
                GRADED h:mm PM · ELAPSED Nh MMm · MIDDAY MATCHES n — real
                provenance: PUBLISHED = the midday_free row's posted_at, the
                literal moment the covered board went to the free room; GRADED
                = the hit-detection clock; never the render clock; the reel
                ABORTS if the board was never posted) → pan → holds (straights
                2.0 / boxes 1.6, ≤3, straights first) → settle. Body ≈ 10.9s
                (1 box) / 13.1–13.5s (median 2 rows). Gold ribbon TODAY'S
                MIDDAY · GRADED, board placement through the whole cover-lift
                segment, spec placement from the summary cut.
     outro      `endcard_motion_sameday` (gold sweeps in and settles around an
                ALREADY-STANDING bolt — renamed off `_confirmed` by ruling:
                the file must never imply the reel only runs when the board
                landed). Lines: HITMASTER ZK6 / POSTED THIS MORNING · GRADED
                THIS AFTERNOON / ZK PRO READS EVERY BOARD FIRST · $2.49/MO
                (gold). Pricing legal: FREE GROUP ONLY, tier 2. PINNED —
                in neither tier pool.
     AUDIO      intro sting → `verif_carrier_sameday` ("Now this is a rare
                one." / "This mornin's board — already checked against what
                drew." / "Same day. Not tomorrow." — VO 1.10–9.69s, single
                part, set of ONE, never pooled) → the last ~1–4s of the holds
                under silence until the endcard audio at the outro (verify's
                standing MKT-27/41 ruling). A sign-off continuation for THIS
                kind would close that gap (pt2 shape).
     RATIO      furniture 12.1s (no stinger): ~53% on the 1-box floor day,
                ~48% at median — healthy. Abort days: ~30% of days have no
                midday match; the reel simply does not run.
     BINDING    FREE GROUP ONLY. No public variant, no cross-post (real
                digits + state attribution — fails Q1 honestly; a masked build
                would be a separate MKT). Pro may also receive it (operator's
                call). Retention-EXEMPT (keep-current-only and the 30d prune
                both skip it). Captions PROVISIONAL (3 operator lines, offset
                9, no numbers) — the real family is yours, against the built
                reel (work order item 11).
     ⚠ DB CHECK migration (`scripts/migrations/2026_08_19_mkt62_verify_
       midday_kind.sql`) is an OPERATOR STEP — no DDL path from the build
       env. Render + assemble work without it; publish names the file if
       the upsert is rejected.

D. CARRIERS (your main deliverable; spec §2/§7): rotating sets —
   allday_pro ×4, allday_free ×4, evening_pro ×4, midday_pro ×4 (MKT-59,
   8/19), verify ×3 (MKT-59 — a 4th, ledger, HELD on the seam proxy; see
   v3.8) — each joined to ONE shared pt2 (verify: window-resolved chain);
   single-entry — midday_free, evening_free, allday_public; PINNED single-
   part (never to be rotated) — verify_midday's `verif_carrier_sameday`
   (its lines are false anywhere else; MKT-62). Ceiling on the
   JOIN ≤20.0s; pt2 last word by ~9.47s of pt2 (margin
   floor +0.184s); generator emits 10.005s presets, trimming is the norm.
   FREE BRIEF SPLITS BY SCOPE: All-Day free = pure value, no Pro pitch;
   session free = NAMES the withholding, pricing ($2.49/mo) permitted there
   and nowhere else; first-access framing barred below Pro.

E. CAPTIONS (§5): template engine at publish, date-deterministic, 12 families
   per kind (allday_public 8); kinds = allday_pro/free/public, verify (free
   draft) + verify_pro, verify_public, midday_pro/free, evening_pro/free,
   verify_midday (PROVISIONAL ×3 — family owed, MKT-62); YT
   title/description/tags as a per-platform transform (MKT-37). Numbers policy
   absolute: real counts/states ONLY on pro kinds. Telegram (tier 4) reshapes;
   YouTube/TikTok/Reddit/X registered but DISABLED (operator-only flip, one at
   a time); Instagram = do-not-build.

F. PUBLIC CAPTURE (MKT-30/16/40/55): `--redact --relabel` = structural digit
   masking (placeholder + gold bolt, never blurred real pixels) + eight copy
   slots + HEAT_RELABEL 11 neutrals + prose-form pick→signal swap (MKT-55,
   8/14) + "ENERGY N" carve-out (MKT-57), every rendered string linted at
   tier 1 fail-closed; stored Q1/Q2 gate record REQUIRED or reel:check FAILS.
   ⚠ Any product/design change to the Home tiles or the six modals is a
   PUBLIC-CAPTURE surface change — new consumer copy must clear tier-1 lint
   before the next morning run or the run aborts (MKT-55 lesson).

G. OPEN / OWED — the standing list (everything else you may have read as
   "open" is closed; see §9 for the closure record):
     1. NATIVE 1080×1920 source assets — generator ceiling confirmed 4×;
        upscale pilot is the only path; blocked on generation balance.
     2. STINGER MOTIONS — NOW 6, ASK STILL OPEN FOR THE SEVENTH (MKT-58,
        8/19): etch + facet REGISTERED and serving; shutter REJECTED (paced
        for its 10s master — see v3.7). Pool 6 = 1 same-day doubling, echo
        36d; SEVEN clears it (v3.6 Q7). ONE more motion closes the ask —
        regenerate shutter (or a new concept) AT THE 4s PRESET against the
        v3.6 spec; `shutter` name stays reserved for the regen.
     3. INTRO QUEUE — PARTIALLY closed 8/19 (MKT-61): juggle REGISTERED
        (pool 9 → 10; visor never tracks — confirmed by eye); coffee
        LANDED but HELD for two rulings (1.94s silent head vs audio-from-
        frame-one; the mug hold measures ~0.65–0.8s vs the ~1.0s floor —
        v3.9); reveal NOT generated (budget). Open at 10 → 12.
     4. CARRIER ROTATION — PARTIALLY closed 8/19 (MKT-59): midday_pro 1→4
        and verify 1→3 now rotate. STILL single-entry: midday_free,
        evening_free, allday_public — those rooms hear the identical opening
        daily; unwritten, budget-gated. Owed to the operator's ear:
        verif_carrier_ledger (held), facet's 2.4s crest (MKT-58), and the
        two MKT-43 gates (seam; signoff_short).
     5. A SECOND PUBLIC INTRO (PUBLIC_INTROS) and/or a SEAL VARIANT
        (VERIFY_SEAL_MOTIONS) — one-line registrations; public-clearance
        FIRST, per file, evidenced.
     6. MORE MKT-11 PANELS — PARTIALLY closed 8/19 (MKT-60): set is SEVEN
        (brand, coverage, anchor, app[regenerated art], zk30, receipts,
        measures; panel_signals RETIRED — measures restates it). Served from
        the bucket, byte-verified. panel_cadence ("NEW BOARDS. EVERY DAY." /
        "ALL-DAY · MIDDAY · EVENING") written, ungenerated: 7 → 8 open.
        Two truth questions raised for the operator: panel_zk30's "COMING
        SOON" vs the closed per-state path; panel_coverage's two-digit
        numerals (cleared 7/27). Copy is still BAKED PIXELS (v4.0 §F).
     7. MKT-15 operator DEVICE test of the social deep links — still owed.
     8. MKT-53 audit entry (SocialBriefCard per-variant layout) — owed by
        whoever shipped it; not a reel asset.
    10. MKT-62 — THE MIDDAY VERIFY REEL: BUILT 8/19, first live build pending
        the day's ledger. OWED: (a) the CAPTION FAMILY for `verify_midday`
        (free-room voice; no numbers; 12 lines like the fleet) — write it
        against the built reel + contact sheet; (b) a COPY PASS on the
        kind's provisional strings — covered board eyebrow/sub/foot ("POSTED
        h:mm · TODAY" / "MIDDAY · SIX SIGNALS · COVERED, AS THE FREE ROOM
        SAW IT" / "GRADED h:mm · SAME DAY"), graded board eyebrow ("GRADED
        h:mm · TODAY"), band sub-line ("POSTED TO THE FREE ROOM COVERED ·
        GRADED AGAINST TODAY'S MIDDAY DRAWS"); (c) OPTIONAL: a sign-off
        continuation for the sameday carrier (closes the 1–4s silent tail);
        (d) MKT-62b — official-results-page screenshots as a RECEIPT beat:
        follow-on work order, ruling recorded (results pages ONLY, never
        payout/consequence content; source field + reviewed flag in the
        upload path; third-party wording through the brand filter first).
        NOTHING in this kind enters a rotation — do not propose variants of
        rise / sameday / the carrier for pooled use.
     9. FIRST-APPEARANCE EYEBALLS owed on the rotation: `juggle` first draws
        evening_free 8/20 (then midday_free 8/21, evening_pro 8/22); etch /
        facet stingers 8/20 (v3.7); `guest` served
        allday_free on 8/19 (per `npm run reel:rotation` — the MKT-54 note's
        "allday_pro 8/19" predated a reshuffle) — look at that cut; `arrive`
        is group-tiers only (it drew midday_pro 8/19).
   NOT COMING / REFUSE IF OFFERED: allday_free_carrier_pt2 raw, midday_free
   _carrier_pt2 raw, the third 7/30 free pt2, the 7/29 seal take — each would
   overwrite a serving file with a gate-failing take. `watermark_source.mp4`
   is evidence only. The MKT-46 ad lane is PARKED (F3 glyphs; head trim
   impossible — never re-attempt). The 1:1 cut is RETIRED.

H. DELIVERY SPEC, SHORT FORM (full text §7): exact lowercase FINAL registry
   name · .mp4 container (a .mov or .MP4 is invisible to the registry and the
   stray scan — seventh failure class) · direct upload, NEVER a GitHub-web
   rename (2-byte stubs, twice) · ffprobe on landing · 10.005s masters,
   intros trimmed to 5.6–6.5s with IN = later of picture cut and audio
   onset, end on clean full-frame smoke, chip zone clear at frame one · every
   motion brief never-moves-first (travelling forms land unpredictably).
   Write generator prompts as SCENE STATES, not negatives (negatives trigger
   refusals); two-hander shots = every figure a machine.

I. BARRED, DELIBERATELY (do not propose): auto-posting; a public-page reel
   lane; full-fidelity free session reels; captions moving numbers outside
   pro; bypassing lint / Two-Question gates; combo-compare in verify
   (classifier = renderer defect, MKT-35); per-kind lockup tucks (MKT-45);
   re-populating the bands from a gap measurement; baking the brand domain
   (hitmasterzk.com — secured, NOT wired) into any asset without a per-
   surface tier ruling; Instagram.

WHAT CHANGED IN v4.3 (2026-08-19, evening — MKT-62 THE MIDDAY VERIFY REEL:
built on the work order + six rulings; first live build waits on the ledger)
  A. RULINGS TAKEN (content agent, 8/19): (1) track-record `?capture=1&
     scope=midday` — capture-gated, recorded as a SANCTIONED EXCEPTION to the
     MKT-51 rig contract (header comment + MASTER_AUDIT MKT-51 addendum);
     (2) NO STINGER (63.8% floor day = the barred number; chip identifies;
     MKT-46 precedent); (3) timestamp pair → the summary band, app untouched;
     (4) cover-lift option (a) — board rendered covered → graded, one page,
     two layers; (5) `endcard_motion_confirmed` → RENAMED
     `endcard_motion_sameday` before registration; (6) MKT-62b = follow-on,
     results pages ONLY (never payout) — encoded in the upload path when it
     is built, not in a doc line.
  B. WHAT NOW EXISTS: kind `verify_midday` at every registration point
     (ReelKind, audience FREE, KIND_UI ⏱️ "Midday · Same-Day Verify", publish
     mode with --variant REFUSED, caption family PROVISIONAL, retention-
     EXEMPT in both functions, `npm run reel:verify-midday`; reel:daily ORDER
     untouched — asserted by reel:check). Renderer: TODAY + midday scope,
     three fail-closed provenance aborts (midday_free never posted / no
     graded midday rows / GRADED ≤ PUBLISHED), band injection asserted cell
     by cell, cover-lift with a digit-free/result-free assert on the covered
     layer. Assembler: pinned set, no stinger (stated as a ruling in code),
     TODAY chip + ribbon, 5-frame contact sheet.
  C. THE FIXED SET, registered PINNED: `anchor_intro_rise` (IN 0.56 / OUT
     6.45 = 5.96s v; master `_master_10s` kept; chip zone on the seated head
     frames = same class as verify/juggle/board — composited and eyeballed),
     `verif_carrier_sameday` (set of one, rest [], not in CARRIER_KINDS),
     `endcard_motion_sameday` via NEW `FIXED_ENDCARD_MOTION` + `endcardMotion
     SetFor()` (every pool enumerator routed through it; the kind sits in no
     ENDCARD_KINDS tier, the motion in no pool). Built endcard on the shared
     1240 band; lockup block ends y=1400. reel:check: 0 FAIL / 106 WARN; NEW
     `checkVerifyMidday()` (15 assertions, all PASS). `reel:rotation` today:
     unchanged for every daily kind (verify=record, midday_pro=ahead, …);
     verify_midday appears in no lane table (not a daily kind).
  D. MEASURED, NOT ASSUMED (Phase 0, live data): midday rows 21/30 days ≥1,
     median 2, mean 1.67, straights 6/21 days; body 8.4 / 10.6–11.0 / 12.8–
     13.2s (+2.5s cover-lift); PUBLISHED is ~8:14 AM in practice (fleet
     posts 8:12–8:16, not the schedule's 10:30) — the truth is the better
     claim (~8h gap). Full report: docs/mkt62_phase0_report.md.
  E. FIRST LIVE BUILD DONE (ledger landed 20:27Z): 618 STRAIGHT in NC —
     PUBLISHED 8:14 AM · GRADED 4:27 PM · ELAPSED 8h 13m. Body 11.30s,
     total 23.77s, furniture 52.4% (the 1-row floor day), both devices in.
     Contact sheet: verify_reels/verify_midday_20260819_contact.png. One
     defect found + fixed ON THIS KIND: a first-viewport featured row sat
     under the y=470 ribbon through the hold → verify_midday's ribbon now
     rides the board segment only (the band + "Today" header carry the
     claim after). ⚠ The same geometry is latent on VERIFY on first-row-
     featured days (8/18's sheet shows it) — a ruling is owed: fade at the
     summary cut there too, or move the ribbon on collision days.
     PUBLISHED 2026-08-19 ~20:50Z once the operator applied the DB CHECK
     (first paste dropped the leading ALTER TABLE → 42601; re-sent as two
     one-line statements). Row registered in Admin → Reels (⏱️ Midday ·
     Same-Day Verify, target free). WAITING on you: caption family + copy
     pass (§G.10).

WHAT CHANGED IN v4.2 (2026-08-19 — POST-BATCH REPORT to the content agent's
fifteen questions; answers only, everything measured earlier today — the
detail lives in v3.7/3.8/3.9/4.0 and the MKT-58/59/60/61 audit entries)
  MKT-58 — 1. NOT closed. Pool is 6 (etch + facet in, shutter out):
     reel:rotation = ONE same-day doubling every day (allday_pro ↔
     allday_public), arrangement echo 36d, and the inside-a-week warning IS
     gone (was 6d). Zero doublings / 119d needs the seventh. 2. Band
     y1300–1450 %>L40 at 1.3/1.6/2.0/2.2: etch 2/0/0/15 · facet 0/0/4/37
     (the 37 is the smoke return under the text-out; std itself reads 44 at
     2.3) · shutter 43/29/25/27 — smoke, not a mark; nothing reproduced
     imprint's 56–59 hold, but shutter is paced for its 10s master (bands
     2.4–2.8, bolt 3.0–5.0) so inside 0–3.0s there is no hero beat and the
     dissolve source is a white flash → REJECTED, regen at the 4s preset.
     3. Hero: etch −3.5dB@1.4 (ramp) · facet −5.0@1.0 · shutter −9.1@1.0.
     Circuit defect (isolated ≥2.4s into ≤−18dB): NONE of the two registered
     — etch late max −5.7@2.4 on −14.5 material (crest); facet has a sharp
     12dB rise at 2.4 (−4.0 on −15.4 material — build-flagged, not the
     quiet-field class, no fade, ear owed). shutter: −64dB hole at 1.8 then a
     swell to −6.2@2.9 = the attack-blind LISTEN class (moot, rejected).
  MKT-59 — 4. All four per kind 10.000s video / 10.005s audio (midday
     incumbent/set/ahead/ready; verify incumbent/record/checked/ledger) —
     law holds. 5. Joined margin is set-invariant by the law: midday join
     20.360, pt2 last word 19.82, +0.19 vs the 20.01 fade (= incumbent's
     +0.184). Part-1 tails: set 0.51 / ahead 0.38 / ready 0.47 (incumbent
     0.30); verify record 0.85 / checked 0.92 / ledger 0.34 (incumbent 0.73).
     NOTHING salvaged, nothing unsalvageable — no gap-tighten needed.
     6. Seam f0 proxy vs pt2 open (accepted band ≤~Δ11, overlapping IQR):
     midday set Δ2.7 ✓ · ahead Δ8.0 ✓ · ready Δ9.3 ✓; verify record Δ6.4 ✓ ·
     checked Δ9.5 ✓ · ledger Δ12.3 with NON-overlapping IQR (87–93 vs
     96–107, falling cadence on "taken away") → HELD for the operator's cold
     listen. ⛔ Tier-1 lint on REAL transcripts (faster-whisper, now in-env):
     record / checked / ledger ALL CLEAN at tiers 1/2/4 — no direct address,
     no time-of-day words, no counts/states. Midday three clean at tier 4,
     "Midday" spoken aloud, no price, first-access framing present.
     7. Window walk keys on AUDIO durations, identical across the set, so the
     boundary is unmoved for every variant: full pt2 (join 16.815s) when
     body ≥ ~9.87s — every featured-row day (e.g. 13.8s); signoff_short
     (12.827s) on a 1-box day (body ~9.0s); silence never in practice. Chain
     fires both directions for record and checked (and would for ledger).
  MKT-60 — 8. app ("SIX SIGNALS. EVERY MORNING.") vs measures ("FOUR
     MEASURES BEHIND EVERY SIGNAL") read as DISTINCT — six daily signals vs
     the four measures inside each. The real duplicate was signals ↔
     measures (same four components, old vs DESIGN-03 vocabulary) → operator
     ruled measures REPLACES signals; set is SEVEN. 9. measures' third line
     survives at the ~1000px in-modal width (smaller gold caps, comparable
     to the others' second lines) — not a regen candidate. 10. Served bytes
     vs local build: ALL SEVEN MATCH (brand c647e54f · coverage 11216978 ·
     anchor 05ea4421 · app e8c4691a · zk30 9fa6e02a · receipts 97575e86 ·
     measures ce2c3c86); old panel_app (bd45a84b) gone from the bucket, old
     source/built replaced locally (git history only); panel_signals bucket
     object deleted (fetch → 400). 11. Filename KEPT (operator ruling);
     recorded as historical in §7 and panel-config. ⚠ Premise correction:
     the "THE APP IS COMING" copy was NOT live anywhere since 7/29 — this
     was an art swap with identical copy.
  MKT-61 — 12. coffee: mug reaches the face ~1.6–1.75s, lowering by ~2.45s
     → hold ≈ 0.65–0.8s, UNDER the ~1.0s floor → HELD, operator rules (also
     a 1.94s SILENT head — ambience from frame one did not generate, so the
     in-point rule would cut into the hold). juggle: the visor NEVER tracks
     the phone (eye check 0.25–3.3s at 1/8s steps) → REGISTERED.
     13. juggle IN 0.450 (audio onset; picture continuous) / OUT 6.650 =
     6.208s; smoke from ~5.9s of the master; chip zone x520–1008/y500–700
     ≤1.06% phone pixels from 0.0 through 2.65s; apex y≈497 — inside the
     y420–1500 band by 77px; open RMS −33.5dB. coffee (untrimmed): onset
     1.94s, chip zone clear across the hold (steam centre-left), mug plain,
     smoke from ~5.5s; open RMS −44.4dB at the onset. Both masters
     10.000/10.005s, no .mov/.MP4 strays.
  ACROSS — 14. REGISTERED: stinger etch + facet; carriers midday set/ahead/
     ready + verify record/checked; intro juggle; panels receipts +
     measures + regenerated app. REJECTED: shutter (regen). HELD: verify
     ledger (ear), coffee (ruling). RETIRED: panel_signals. Asks: §G.2
     stingers PARTIAL (6 of 7) · §G.3 intros PARTIAL (10; coffee held,
     reveal ungenerated → 12) · §G.4 carriers PARTIAL (midday_pro + verify
     rotate; midday_free / evening_free / allday_public remain single-entry)
     · §G.6 panels PARTIAL (7 served; cadence ungenerated → 8).
     15. Strays: none unexplained — shutter / coffee / ledger carry
     UNREFERENCED_OK reasons; the three 8/19 deliveries that registered
     left the stray list. New warning classes: none beyond 720×1280 (+ the
     24fps note juggle joins); facet's "uncovered transient at 2.4s" is the
     existing build-time class; carrier lane's 6d arrangement-echo note is
     pre-existing. reel:check 0 FAIL throughout.

WHAT CHANGED IN v4.1 (2026-08-19, session close — ledger of the day, cleanup,
and what is waiting on the operator)
  A. TODAY'S VERSIONS: v3.5 resync + snapshot · v3.6 stinger spec answers ·
     v3.7 MKT-58 (etch/facet in, shutter out) · v3.8 MKT-59 (midday_pro ×4,
     verify ×3, ledger held) · v3.9 MKT-61 (juggle in, coffee held) · v4.0
     MKT-60 (panels 7, published) · this note. Commits 08b262a → 2c1a540 +
     this one, all pushed. Audit entries MKT-58/59/60/61 written.
  B. WAITING ON THE OPERATOR (rulings/ears — nothing blocks the daily run):
       · shutter regen at the 4s preset (closes MKT-58 at 7);
       · coffee: silent-head + short-hold ruling (three options, §7);
       · verif_carrier_ledger cold listen (one-line registration);
       · facet's 2.4s crest — ear;
       · panel_zk30 "COMING SOON" and the stamp-chip-over-modals
         observation (v4.0 §G and the side note);
       · device check of the in-app panel rotation until the next release
         (retired panel_signals.png is no longer served).
  C. NEXT ASSET ASKS, in value order: shutter regen (stinger 6→7) · reveal
     intro (10→12 with coffee) · panel_cadence (7→8) · carrier part-1 sets
     for midday_free / evening_free / allday_public · a second public intro
     or seal variant (clearance first). Generator lesson of the day, twice:
     10s-preset masters pace their beats to 10s — brief the 4s/6s preset or
     script beat timings against the 6.5s (intro) / 3.0s (stinger) cut.
  D. ENVIRONMENT: faster-whisper (small, int8, CPU) is installed on the
     codespace — carrier transcripts are measurable; the base/small.en
     models were removed to save space, `small` kept. The detached dev
     server started for the MKT-60 in-context capture was STOPPED (port
     8081 free for the morning `start-tunnel`). Disk: ~4.5G free after
     cleanup (the git pack is ~4.5G of binaries; a full gc needs more temp
     space than the codespace has — do not run `git gc --prune` here, the
     8/19 attempt was aborted at 99% disk and its temp pack removed).
  E. UNTRACKED-BY-DESIGN: stinger_motion_shutter.mp4 is now committed as
     the rejected evidence copy (same name the regen replaces — the
     UNREFERENCED_OK entry names it); `_preview_pro_brief_2026-08-07.png` at
     the repo root is an older untracked preview, left as found.

WHAT CHANGED IN v4.0 (2026-08-19, same day — MKT-60 EXECUTED on the amended
order: panel set 6 → 7 — panel_app art REGENERATED, receipts + measures NEW,
panel_signals RETIRED; served from the bucket and byte-verified)
  A. RULINGS APPLIED (operator): panel_app KEEPS its filename (slot name is
     historical — recorded in §7 and in panel-config); measures REPLACES
     signals. Before the swap the old artwork was confirmed in git (sha
     1af55fb…), so no manual backup was needed.
  B. PHASE 1 — spec: all three ~3:2 (1536×1024), centred band detected as
     the only content run (no watermark row inside or below the band),
     aspects 3.50 / 3.51 / 3.41:1 against the briefed ~3.5:1 (serving set
     2.97–4.40); feathered 10px on all four edges at build. LEGIBILITY at
     the ~1000px in-modal width: every line reads, INCLUDING measures'
     third line ("FOUR MEASURES BEHIND EVERY SIGNAL" — smaller gold caps,
     comparable to the other panels' second lines). Not a regen candidate.
  C. PHASE 2 — manual clearance from rendered pixels (the check lint cannot
     make): all strings exact incl. periods and middle dots; tier-neutral
     (no price/CTA/Pro pitch); TRUE IN BOTH ROOMS (a reel viewer without
     the app and a subscriber inside it read the same true sentence); no
     numerals ("SIX"/"FOUR" are words); no glyphs in the light; receipts'
     ring is PLAIN; measures' four rules are equal-length plain marks, not a
     chart. Pinned: app d0dfc723… · receipts 745ce761… · measures cc518b0d…
     (cleared 2026-08-19).
  D. PHASE 3 — built 7/7 and PUBLISHED to the app-panels bucket; served
     bytes fetched back and compared: ALL SEVEN MATCH the local build
     (panel_app served sha e8c4691a… — the old bd45a84b… object is gone);
     panel_signals object DELETED from the bucket (fetch → 400); old
     panel_app artwork exists only in git history. reel:check: "all 7
     served from the app-panels bucket and byte-matching the local build",
     0 FAIL.
  E. PHASE 4 — rotation at 7 with 6 modals/day: distinct six per day, no
     gap, no double-draw (simulated 8/19–8/26; it is a sliding window, so
     day-to-day NEIGHBOURS persist — brand always follows measures etc.).
     In-context proof: an allday pro body was rendered against the live
     dev server (not assembled, not published — today's reels stand) and
     100% crops of modals 3/5/6 with app / receipts / measures in place
     below "RESOLVED IN" are at assets/marketing/panels/_mkt60_incontext_
     20260819.png — all three read at rendered size.
     ⚠ DEPLOYMENT NOTE: the CAPTURE reads REEL_PANELS from the dev server,
     so tomorrow's reels carry the new set; SUBSCRIBER DEVICES read
     REEL_PANELS from their installed/served app build — a release (or the
     web redeploy) is what moves the in-app rotation from 6 to 7. Until
     then devices keep requesting panel_signals.png, which the bucket no
     longer serves → that slot falls back to whatever the component does
     on a 400 (verify on device; re-uploading the retired file is a one-
     line undo if it renders as a hole).
  F. STANDING QUESTION (reported, not built): converting panel copy from
     baked pixels to config strings = ReelPromoPanel renders the lines
     from reelPanels.ts over TEXT-FREE art beds (the MKT-10 pattern);
     clearance becomes lint-of-strings + one sha per art bed; every future
     wording fix is a string edit. Cost: one component change, seven text-
     free art regenerations (or crops of the current beds), a lint hook.
  G. Two truth-in-both-rooms questions surfaced while reading all the copy
     together, for the operator: panel_zk30 "SINGLE-STATE ENGINE · COMING
     SOON" against the per-state path closed at ENG-PERSTATE-P0 (8/13); and
     panel_coverage's two-digit numerals (39 / 40+) — cleared 7/27, Q1 is
     about three-digit combinations, noted only.
  ⚠ SIDE OBSERVATION (not this order): on the ASSEMBLED reel the "TODAY'S
     DATA DROP" stamp chip (y=1000, MKT-57) persists through the MODAL
     segments too and sits over the WHY THIS ORDER rows (seen on today's
     allday_pro at 19.5s). It has shipped that way since 8/17; flagging so
     the operator can rule whether the chip should fade at the still→modal
     cut.

WHAT CHANGED IN v3.9 (2026-08-19, same day — MKT-61 EXECUTED: juggle REGISTERED,
pool 9 → 10; coffee HELD for two rulings; reveal ungenerated — §G.3 open 10 → 12)
  A. Both landed as real mp4 at exact lowercase names (no .mov/.MP4 strays
     anywhere in assets/marketing), 10.000s v / 10.005s a, 720×1280 24fps,
     nothing overwritten. Masters kept (juggle renamed *_master_10s; coffee
     left as delivered).
  B. ✅ juggle: IN 0.450 (audio onset) / OUT 6.650 = 6.208s. THE GAG HOLDS —
     the visor never tracks the arc (eye check, 1/8s steps). Phone apex
     y≈497, inside the 1:1 band by 77px; chip zone clear; smoke from ~5.9s;
     open −33.5dB RMS. §7 has every number. First draws: evening_free 8/20,
     midday_free 8/21, evening_pro 8/22 — eyeball the 8/20 cut.
  C. ⏸ coffee HELD, not rejected: technically clean everywhere (unmarked
     mug, clean wall, clear chip zone, smoke from ~5.5s) but (1) the master
     is SILENT for its first 1.94s — the "ambience from frame one" in the
     brief did not generate — so the in-point rule collides with the joke;
     and (2) the mug-at-face hold measures ~0.65–0.8s, under the ~1.0s you
     set as the floor. Three options are written in §7; the operator rules.
     ⚠ Generator note for the regen lane: both concepts delivered at 10s
     with the beats paced to 10s (shutter did the same in MKT-58) — ask for
     the 4s/6s preset where the tool offers it, or script the beat timings
     against a 6.5s cut explicitly.
  D. Rotation: intro lane "6 rotating · pool 10 · never repeats a full
     arrangement inside 300d" (was 186d at 9). The pinned kinds (verify,
     verify_public, allday_public) are untouched by design. reel:check
     0 FAIL / 102 WARN (juggle joins the 720×1280 + 24fps warning class).
  E. §G.3 is PARTIALLY closed: juggle in; coffee held; reveal ungenerated
     (prompt held, budget-gated; trim ~OUT 7.4, 6.5s ceiling wins). Pool
     10 → 12 remains the ask.

WHAT CHANGED IN v3.8 (2026-08-19, same day — MKT-59 EXECUTED: midday_pro 1→4
and verify 1→3 carriers rotate; verif_carrier_ledger HELD on the seam proxy)
  A. Six part-1 files landed, all real mp4, exact lowercase names, no
     strays, incumbents untouched (July/Aug-1 mtimes). ⛔ FILE LAW: all six
     10.000s video / 10.005s audio — same as every incumbent, so the joined
     margin is set-invariant (+0.19s to the 20.01 fade for the midday join,
     unchanged from the incumbent's +0.184) and the verify window boundary
     does not move. No MKT-32 gap-tighten needed anywhere.
  B. TRANSCRIPTS ARE NOW MEASURABLE IN-ENV: faster-whisper (small, int8) is
     installed on the codespace — every carrier in this order was
     transcribed and run through the real lintCaption at the tier it
     serves. The lines you scripted match what the voice says (one
     wording drift: "stamp for the draw" / "first looks yours" on `set` —
     the read, not the copy). All clean; verify's three are tier-1 clean
     (shared with verify_public), no "partners", no time-of-day words.
  C. REGISTERED: midday_pro set/ahead/ready + verify record/checked.
     HELD: verify `ledger` — seam f0 proxy Δ12.3 vs the pt2 open with
     NON-overlapping IQRs (the exit drops to 87–93 Hz on "taken away";
     pt2 opens 96–107). The MKT-48 accepted band was ≤~11 with overlapping
     IQRs. It is a cadence, not a different voice (whole-file median 107.4
     vs pt2 104.6) — which is exactly what an ear decides and a proxy
     cannot. One line registers it if the operator's cold listen passes.
  D. ROTATION (`reel:rotation`): carrier lane "5 rotating · pool 3/4";
     depth midday_pro 163 → 257, verify 4 → 12 (verify_public shows 5 in
     the table because the shared carrier is resolved under kind `verify` —
     both halves draw the same file; the display does not count it). The
     lane's pre-existing inside-a-week arrangement echo (6d) and the "1
     repeat" lockstep note stand — they are set-POSITION echoes across
     mixed pool sizes, not shared files. STILL SINGLE-ENTRY: midday_free,
     evening_free, allday_public (snapshot §G.4 — partially closed).
     First draws: 8/19 midday_pro would now draw `ahead` and verify
     `record` (today's reels shipped this morning under the single-entry
     sets — not re-run); 8/20 midday_pro = set, verify = checked.
  E. reel:check 0 FAIL / 102 WARN (720×1280 class + the MKT-61 intros +
     panels still pending). CARRIER_KINDS += midday_pro, verify (appended —
     live-kinds-first; the three existing rotating kinds keep their index).

WHAT CHANGED IN v3.7 (2026-08-19, same day — MKT-58 EXECUTED: etch + facet
REGISTERED, shutter REJECTED; stinger pool 4 → 6; THE ASK IS NOT CLOSED)
  A. LANDED AND GATED (all three real h264/aac mp4, exact lowercase names,
     no .mov/.MP4 strays, nothing overwritten — the serving files kept
     their July mtimes): etch 4.000s video / 4.010s audio; shutter 10.000/
     10.005; facet 10.000/10.005 — all 720×1280 24fps. Two at the 10s
     preset, which is where shutter's failure came from (below).
  B. ✅ etch + ✅ facet PASS the v3.6 band gate with margin (etch 2/0/0%,
     facet 0/0/4% at 1.3/1.6/2.0 — std is 34/28/1), end on full-frame
     smoke, open on smoke, carry no text/glyph/numeral (the generator's
     small sparkle watermark bottom-right is on EVERY serving motion
     already — fleet-wide, not a rejection). Lockup frames at 100% crop
     (1.6s, allday_pro builds) read clean by eye on both. §7 has every
     number. Audio: etch clean; facet's smoke-return crest lands at 2.4s
     as a sharp 12dB rise on −15.4dB material — the build flags it as an
     uncovered late transient; it is NOT the circuit class (material is
     not quiet) and gets no authored fade; ⚠ operator ear owed.
  C. ⛔ shutter REJECTED — pacing, not concept. The generator paced the
     concept across its 10s master: smoke to 2.2s, the three white bands at
     2.4–2.8s, the bolt standing 3.0–5.0s, smoke back at ~6s. The reel
     consumes 0–3.0s only (v3.6 Q5), so what the stinger would actually
     show is smoke → a white flash where the dissolve source must be smoke
     → a bolt edge at the cut — and the bands read as LETTERFORMS (the
     order's item 6 risk, realised). Audio has a −64dB hole at 1.8s then a
     swell into it (the attack-blind LISTEN class). No trim fixes both ends.
     ⚠ REGENERATE shutter AT THE 4s PRESET with the SAME concept and name —
     the 4s preset is what etch landed on and it paced correctly; the 10s
     preset stretched the beats. Nothing else about the brief changes.
  D. ROTATION AT SIX (measured with `npm run reel:rotation` after the
     registration): "STINGER — 7 rotating · pool 6 · closest identical arrangement 36d
     apart (2026-07-24 → 2026-08-29)", ONE same-day repeat every day
     (the allday_pro / allday_public pair, indices 0 and 6 mod 6), and the
     stinger lane's "⚠ inside a week" warning is GONE (was 6d at pool 4).
     Today's 8/19 reels shipped this morning under pool 4 and are NOT
     re-run; first full-reel draws: 8/20 allday_pro + allday_public =
     facet, allday_free = etch (8/21 allday_pro/public = etch, evening_
     free = facet).
     Pool 7 is still the number that clears the doubling (v3.6 Q7) — one
     more motion closes MKT-58. Report language: the ask is NOT closed.
  E. Registration + build: two lines in STINGER_MOTIONS (labels "inscribed
     outline" / "hazed planes"), STINGER_KINDS untouched, full matrix
     rebuilt (`stinger:build`). Build warnings as logged: etch — NONE (no prominence hit; late max −5.7dB@2.4 on −14.5dB
     material is a crest, not flagged; no authored fade). facet — "uncovered
     transient(s) at 2.4s — inside the last 0.6s of the 3s window … consider
     an authored fade" on all 8 builds (expected, see B; no fade applied,
     ear owed). seal — its standing "LATE PEAK 2.7s −5.7dB after −20.8dB"
     LISTEN line, unchanged since MKT-31. 49 clips rebuilt (8 variants × 6
     + verify×seal; verify_public shares verify's builds). reel:check:
     0 FAIL / 108 WARN (the 720×1280 class + the day's pending-registration
     strays for MKT-59/61), stray scan names shutter → now UNREFERENCED_OK
     with the rejection reason.
     `reel:check`: see the MKT-58 audit entry for the exact result.
  F. First-draw days for the new members (so the first full reels can be
     eyeballed): see the MKT-58 audit entry / `npm run reel:rotation`.

WHAT CHANGED IN v3.6 (2026-08-19, same day — ANSWERS to the content agent's
nine pre-generation questions for the stinger session; MEASURED, no work done)
  Method: the four serving motions (+ imprint, seal) were re-measured today at
  1080×1920 (scaled) on luma, and on audio with the SAME peaks() pass the
  builder uses (100ms buckets, mono). Rotation arithmetic was simulated with
  the real laneRotate over 400 days. Nothing below is a recollection.

  Q1. THE LOCKUP BAND, IN PIXELS (⛔ answered).
      · Shared LOCKUP_TOP = 1330. The rendered slate text block (46px bold
        wordmark + 14px gap + 24px medium headline) occupies y≈1341–1422 —
        ~92px tall, centred, white. Verify's headline-dominant layout lays
        the large gold/white line at y≈1345–1389 (+ small line beneath).
      · HARD LIMIT: block bottom must stay ≤ 1500 (CROP_SAFE_BOTTOM — the
        1:1 band y420–1500). The builder measures the laid-out block and
        ABORTS past it. The bolt/mark may occupy y615–1294 (std's measured
        extent); nothing requires the mark to reach the text.
      · THERE IS NO NUMERIC CLEARANCE GATE. MKT-45 deleted the 380px strand
        gate and emptied MOTION_LOCKUP; placement is a whole-frame judgement
        by eye and every kind sits on 1330. So brief to the TEXT-READABILITY
        numbers, not to a clearance: the band y1330–1430 must be DARK while
        the text is up — measured serving values, % of band pixels > L40
        (mean luma in brackets) at t = 1.3 / 1.6 / 2.0 / 2.2s:
          std       34% (L40) · 28% (L38) ·  1% (L17) ·  1% (L16)
          strike    11% (L26) ·  0% (L15) ·  0% (L14) ·  0% (L14)
          circuit   59% (L63) ·  2% (L22) ·  0% (L17) ·  0% (L17)  ← 1.3s is
                    the bolt passing through; clear by 1.6
          fracture  64% (L79) · 78% (L75) · 32% (L44) · 19% (L38)  ← the
                    brightest-under-text member and the OUTER BOUND that
                    passed (accepted on the one-hero-beat rule; the text
                    reads because the field is diffuse). Do not brief past it.
          imprint   98% (L127)· 53% (L47) ·  4% (L16) ·  2% (L15)  ← REJECTED
        Target for a new motion: ≤ ~35% / mean ≤ L45 at 1.3s, ≤ ~10% by 1.6s,
        ~0% from 2.0s. Above the band, anything goes — the mark settle
        heights in the fleet are y1294 (std bolt), y866 (circuit bolt), ~y700–
        738 (seal ring); a high-settling mark with a void above the text is
        the motion's negative space BY RULING (MKT-45), never a defect.
      · Your "45–72% of height clear" (y864–1382) overlaps the text band by
        ~50px and under-reaches it by ~40px; it passed on the seal because
        the seal is dark everywhere below y~740 from 1.0s. Replace it with:
        "y1300–1450 dark from 1.3s; composition inside y420–1500; mark may
        settle anywhere above ~1294".

  Q2. IS THERE A BED CHECK ON STINGERS? (⛔ answered) — NO. Nothing analogous
      to bedWindow()/the ±6dB clamp runs on stingers; a stinger is never
      looped or bedded. "Level-steady pre-transient audio" buys NOTHING
      here — drop the constraint and take the better-sounding clip. What
      DOES run on stinger audio, all WARNINGS (never a build fail) in
      `npm run stinger:build`, over the USED window 0–3.0s only:
        (a) PROMINENCE transient detector — a local peak ≥12dB above the
            floor of the preceding 0.4s. Any such transient inside the LAST
            0.6s (≥2.4s) is flagged "the circuit defect" (fires after the
            lockup is out, lands on the dissolve). Authored fades are
            per-motion config (circuit: afade from 2.45s, target 2.7s).
        (b) ATTACK-BLIND late-peak check — if the median peak over 1.8–2.3s
            is ≤ −18dB (a quiet field) AND any peak ≥2.4s exists, it is
            flagged LISTEN. A late SWELL riding on material louder than
            −18dB is NOT flagged (std/strike/fracture all crest at 2.6–2.8s
            on −8 to −12dB material and ship daily).
        (c) The assembler fades the stinger's audio to silence over its
            last 0.25s (2.75–3.0s) and normalises the reel to −14 LUFS.
      There is no gate on the OPEN level (the intro's sting is still
      sounding through the 0.3s crossfade). Measured first-0.3s peaks: std
      −32.3 · strike −27.1 · circuit −33.8 · fracture −11.1 dB — a spread of
      21dB with no complaint, so open however the concept wants.
      "Dissolve bed at 3.0s" in §G.2 was VISUAL shorthand — see Q5.

  Q3. THE FOUR SERVING MOTIONS — measured.
        tag       master            used   hero beat            late (≥2.4s)
        std       10.000s v/10.005a 0–3.0  −1.1dB @1.2s (+12dB  −6.7dB @2.8s on
                  720×1280 24fps            prominence hit)     −11.7 material
        strike    10.000/10.005     0–3.0  −2.5dB @1.2s (ramps; −6.0dB @2.6s on
                  720×1280 24fps            0 prominence hits)  −11.3 material
        circuit   10.000/10.005     0–3.0  −4.8dB @1.4s; a 2nd  −4.7dB @2.7s on
                  720×1280 24fps            hit −21.9 @2.2       −24.5 material
                                                                 = THE DEFECT,
                                                                 authored fade
        fracture   4.000/4.010      0–3.0  −0.7dB @1.1s (+12dB  −2.8dB @2.7s on
                  720×1280 24fps            prominence hit)     −8.1 material
      What to MATCH: one hero beat between ~1.0 and ~1.4s (it lands under
      the text-in at 0.9–1.3s), peaking −0.7 to −4.8dB; a smoke-return
      swell from ~2.2s that CRESTS at 2.6–2.8s on top of material ≥ −12dB;
      full-frame smoke at 0.0 and again from ~2.2s. What to AVOID: circuit's
      shape — an isolated hit into a quiet (≤ −18dB) field after 2.4s; and
      fracture's brightness under the text (Q1) — it passed, it is the edge.
      All four are 720×1280/24fps (standing ask #1); fracture's 4.0s preset
      is the cheapest delivery and loses nothing (Q5).

  Q4. IMPRINT'S RETIREMENT — it was NOT audio timing. Imprint's track is
      clean: one hero beat −1.0dB @1.1s, nothing isolated after 2.4s, late
      crest −7.4dB @2.8s on −10.1 material (it reads 0 on the prominence
      detector only because it ramps in over >0.4s, exactly like strike).
      The defect is VISUAL TIMING: its bloom is FULL-FRAME through the
      lockup's fade-in — band >L40: 99% @0.9 · 100% @1.0 · 96% @1.1 · 56%
      @1.2 · 59% @1.3 · 10% @1.4 · 0% @1.5 — so white text at 1330 has
      nowhere to sit until 1.5s, and moving TEXT_IN_START that late leaves
      ~0.7s of readable hold (a per-motion TIMING override, deliberately
      not built for one clip). Design rule, in numbers: the hero flash may
      fill the frame at its peak (~1.0–1.1s) but the lower third (y1300–
      1450) must be back under ~35% >L40 by 1.3s. std does exactly this
      (100% @0.9 → 34% @1.3); imprint did not (56–59% @1.2–1.3).

  Q5. CROSSFADE MECHANICS — the file is consumed 0–3.0s, FULL STOP. The
      builder trims the motion at `-t 3.0` when it composites the lockup
      (so the stinger_<kind>_<tag>.mp4 builds are 3.0s); the assembler then
      trims those to STINGER_DUR again. Nothing resolves a window. Inside
      the 3.0s: 0–0.3s is under the intro→stinger crossfade (INTRO_XFADE
      0.3 — both sides full-frame smoke; the dissolve exists because the
      two smokes differ in saturation, Δ21, so a butt-cut popped); text in
      0.9–1.3, hold to 2.2, out 2.2–2.4; 2.2–3.0 is the stinger→body 0.8s
      dissolve source and must be full-frame smoke returning. Your 3.0–4.0s
      smoke tail is DEAD WEIGHT — unread — but the generator's 4s preset is
      the floor, so it costs nothing extra to leave it; just do not design
      anything into it. Both ends on smoke is the whole contract.

  Q6. TIER-NEUTRALITY / SEAL PINNING — confirmed in code.
      `stingerMotionsFor(kind)`: if kind ∈ SEAL_KINDS = ['verify',
      'verify_public'] and any seal member is live (not `held:`), it returns
      laneRotate over VERIFY_SEAL_MOTIONS ONLY — the shared STINGER_MOTIONS
      array is never consulted for those kinds. The shared pool reaches
      verify only if EVERY seal member is held (the documented pre-seal
      fallback), which is a deliberate re-arm, not a side effect. Adding
      three members to STINGER_MOTIONS changes NO verify / verify_public
      arrangement (the seal lane is seeded and indexed on its own set).
      The pool is tier-neutral by construction: no tier field, no tier
      filter; every slate and public kind draws from it.

  Q7. ROTATION ARITHMETIC — simulated with the real laneRotate, 400 days,
      the 7 live stinger kinds (allday_pro/free/public, midday_pro/free,
      evening_pro/free; STINGER_KINDS also lists the two dormant public
      session kinds LAST, live-kinds-first):
        pool 4 → max 3 same-day doublings, identical 7-kind arrangement
                 recurs within 6d (today's measured state)
        pool 5 → 2 doublings, echo 7d
        pool 6 → 1 doubling, echo 36d
        pool 7 → 0 doublings, echo 119d    ← SEVEN FULLY CLEARS IT
        pool 8 → 0, echo 365d
        pool 9 → 0, echo 365d
      Mechanism: position = (dayclock + kindIndex) mod poolSize, so with
      pool ≥ live kinds every kind lands on a distinct member every day.
      Three files (4→7) is exactly the number; a fourth (→8) buys only the
      arrangement-echo (119d → 365d). Deliver three; an eighth is a
      nice-to-have, not a correction.

  Q8. REGISTRATION SHAPE — one line each in STINGER_MOTIONS (brand-motion.ts):
      `{ file: 'stinger_motion_<tag>.mp4', tag: '<tag>', label: '…' }`. It
      does NOT touch the spread axis (STINGER_KINDS is untouched; it is the
      consumer list, not the pool). What DOES happen, inherent and accepted:
      widening a date-derived pool reshuffles every kind's draw from the
      registration day (same as INTRO 6→9 did on 8/13) — nothing re-collides
      because the collision count is a function of pool size (Q7), and at 7
      it is zero. Then: `npm run stinger:build` (rebuilds 9 kinds × 7
      motions + seal = ~66 clips, the build logs the Q2 warnings per file),
      `npm run reel:check` (validates + stray scan; expect the 720×1280
      warning class), `npm run reel:rotation` (shows the new schedule and
      should report the stinger lane's inside-a-week warning GONE).

  Q9. NAME CLEARANCE — etch / shutter / facet are CLEAR: zero hits in
      scripts/, constants/, MASTER_AUDIT.md, this file and assets/marketing
      (no registered, retired, parked, rejected or reserved file or tag in
      any lane). Deliver as `stinger_motion_etch.mp4`, `stinger_motion_
      shutter.mp4`, `stinger_motion_facet.mp4` — exact lowercase, .mp4,
      direct upload (snapshot §H). Reserved/barred for reference: std,
      strike, circuit, fracture, imprint (retired), seal (verify set); pro
      endcard tags reveal/discharge/ignite/lock; free endcard tags std/alt/
      tide/bloom/adopen (+ updraft rejected); intro labels standard/deadpan/
      arrival/monitors/powerup/board/guest/window/arrive (+ coffee/juggle/
      reveal queued — note `reveal` is ALSO a pro endcard tag; the intro
      queue name predates the endcard and is an intro filename, not a
      motion tag, so no resolver collision — but pick a different word if
      you can).

  BRIEF, CONSOLIDATED (what to hand the generator): 4s preset, 9:16,
  720×1280 accepted (native 1080×1920 if the generator ever offers it);
  opens on full-frame dense purple smoke (0–0.3s under the crossfade);
  smoke condenses into ONE hero event peaking ~1.0–1.2s — the bolt/mark
  may fill the frame at the peak; by 1.3s the lower third (y1300–1450) is
  dark (≤35% >L40) and stays dark through 2.2s while the mark holds above
  ~y1294 (anywhere — high settles are fine); from ~2.2s the smoke sweeps
  back and refills the frame fully by 3.0s. AUDIO: one hit under the hero
  event (−1 to −5dB, ~1.0–1.4s), then a continuous smoke-return swell
  cresting 2.6–2.8s on material ≥ −12dB; NO isolated hit after 2.4s, no
  silence-then-pop; open level free. Composition inside y420–1500; no
  glyphs/numerals anywhere (public kinds draw this pool). Write as scene
  states, not negatives.

WHAT CHANGED IN v3.5 (2026-08-19 — MKT-57a Home modal panel; three production
days on the coffee-Home body; FULL DOC RESYNC — the snapshot above is new)
  A. MKT-57a (8/17, commit 0044ffb): the MKT-11 rotating panel was MISSING
     from the coffee-Home modal captures on the first production run — the
     app's Home mounts never passed `slateDate` to the Pick Detail Modal,
     so the panel (which Explore rendered) did not render from Home. Fixed
     in the app (both Home mounts now pass it — panels are APP-WIDE by the
     MKT-11 ruling, so this is Home coming into line, not a capture hack).
     8/17 verify/allday/midday shipped first WITHOUT the panel; allday +
     midday were re-run and re-published after the daily run; evening
     rendered after the fix. Panel copy is product surface — unchanged.
  B. Production record: 8/17, 8/18, 8/19 all ran nine kinds on the coffee-
     Home body with the re-laid legibility layer (v3.4 §B) — no aborts, no
     operator corrections since MKT-57. The coffee body is the standing
     capture; the Slates grid is HISTORICAL for reels. Any carrier/caption
     line still describing signal bars, set labels, the Signals tab or
     "Grid view" is scene-inaccurate (v3.4 §D stands).
  C. DOC RESYNC (live text brought current; era notes untouched):
       · NEW top block "STATE OF THE SYSTEM — 2026-08-19" — the one-pass
         current picture, maintained in place from now on.
       · §1 run descriptions rewritten for the coffee-Home body and the
         nine-reel output; stale "All-Day signal board" wording struck.
       · §2 beat map corrected to the 10.0s still + 1.5s modals (it still
         read the 4.0s board / ~2s-per-signal cadence retired by MKT-56).
       · §5 template count (8 → 12 per kind, allday_public 8 — MKT-37) and
         kind list (allday_public, verify_public added) corrected.
       · §9 retitled to 2026-08-19; BUILT list extended MKT-46 → MKT-57a;
         DAILY OUTPUT paragraph rewritten to nine kinds; standing-ask list
         annotated against snapshot §G.
       · §10 retitled; its last block ("FREE-GROUP MIDDAY/EVENING REEL — not
         a lane … PRO-ONLY ruling still stands") struck as HISTORICAL —
         those reels have shipped redacted since MKT-26 (7/29) with their
         own endcards and carriers.
     Nothing in §3, §4, §6, §7, §8 was found wrong at the section level;
     §7's per-asset entries remain the detail record (read them before
     generating, the snapshot is not a substitute for the delivery spec).
  D. Asks FOR the content agent this version: (1) confirm the 8/19
     allday_free `guest` cut reads as intended (first full-pipeline
     appearance — the schedule reshuffled it off allday_pro); (2) if
     any carrier line in the rotating sets narrates the modals before
     carrier-local ~10.4s, flag it — re-timing is a re-delivery, not an
     edit; (3) the stinger 4→7 ask (snapshot §G.2) is the highest-value
     generation spend when budget returns — confirm the three concepts
     before generating so names and bed windows are agreed first.

WHAT CHANGED IN v3.4 (2026-08-17 — MKT-57: THE BODY'S FIRST SCENE IS NOW THE
COFFEE-MODE HOME, not the Slates grid; ruled 8/16, first full daily run 8/17)
  A. ⚠ THE CAPTURED SURFACE CHANGES. Operator ruling on the 8/16 evening-pro
     smoke test: the Slates GRID (signal bars, {a,b,c} set labels, tab rows,
     energy bands) is too complicated for a new member reading six digits off
     a 10s frame. From 8/17 every slate reel body — allday / midday / evening,
     Pro, Free-redacted and Public-relabelled — captures the app's COFFEE-MODE
     HOME instead: scope switcher (top) · "NEXT DRAW · Nh NNm" · a 2×3 grid of
     rank tiles, each "#N / digits / ENERGY nn" · a large black space · the
     bottom tab bar. Nothing else on the frame. The still is still 10.0s and
     the six modals (10.0–19.0s) are unchanged — the modal is the same Pick
     Detail Modal opened from the coffee tile, so the panels, INTEL tab, and
     everything in v3.2/v3.3 §B–D about the modals stands. Body = 19.0s exactly,
     reel timeline unchanged (see the timeline block).
  B. Member-legibility layer (v3.3 §C) is RE-LAID for the coffee frame:
       · EMBOSS — the tile digit row is centred on the coffee tile, so it
         scales 1.25× from centre-top (drop-shadow + glow as before) and the
         "ENERGY nn" line nudges down 8px to stay inside the tile;
       · NOTATION STRIP — now a rounded band at the BOTTOM of the black
         space, above the tab bar (16px in from each edge, 112px tall, 30px
         mono digits): "EVENING · 6 SIGNALS · RANK ORDER" then #1…#6. The
         v3.3 "over the Signals/Matches/More tab row" position is RETIRED
         with the Slates surface — that row does not exist on the coffee Home;
       · "TODAY'S DATA DROP" stamp chip moved to y=1000 — in the black space
         BELOW the tiles and ABOVE the band (operator ruling: band at the
         bottom of the black space, date chip above it). It covers nothing.
         Verify reels keep the chip at 470 (different renderer, unaffected).
       · HOLD TIMER unchanged (bottom edge, drains across the 10.0s).
     Still pro/member bodies only — never on redacted/public cuts.
  C. Rig: no hardcoded tile coordinates any more — the six tap targets are
     READ from the DOM (#N rank leaf → its parent tile → centre, sorted by
     rank), so a coffee-grid layout change moves the taps with it. The
     renderer ABORTS unless it sees the "NEXT DRAW" block and exactly six
     #1…#6 tiles (guards against capturing the full Home by mistake). The
     redaction lexical scan gained an "ENERGY N" carve-out (the coffee tile
     renders energy as one leaf, "ENERGY 100"; the label is methodology the
     free cut keeps, and no combination ever renders behind that word).
     Capture-side only — coffee mode is switched on in the capture browser
     via the app's own storage key; the app is not modified.
  D. Content agent implications:
       · Any VO / caption / carrier line that names "signal bars", "the set
         label", "the Signals tab", "Grid view" or otherwise describes the
         Slates board is now SCENE-INACCURATE for the still. Describe the
         still as: six ranked tiles, energy under each, the countdown above,
         the six combinations listed along the bottom. Modal-scene lines are
         unaffected.
       · The frame is far emptier and darker (black space ~y950–1790): panels
         and lower-third ideas that were rejected for "no room on the grid
         still" now have room — but the notation band and the date chip own
         that space by ruling; propose before placing anything there.
       · Reference: assets/marketing/evening_reels/evening_pro_20260816_contact.png
         (third publish of 8/16 — the first coffee-Home body).

WHAT CHANGED IN v3.3 (2026-08-16 — MKT-56: BODY GRID IS A STATIC STILL, no
zoom; grid 4.0→10.0s, modals 2.5→1.5s each; body total STILL 19.0s)
  A. ⚠ MEMBER-REPORTED. Members write the six picks down off the reel's
     slate frame, and the 4.0s Ken Burns push-in (1.00→1.10 zoompan) was
     (a) moving the digits under them and (b) too short to notate six
     three-digit picks from. Operator ruling 8/16: the grid scene is a
     STILL — no zoom, no pan, no ease — held 10.0s. To keep the body at
     exactly 19.0s (so nothing downstream moves: VO window, stamp fades,
     endcard hum-bed/outro mix, check-reel-assets BODY=19.0, carrier
     ceilings), the six modal holds are shortened 2.5s→1.5s each. Modal
     content is unchanged (digits · energy · breakdown at scrollTop 0), it
     simply moves faster; the pick-detail scene is 9.0s (was 15.0s).
  B. Timeline shift INSIDE the body only: grid 8.3–18.3s, modals 18.3–27.3s
     (was grid 8.3–12.3, modals 12.3–27.3). Panels (§ below) still appear
     from the first modal, i.e. from 18.3s now. VO copy that narrated the
     modals over 12.3–27.3s now lands mostly on the still — the WORDS still
     fit (window unchanged) but if a carrier says "tap in" at ~4s of body it
     is now 6s early; re-time modal-specific lines to body+10.0s or write
     them scene-agnostic. Verify reels are unaffected (different renderer).
  C. ⚠ THE FULL-FIDELITY STILL NOW CARRIES A MEMBER-LEGIBILITY LAYER (MKT-56b,
     same day; pro/member bodies only — NEVER on the redacted free-session or
     public cuts, where the digits are the thing withheld):
       · EMBOSS — tile digit rows scaled 1.3× with drop-shadow + glow (the
         tile's {a,b,c} set label nudged down out of the way);
       · NOTATION STRIP — an opaque band OVER THE SIGNALS/MATCHES/MORE TAB
         ROW (top of the frame, under the scope tabs — MKT-56c ruling): "ALL-DAY ·
         6 SIGNALS · RANK ORDER" then #1…#6 with the six combinations, large
         mono, one line — read back from the rendered grid, so it can never
         disagree with the tiles; the render aborts unless it reads six;
       · HOLD TIMER — a thin white bar along the bottom edge that drains
         across the 10.0s hold, so a member sees how long the frame lasts.
     All three are capture-time DOM injection / encode-time filters — the app
     is not modified. The strip is removed before the modals are captured.
     ⚠ STAMP MOVED (MKT-56c): the "TODAY'S DATA DROP" chip now sits at
     y=604 (empty middle of tile row 1) on DROP reels — it was covering the
     enlarged #1/#2 digits at 470. Verify reels keep the chip at 470.
     Content agent: the strip's vocabulary is fixed (SIGNALS / RANK ORDER);
     if copy work is wanted there, it is a scripts/reel-notation.ts change,
     not an app change, and it must stay off the avoid list.
  D. Rig: the separate 3x still + zoompan pass is gone; the grid frame is
     captured at native 1080x1920 in the same context the modals open from,
     and the whole body is one PNG sequence → one encode. Provenance tags
     (hm_reel_date / hm_reel_redacted / public) unchanged. Reference render
     8/16: 1140 frames, 19.000s, PSNR 46 dB grid-first vs grid-last frame
     (identical content), hard cut at 10.0s. Applies to allday/midday/evening
     bodies and all three capture modes (full, --redact, --redact --relabel).

WHAT CHANGED IN v3.2 (2026-08-13, same day — DESIGN-03: the APP SURFACES the
reels capture were visually revised; tomorrow's bodies LOOK different)
  A. ⚠ THE CAPTURED COMPOSITION CHANGES from the next body render. The Slates
     grid and Pick Detail Modal (the reel body's two scenes) got a visual
     pass (operator-ordered, executed 8/13, operator smoke test pending):
       · Modal INTEL (the captured tab): "ZK6 CONFIDENCE" band relabeled
         "ENERGY PERCENTILE" (honest name — it IS the energy percentile);
         WHY THIS ORDER copy now TIERS WITH THE MEASURED SCORE (leading /
         supporting / quiet) instead of asserting "surging" at any value;
         micro-type raised 7-8pt → 9-10pt; the hardcoded "ZK6 v2.1" hero
         line is GONE; tab bar + chrome icons are now lucide vector (were
         emoji — platform-variant rendering on the capture host); third tab
         label reads ACTION (was PLAY, brand avoid-list).
       · Grid tiles: rank chips now carry the SCOPE ACCENT (midday gold /
         evening purple / allday cyan, dark palette) — the three scopes'
         reels read distinct at a glance; signal bars thickened 2.5→4px;
         tile micro-type +1pt. ⚠ GEOMETRY UNCHANGED — tile centers, padding,
         rows and the no-scroll contract are untouched (they are the rig's
         click targets).
     NOT captured, changed anyway: ACTION-tab vocabulary ("Entry"/"Pays"/
     "ANY ORDER" — off the avoid list, amounts unchanged), share text drops
     the un-owned hitmaster.app line, PAIRS tab rebuilt on measured
     position-pair data only (the old 4-row matrix multiplied whole-pick
     signals by hardcoded constants and presented them as per-pair data —
     nothing synthetic renders any more).
  B. RIG CONTRACT UNTOUCHED AND RE-VERIFIED BY READ: "PICK #N" and "BEST
     STRAIGHT" strings, digits-in-text-nodes, close-button position, the
     "Grid" toggle label, scope-tab names and the one-screen grid all stand.
     The MKT-35 chip zone is on the INTRO, not these frames. First live
     bodies with the new look: the next daily run after the operator's
     smoke test — eye-check that morning's grid still + one modal frame
     against this note (captions/copy unaffected).

WHAT CHANGED IN v3.1 (2026-08-13, same day — content-agent doc audit: seven
corrections, and the MKT-50→54 ID GAP EXPLAINED with a ledger)
  A. ⚠ THE ID GAP IS REAL WORK, NOT DRIFT. The 8/13 work order arrived
     stamped "MKT-50"; the number was already consumed — 50 through 53 were
     claimed by the SOCIAL-BRIEF / TRACK-RECORD lanes, which live in
     MASTER_AUDIT and never crossed this doc because they are not reel
     assets. Ledger, so the gap stops looking like a hole:
       MKT-50  Morning Brief → Pro-group deliverable (SocialBriefCard Pro
               depth, pos-rule divergence fix, lint scope). SHIPPED 8/07.
       MKT-51  Verified Track Record: integrity hardening + capture-rig
               contract — ⚠ REEL-ADJACENT: the track-record screen IS the
               nightly verify-reel live set, and ?capture=1 + day-<date>
               anchors are rig contract. SHIPPED 8/07.
       MKT-52  Save/share lane for the captions PDF + social brief PNG on
               the admin Reels screen. SHIPPED 8/08.
       MKT-53  SocialBriefCard per-variant layout (8/09) — ⚠ code-stamped
               with NO audit entry; the entry is owed (the MKT-23
               orphaned-ID class). Recorded in MKT-54's audit entry.
     This batch therefore took MKT-54, first free in both audit and code.
  B. `guest` phone-raise divergence RESTATED AS A MEANING CHANGE, not merely
     cosmetic (§7 entry updated): the shot reads as the Anchor presenting TO
     a guest, not a guest endorsing the product — a different beat. Accepted
     for the slate rotation; re-evaluate if the member is ever proposed for
     a public or ad surface.
  C. Staleness struck: §7 powerup "rotation of 6" → the count lives in §2;
     §9 ask #7's retained "powerup needs REGENERATION" paragraph marked
     HISTORICAL (regenerated + serving since 7/30); §2 stinger anatomy
     "three motions" → four shared + the seal set; §10's MKT-14 "Parked"
     block marked SHIPPED 7/31 (the §9 in-flight list was already right);
     pasted-path typo in §7's endcard anvil note cleaned.

WHAT CHANGED IN v3.0 (2026-08-13 — MKT-54: slate intro pool 6 → 9; the arrive
hold cleared by operator ruling the same day; a SEVENTH delivery failure class)
  A. TWO NEW SLATE INTROS REGISTERED — `guest` (the pool's first TWO-SHOT: a
     bronze, rounder, older domestic-model robot in a burgundy robe beside the
     Anchor; both figures verified inside the 1:1 band) and `window` (back to
     camera at a pre-dawn city; skyline glyph-swept clean). With note B's
     ruling INTRO_ROTATION is [standard, deadpan, arrival, monitors, powerup,
     board, guest, window, arrive].
     Trims, measured not assumed: guest IN 0.0/OUT 6.2 (6.21s), window IN
     0.0/OUT 6.2 (6.21s) — window's master ghosts its pull-back through the
     smoke from ~6.6s, so any re-trim must keep OUT ≤ ~6.3s.
  B. `arrive` DELIVERED, TRIMMED (IN 0.310/OUT 6.510, every technical gate
     passing), GATED for a day on a legible white "S" letterform riding its
     desk-front display from FRAME ONE (the thumbnail) to ~1.2s — then
     ⚠ OPERATOR RULING same day: ACCEPTED as set dressing (a news-desk bug,
     not a digit board), keeping the empty-desk beat intact. REGISTERED —
     the pool is NINE. The acceptance covers the slate rotation's group
     tiers only: if this pool ever feeds a public kind, arrive needs a
     fresh Q1/Q2 ruling, because the letterform is on the poster frame.
  C. ⚠ DELIVERY-PATH FAILURE CLASSES — now SEVEN. This drop added two events:
     (1) WRONG CONTAINER EXTENSION — all three landed as `.mov`. A .mov is
     invisible to the registry exactly as the `.MP4`-uppercase incident was:
     nothing errors, the pool silently stays at its old size. Remux is
     `-c copy` when the streams are already h264/aac (they were — measure,
     don't assume). (2) The GitHub-web rename struck AGAIN: all three .movs
     were destroyed to 2-byte stubs by upload-then-rename; real bytes
     recovered from the upload commits. Deliver at the FINAL lowercase
     name, exact extension, direct upload, never a web rename.
  D. ⚠ PROMPT-NOTES FACT (belongs with §9 ask #7's negatives note): THREE
     prior attempts at the guest shot with a HUMAN guest were REFUSED by the
     generator; the all-robot version cleared on the FIRST try. The block is
     the second HUMAN, not the two-shot composition. Robot guests, reaction
     shots and two-hander comedy are an OPEN LANE provided every figure in
     frame is a machine; human guests remain closed.

WHAT CHANGED IN v2.9 (2026-08-06 — MKT-49: the rotation stopped repeating, and
your next assets are ONE-LINE registrations)
  A. WHY: the operator noticed intros/stingers/endcards recurring across a few
     days of watching. Measured from the live registries, the whole nine-reel
     stinger assignment was IDENTICAL every 4 days without end, and every 4th
     morning all three rotating carriers landed on "incumbent" at once (the
     carrier lane had missed the 7/29 per-kind spread fix). Root cause: a
     date-offset walk over a set of n repeats with period exactly n — the
     PAIRINGS recur, not just the assets.
  B. THE SCHEDULE IS NOW RESHUFFLED PER CYCLE. Every member still plays
     exactly once per n-day block (usage stays perfectly even), but the order
     is redrawn each cycle, so the same arrangement no longer returns on a
     fixed loop. Still clock-free and deterministic per date. Residual, by
     arithmetic: a pool of n admits only (n-1)! distinct daily patterns — SIX
     for a pool of 4 — so the stinger and carrier lanes still show ONE 6-day
     recurrence a year. ⚠ RETROACTIVE (operator-approved): re-running a past
     date reproduces its BODY exactly (MKT-18 untouched) but not its
     intro/stinger/endcard/carrier. Published reels keep what they shipped.
  C. TWO NEW SURFACES REPORT THIS SO IT NEVER AGAIN TAKES DAYS OF WATCHING:
       npm run reel:rotation     the schedule days ahead, per lane per kind,
                                 plus distinct-arrangement depth per kind
       reel:check                now warns when any lane's closest identical
                                 arrangement is under 7 days apart, when an
                                 asset plays on 2+ reels today (and WHICH
                                 cause: pool too small vs config defect), and
                                 flags kinds with under a fortnight of
                                 distinct arrangements
     Both read the SCHEDULE (candidate[0]); a missing file shifting a day's
     pick is reported separately by the per-asset checks, same as ever.
  D. ⚠ FINDING 4, FIXED — verify_public and midday_free had drawn the SAME
     free endcard EVERY DAY since MKT-40: the free spread axis listed the two
     dormant session publics BETWEEN live kinds, putting verify_public at
     index 6 ≡ midday_free's 1 (mod the 5-motion pool). THE RULE THAT CAME
     OUT OF IT, now applied at every spread axis: LIVE KINDS FIRST, DORMANT
     LAST — a dormant entry must never sit where it can shift a live kind's
     offset. Registering a dormant kind later APPENDS; nothing live moves.
  E. ⚠⚠ THE PINNED LANES ARE NOW SETS — READ THIS BEFORE PROPOSING VERIFY OR
     PUBLIC ASSETS, IT CHANGES WHAT IS POSSIBLE. The rulings pin verify and
     the public kinds AWAY FROM THE SLATE ROTATION; they never said "one file
     forever". With every lane it uses pinned to a set of one, verify had
     FOUR distinct full arrangements, ever, and allday_public — the cold
     audience, where novelty matters most — had 20 (worst in the fleet;
     allday_free has 221). What is now open, each a ONE-LINE registration:
       - SEAL VARIANTS (same closing-bell concept, different cuts) →
         VERIFY_SEAL_MOTIONS in brand-motion.ts. verify + verify_public
         spread across the set, so on multi-variant days the two halves draw
         different cuts of the same bell. Per-variant: stinger:build + a
         MOTION_LOCKUP measurement per tag (MKT-41's 820 was only ever safe
         because the set had one member). A slate kind still cannot reach
         the seal — nothing else looks the set up.
       - A SECOND PUBLIC INTRO → PUBLIC_INTROS in anchor-intros.ts. ⚠ THE
         CLEARANCE GATE COMES FIRST: members feed four public kinds, so the
         full MKT-30 Q1/Q2 + glyph/numeral sweep, per file, evidenced,
         BEFORE the line is added. A slate member being clean is NOT
         clearance. The morning's two public reels then open on different
         cleared hooks.
     The drop-in path was verified with simulated members BEFORE any asset
     exists: rotation, same-day spread, no slate leakage into public or
     seal, per-member holds, fallback ordering, and reel:check's depth
     numbers all follow automatically. Single-member behaviour is
     byte-identical to the old pins, so nothing changed today.
  F. ASSET ASKS, NOW EXACT (generation-gated; nothing commissioned): stinger
     4 → 7 buys a distinct motion per rotating kind daily (at 4, three
     motions double up every single day — the thing the operator was seeing);
     free endcard pool is ADEQUATE at 5 now that D is fixed — retire any
     "free tier is thin" framing; midday_pro / midday_free / evening_free /
     public carriers are SINGLE-ENTRY sets (those rooms hear the identical
     VO opening every day); the three intros on the operator's device
     (coffee / juggle / guest) take the slate pool 6 → 9 and are pure
     drop-ins. Depth after this session: allday_free 221 · allday_pro 211 ·
     evening_pro 203 · evening_free 113 · midday_free 112 · midday_pro 94 ·
     allday_public 20 · verify_public 5 · verify 4. The last three move ONLY
     with the E-lane assets.
  G. REGISTRY RESYNC (your 8/02 status report ran stale against these —
     recorded so this document stops being the source of that): free endcard
     pool is FIVE (tide + bloom + adopen registered; adopen free-shared by
     operator ruling 8/03); evening_pro carrier rotation is SERVING (landed /
     dusk / last + incumbent); updraft REJECTED 8/03 (54.5/54.8 breaches the
     53 ceiling, band_max 232, bed −9.9dB); the "Covered vs Uncovered" ad
     pair was GENERATED and PAIR-REJECTED on Q1 8/03 (seg2 resolves eight
     serif O/zero glyphs — F3 class; files kept as *_rejected_20260803) —
     ad_pro remains 1 body, the wave is NOT 11/11. Full detail: MASTER_AUDIT
     MKT-48 and the MKT-49 entry + addendum.
     PROMPT-LAW VERDICT (the updraft ask, answered against the numbers):
     confirmed in its WEAK form only. "Never-moves passes, travelling
     fails" implies a direction; the record shows travelling forms land
     UNPREDICTABLY — the four pro travelling concepts settled HIGH (43-49%,
     twice, from corrected prompts), updraft settled LOW (54.5 vs the 53
     ceiling) with a band collision, anvil passed composition then dipped
     to y1192 — while never-moves constructions are the reliable class,
     with one recorded deviation (tide floats ~11pp above family, passed
     as an atmospheric alternate). LAW AS RECORDED: write motion briefs
     never-moves-first; a travelling-form concept is not banned but must
     be treated as gate-risk with no predictable failure direction — do
     not prompt-correct and retry expecting a different landing, that was
     tried twice and both landings missed the same way.
  H. 8/06 note: allday_free, allday_public, evening_pro, evening_free and
     group verify were REASSEMBLED and republished under the new schedule
     (variant-scoped; allday_pro, the midday pair and verify_public kept
     their morning cuts). Verify's furniture resolves on its STAMP date
     (D-1), by design — reel_date = content date, as ever.
  I. BRAND DOMAIN SECURED (2026-08-06, BRAND-07): hitmasterzk.com is ours,
     DNS-confirmed, registered as app_config.brand_domain — one key every
     future consumer reads, same substitution pattern as the group URLs.
     ⚠ NOT WIRED TO ANY SURFACE YET, deliberately. Before the domain
     appears on ANY public asset (endcard CTA, caption, YT description,
     panel): Two-Question filter + tier-map ruling per surface — a domain
     on a public asset is an invitation, and which tier it invites into is
     a ruling, not a default. If you are drafting copy that wants a URL,
     propose it against a tier; do not bake the domain into generated
     footage (evergreen assets never carry claims that can go stale —
     same law as dates, MKT-18 class).

WHAT CHANGED IN v2.7 (2026-08-01 early AM — MKT-41→45; THE 880 RULING IS DEAD)
  A. ⛔⛔ **EVERY LOCKUP TUCK IS REVERSED. ALL KINDS ARE ON THE SHARED BANDS:
     STINGER 1330, ENDCARD 1240.** Read this before you act on ANY older
     geometry note in this file. Over roughly four hours on 7/31→8/01 three
     changes shipped that tucked lockup text up under the mark — MKT-41
     (verify stinger 820), MKT-42 (stinger circuit 950), MKT-44 (endcard
     per-motion 700/850/860/910) — and the operator rejected all of it on
     sight. **MKT-45 reverted the lot.**
     THE REASON, because it governs future work: the measurements were
     RIGHT (marks settle y613-866; text really was 416-627px below) but the
     CRITERION was wrong. "Distance from the mark" is not the quantity to
     minimise. Closing it drags the mark+text group into the upper ~55% of
     frame and leaves the lower ~45% dead. **FRAME BALANCE WINS. The space
     under a high-settling mark is the motion's own negative space, not a
     defect to be closed.** Operator evidence frames are committed at
     assets/marketing/screenshot_intro.png and screenshot_outro.png.
  B. ⛔ THE "DO NOT FIX 880 BACK TO 1240" RULING IS ITSELF REVERSED. It
     appears in at least six places in this document (v2.3 note C addendum 2,
     §3's CLOSE block, §7's verif_endcard entry, the MKT-31 ledger line, and
     the verify_public differences list). Every one of them is struck inline
     below. **1240 is now correct for verify too, and verify closes
     identically to the whole fleet.** The old requirement that a new verify
     endcard motion re-measure the 880-1040 zone is retired with it — there
     is no tuck zone to re-purchase.
  C. ⚠ NOTHING AUTOMATED CHECKS LOCKUP PLACEMENT ANY MORE, and that is
     accepted, not an oversight. MKT-42's 380px "strand gate" in reel:check
     was DELETED rather than retuned, because its pass condition IS the
     rejected criterion — it would now fail the entire healthy fleet every
     run. Placement is a whole-frame composition judgement. **Check a frame
     by eye; do not propose rebuilding the gate.**
  D. The per-motion override MECHANISM survives, empty, in both configs
     (MOTION_LOCKUP by tag, ENDCARD_MOTION_LOCKUP by motion FILE). A value
     belongs there only for a motion whose WHOLE-FRAME composition needs it.
     **Never populate either from a gap measurement.**
  E. Scope of the defect, corrected: only kinds that rotated onto `circuit`
     (stinger) or a PRO endcard motion ever carried a tuck. Free-tier
     stinger/endcard combinations were never affected — proven when
     evening_free's rebuild came out BYTE-IDENTICAL (sha256 0f7ec767…).
  F. NEW VERIFY CARRIER PAIR (MKT-43) — verif_carrier.mp4 + its pt2 were
     replaced 8/01 as a designed SAME-SESSION pair, fixing the seam where
     pt1 and pt2 read as different men. Spoken lines, transcript-verified
     and tier-1 clean: pt1 "Yesterday's board graded in the open. / Every
     call we posted checked against what drew. / Matches marked, nothing
     hid." pt2 "That one's on the record. / Signals first. / Receipts
     after." **NO TIME-OF-DAY WORDS in pt1 — deliberate, so verify may post
     at any hour.** pt2 is LONGER than the incumbent (6.542s vs 5.667s), so
     the join moved 15.995s → 16.897s: coverage improves (bodies up to
     13.9s) but short-body days fall back to signoff_short more often.
     ⚠ TWO EAR GATES REMAIN OWED and cannot be measured — (1) the pt1/pt2
     seam itself, (2) whether signoff_short (OLD session family) still reads
     as the same man behind the new pt1.
  G. MKT-14 SHIPPED (7/31) — the slate stamp now carries the brand line
     "HITMASTER ZK6" BELOW the date line. It is no longer parked; the §10
     IN-FLIGHT entry below is struck.
  H. MKT-46 AD LANE — PHASE 0 ANSWERED, PHASE 1 AUTHORISED (2026-08-01).
     14 files delivered and ALL 14 PROBE CLEAN (9 body segments + 5 intros;
     h264+aac, 10.000s video / 10.005s audio, 720×1280) — the first
     delivery on this path with zero eaten binaries. There is NO sixth
     intro: the five are thought · paper · wall · arrives · building.
     Confirmed shape — TWO kinds (ad_free / ad_pro, forced by the caption
     registry keying on kind alone); NO stinger ever; intro on 2-PART
     bodies only (both anatomies land 38-39% branding vs 60% with full
     furniture); stamp = BRAND LINE ALONE, no date, no scope (a date on
     evergreen footage is the MKT-18 false-claim class); "body supplies
     audio" as an EXPLICIT kind property, never inferred from an absent
     carrier (absent carrier has historically meant hum-bed mode, and that
     inference firing on an ad is the silent-degradation class).
     ⚠ THE 5:1 FREE:PRO RATIO IS A **DISPATCH PROPERTY, NOT A POOL
     PROPERTY** — do not try to encode it. With two kinds each rotating its
     own pool, exposure is governed by how often the OPERATOR publishes
     each kind, and that lives in operator behaviour, not config. Standing
     operator default: **ad_pro goes to the FREE GROUP ONLY and
     infrequently** — it is an upgrade pitch and the pinned upgrade video
     already carries that job permanently. **ad_free is the workhorse.**
     ⚠ RETENTION WOULD HAVE KILLED THE POOL TWICE: supersedeOlder() deletes
     the previous ad on the next publish, AND prune() is NOT kind-filtered,
     so an evergreen ad dies at 30 days even unsuperseded — it would have
     looked like it worked, then quietly emptied. Both get kind-test
     exemptions plus a reel:check assert that FAILS if an ad kind is
     reachable by either path.

WHAT CHANGED IN v2.6 (2026-07-31 PM — MKT-40: verify_public LIVE)
  A. ✅ verify_public — THE GRADING HALF OF THE PUBLIC PAIR — IS BUILT,
     PUBLISHED AND IN THE DAILY SEQUENCE (reel:verify builds both cuts).
     Your delivered copy is live verbatim: endcard (PUBLISHED FIRST.
     CHECKED AFTER. / SEE THE FULL RECORD FREE, gold CTA), chip
     YESTERDAY'S RECORD (gold, dated, frame one), stinger shares verify's
     YESTERDAY'S RECEIPTS seal builds (headline-dominant extended by the
     recorded ruling), 8 captions ({free_group_url} substituted from
     app_config at publish), and your own YouTube set (6 titles / 3
     descriptions — never inherits allday_public's).
  B. Phase-0 decisions as shipped: rollup reframed STRUCTURAL (DAYS ·
     JURISDICTIONS · 40+ STATES COVERED · 6 SIGNALS DAILY — no unbounded
     totals; option c); no fabricated sameness cue (option i) — the gold
     EXACT ORDER treatment carries the hierarchy as color + label; the
     carrier pair is SHARED (your suspected free win, confirmed by
     transcript: the spoken lines are Q2-clean); lockup stays on the
     SHARED BAND — the 880 tuck was re-measured on the free motions per
     the then-standing rule and FAILED (10% px >L40, max L255).
     ⛔ THE CONCLUSION STILL HOLDS (shared band) but the RULE behind it is
     dead: as of MKT-45 there is no 880 tuck anywhere and nothing to
     re-measure. See v2.7 notes A/B.
  C. Match vocabulary on the public cut, as finalised: EXACT ORDER /
     ANY ORDER (bare badge forms; your MATCH-suffixed forms lint clean
     and remain available for prose), DREW · EXACT ORDER on the board,
     sessions + state codes dropped, attribution reads "Drew ••• ·
     verified against the official draw", day headers de-yeared.
     Q1/Q2 gate record stored; reel:check FAILs the kind without it.
  D. Cadence: TWO public reels/day is now the shipped state (the
     publish-then-grade pair complete). Operator can pull it back to
     alternating by skipping the public verify publish on any day.

WHAT CHANGED IN v2.5 (2026-07-31 PM — MKT-38 retention)
  A. ⚠ REELS RETENTION IS NOW KEEP-CURRENT-ONLY (MKT-38, operator directive:
     storage pressure). Each kind's new-day publish DELETES the older
     versions — bucket objects removed, rows archived (posted history kept),
     local files swept. Per-kind, keyed to the new row's own reel_date, so
     verify's D−1 dating is safe by construction. Retro-applied same day:
     674MB freed locally, 20 bucket rows superseded; every kind now holds
     exactly its newest cut. §4 has the rule. Consequence for content work:
     an older cut can only be re-examined from git history — measure and
     frame-check deliveries the day they land.
  B. Status note: the verify PIPELINE repairs (gold straight board, asserts,
     per-segment ribbon, frame-one dated chip) are all live in code; the
     first fully-repaired PUBLISHED verify cut is the next morning run's.
     Open on verify: part-1 VO candidate gate (whisper env). (The
     verify-free #9 replacement line arrived later on 7/31 — closed.)
  C. ⚠ THE 1:1 FEED CUT IS RETIRED (MKT-39 cost audit → operator ruling,
     2026-07-31). Zero logged use across 48 assisted posts; it was 37% of
     bucket storage and 8 encodes per run. All existing 1:1 objects deleted,
     rows nulled, local files removed, encode dropped from both assemblers.
     Bucket steady state 214 → ~135 MB. ⚠ The 1:1 centre-crop BAND
     (y 420-1500) in the delivery spec remains BINDING — it anchors the chip
     zone and every composition clearance; do not relax it, and a future 1:1
     need is a code resurrect (recipe at the removal site), not an asset
     respec.

WHAT CHANGED IN v2.4 (2026-07-31 PM — MKT-35 shipped in full · MKT-36 intro swap · MKT-37 captions)
  A. ✅ MKT-35 SHIPPED IN FULL (was "in flight" in v2.3):
     1. DATED IDENTITY CHIP — the intro chip now carries a second line
        ("FRI · JUL 31" — 22px, gold, letter-spaced) and renders at FULL
        OPACITY FROM FRAME ONE, no fade-in (frame one is the camera-roll
        thumbnail — the load-bearing half); hold to 2.65s, then the 0.25s
        fade. Date from the assembler's provenance-asserted stamp, never a
        clock; verify chips YESTERDAY (its stamp is the content date by
        construction). Ad kinds are EXCLUDED in config (datedChipExcluded —
        an evergreen ad must never carry a date, the MKT-18 class). Chip
        zone re-verified AT 0.0s across all seven intro files: all pass.
        ⚠ Any §-text that says the chip appears "at 0.4s" is historical.
     2. RIBBON PER-SEGMENT PLACEMENT — during the BOARD segment the gold
        ribbon rides at y 100-220 inside a band the board renderer now
        reserves (board content top-padding 96→328; clear y 0-300 measured
        through the push-in); it returns to spec y 470 exactly at the
        board→summary scene cut. It never crosses a card, digit or badge.
     3. Slot 9 of the heat vocabulary: OVERDUE → LONG GAP (⚠️ drops, both
        VS16 forms), collision-checked, lint-clean at tier 1.
  B. ✅ MKT-36 — anchor_intro_public REPLACED (serving-asset swap, won the
     comparative gate). ⚠ The delivery path ATE TWO MORE BINARIES (running
     total SIX): the 7/31 GitHub-web renames corrupted the delivery AND the
     shelved incumbent to 2-byte stubs — the public lane's only intro was
     dead on disk until both were restored from git. §7 has the full
     measurements (trim IN 0.4/OUT 6.4; onset −20.2dB mean, the fleet's
     hottest open; chip zone clean from frame one). Rollbacks parked as
     *_incumbent_backup.mp4. Still 720×1280 — ask #1 stays open, generator
     ceiling confirmed a FOURTH time; the upscale pilot is the only path.
  C. ✅ MKT-37 — YOUTUBE SET + TEMPLATE EXPANSION:
     1. YouTube Shorts content set registered as a PER-PLATFORM TRANSFORM
        (constants/socialPlatforms.ts): 8 titles + 4 descriptions rotating
        independently (all 32 pairings over 32 days), tags ruling recorded
        (#Shorts #DataAnalysis #PatternAnalysis — description only, never
        the title, no gambling-adjacent tags). {free_group_url} wired.
     2. Caption templates grew 8 → 12 per kind (§5). verify-free reached 12
        later the same day — the first-delivered #9 duplicated shipped
        template 5 verbatim and was not registered; the replacement
        ("Every signal on yesterday's board went up before the draw…")
        arrived and closed the set. verify-free #12's straight claim is structurally gated
        (it renders only via straightPhrase, which only exists on days the
        record contains a straight). Spacing re-verified at 12: HOLDS
        (pro trio residues 5/7/11). 196 rendered captions + the full YT
        set: ZERO lint trips.
     3. HANDOFF KIND GUARD — ReelsView now WARNs (non-blocking) when a
        non-public cut is pointed at a tier-1 platform or the cross target:
        free/pro cuts carry real digits and cannot honestly clear Q1.
        Context: the operator posted to YouTube 7/30 + 7/31; the DB shows
        allday_public handed to the cross-post lane both days (the correct
        file), but the DB records targets, not platforms — operator
        confirmation of which files went up is still worthwhile.

WHAT CHANGED IN v2.3 (2026-07-31 — MKT-35 doc resync: MKT-31 recorded, stale body struck)
  A. ⚠ MKT-31 — THE VERIFY IDENTITY PACKAGE — WAS ABSENT FROM THIS DOC
     ENTIRELY despite shipping 2026-07-30, operator-accepted. Recorded now
     (changelog here, ledger §9, anatomy §3, assets §7). The package: (1)
     verify stinger hierarchy INVERTED — "YESTERDAY'S RECEIPTS" dominant
     ~62px, wordmark small in gold beneath (headline-dominant layout, verify
     only); (2) persistent gold DATE RIBBON ("YESTERDAY'S RECEIPTS · <day> ·
     <date>") riding every body frame, fade-out at the endcard cut; (3)
     endcard matrix rebuilt with gold CTA; the stale unversioned fallback
     verif_endcard.mp4 refreshed; (4) gold #FBBF24 accents (ribbon, stinger
     line 2, endcard line 3, intro chip) — verify only, slates keep
     purple/cyan; (5) SEAL CLEARED — see B; (6) sign-off boundary confirmed.
  B. ✅ THE SEAL HOLD IS CLEARED (2026-07-30, measured): the re-delivery
     peaks −17.4dB at 1.10s (bound ~2.0s), nothing after 2.4s above −19.2dB;
     the 7/29 defect (bell 2.7s/−3.5dB over the VO onset) is gone. Binding
     TIGHTENED: seal is verify's FIXED stinger motion REPLACING rotation —
     a missing/defective build degrades to NO stinger via probeStinger's
     null, NEVER to a slate motion; a re-armed `held:` returns verify to
     rotation. §7's HELD entry and v2.0 note A are superseded by this note.
  C. ⚠ MKT-31 ADDENDUM (geometry, blocking postability): verify's outro now
     takes the endcard's FIRST 6.5s (All-Day's proven window; was the last
     2.5s static tail). Reel +4.0s; envelope ~23.4-28.8s; carrierNeed for
     the reference body 13.50→17.50s so the full pt2 fits-whole again.
     ⛔ ADDENDUM 2 IS STRUCK — REVERSED 2026-08-01 BY MKT-45 (see v2.7 note
     A/B). It read: "verify's endcard lockup is tucked under the bolt at
     per-variant lockupTop 880 (block 880-1040), OVERRIDING the shared band
     for verify only; do not correct 880 back to the band." **A later
     operator ruling reversed it. Verify's endcard is on the SHARED 1240
     with every other kind, and the re-measure-the-880-1040-zone
     requirement for new verify motions is retired.** Frame balance beats
     distance-to-mark; the space under the bolt is the motion's own
     negative space. The rest of MKT-31 addendum 1 (the FIRST-6.5s window)
     stands unchanged.
  D. STALE BODY STRUCK by MKT-35: v2.2 note B.3 (powerup — it serves in the
     rotation of 6 since 7/30, v2.0 note F was correct); §9 MKT-28/MKT-30
     remaining-work clauses (both closed in v2.1); §10's MKT-16 block and
     the IN-FLIGHT list (Phase 1+3 shipped 7/30 — allday_public is LIVE,
     daily output is EIGHT reels); §3's seal-held and 2.5s-close text.
  E. MKT-35 P1 (same day): the verify BOARD segment now renders straights
     GOLD with "DREW · STRAIGHT" (was one green treatment for every match —
     the 7/30 board showed two straights as plain matches). Fail-closed
     asserts added: posted==drawn must carry the straight annotation, and
     the board/ledger must agree on match TYPE, else the render aborts.
     Data layer verified correct on live rows — bestOrder IS the posted
     permutation; no backfill was needed (all defect classes: 0 rows).

WHAT CHANGED IN v2.2 (2026-07-31 PM — MKT-32 ADDENDUM salvage pass + web-rename incident)
  A. ✅ ALL FOUR v2.1-note-A REJECTIONS WERE SALVAGED BY EDIT AND ARE LIVE —
     NO REGENERATION NEEDED FOR ANY OF THEM. The v2.1 respec asks for these
     four are RETIRED (raw takes stay parked as masters/evidence):
       verif_carrier_signoff_short — dead pause spliced 2.05s→0.215s beat;
         last word 2.218s ≤ 2.3s. SLOT IS FILLED.
       evening_pro pt2 — tail gated + both line gaps −80ms → margin +0.212s.
       allday_free pt2 — both line gaps −80ms → margin +0.248s.
       midday_free pt2 — two largest gaps −60ms → margin +0.234s.
     All vs the +0.184s floor; every word transcript-verified intact; every
     splice in room tone (seam step ~0.0003, no clicks). Incumbents kept as
     *_part2_<date>_backup.mp4. §7 margin notes predating this pass are
     superseded by these numbers.
  B. ⚠ ASSETS THAT STILL NEED GENERATION OR OPERATOR ACTION — the real list:
     1. VERIFY PART 1 (new read): delivered 7/31 via GitHub web, but BOTH web
        renames CORRUPTED their targets (the delivery AND the shelved
        incumbent became unreadable stubs — 4th binary this path has eaten).
        Real bytes were recovered from the upload commit and parked INTACT as
        verif_carrier_part1_candidate_20260731.mp4 — probes clean, 10.005s
        law holds, VO end 9.568s. It is NOT a regen case: it awaits its
        CONTENT + VOICE-FAMILY GATE (transcript tool broke mid-session), then
        swap-or-reject. The OLD incumbent verif_carrier.mp4 was restored from
        git and is serving — verify lane is green today.
        ⚠ FUTURE DELIVERIES: never rename binaries in the GitHub web UI.
        Upload under the final name, or rename via CLI, and probe after.
     2. ~~evening_pro PART 1 — never delivered (v2.1 note C still stands);
        scope-named incumbent serves. §9 rotation-set gap also still open.~~
        STRUCK in v2.9 — STALE since 8/03: MKT-48 Lane D delivered and
        REGISTERED landed/dusk/last; evening_pro runs the All-Day rotation
        shape (set of 4, incumbent = entry 0 + fallback), all part 1s
        10.005s, joining the EXISTING pt2. This line fed the 8/02 status
        report's staleness (v2.9 note G).
     3. [STRUCK in v2.3 — STALE. powerup's regeneration was validated and
        SERVES in the rotation of 6 since 2026-07-30 (v2.0 note F, §7).
        This line contradicted both and leaked into the audit's close-out;
        both struck by MKT-35. It owes nothing.]
     No other reel asset is awaiting delivery or regeneration.

WHAT CHANGED IN v2.1 (2026-07-31 — MKT-32 carrier wave gate; audit entry has the tables)
  A. ⚠ THE 7/30–7/31 CARRIER DROPS WERE GATED: ONE OF FIVE SURVIVES.
     LIVE: evening_free_carrier_pt2 — margin +0.359s, content clean, voice in
     family (one non-blocking ear-flag: −2.7dB quieter than its part 1).
     REJECTED, parked on disk with reasons in UNREFERENCED_OK: allday_free pt2
     (+0.088s vs the +0.184s floor), midday_free pt2 (+0.114s), evening_pro pt2
     (~+0.10s AND a non-silent tail to ~9.8s — end part 2s in clean silence),
     and verif_carrier_signoff_short (a 2.05s dead pause mid-line puts the last
     word at 4.03s from onset vs the ≤2.3s acceptance — regeneration as ONE
     CONTINUOUS READ, not a trim). Incumbents serve everywhere but evening_free.
     RESPEC ASKS: same close lines; last word by ~9.47s of part 2 (sessions) /
     ~9.2s (evening_pro, keep "Receipts at sunup, partners" — the endcard echo
     is confirmed); clean silence to EOF.
  B. ⚠ DELIVER TO THE EXACT REGISTRY NAME, LOWERCASE .mp4. Two of the three
     "overwrites" landed as `.MP4` (uppercase) BESIDE their incumbents — the
     registry reads exact names on a case-sensitive filesystem, so both were
     invisible to the joiner AND to the stray scan, and the incumbents kept
     serving with nothing erroring. The fifth filename failure in this lane,
     and a new class again: correct name, wrong case.
  C. ~~evening_pro PART 1 WAS NOT DELIVERED (only its pt2 came). The
     incumbent is already scope-named ("Evening, partners…") at 10.005s, so
     the pair's open items are the pt2 respec above and §9's rotation-set
     gap. The scope-naming asymmetry item does NOT retire yet.~~ STRUCK in
     v2.9 — superseded 8/03 by MKT-48 Lane D: three rotation part 1s
     (landed/dusk/last) delivered, measured and SERVING. See v2.9 note G.
  D. v2.0 notes D and E are CLOSED: MKT-28 shipped (the 11 HEAT_RELABEL slots
     reconciled, table + lint gate), MKT-16 registered allday_public and the
     public reel assembles daily with its stored Q1/Q2 gate record — the heat
     neutrals in the public cut are the REAL set now, not provisional.

WHAT CHANGED IN v2.0 (2026-07-30 — content-agent ruling pass + MKT-29/30)
  A. [SUPERSEDED by v2.3 note B — the hold CLEARED on the 7/30 re-delivery
     (peak −17.4dB at 1.10s) and the seal is now verify's FIXED motion
     REPLACING rotation. Kept for the record:]
     ⚠ SEAL IS VERIFY'S PINNED STINGER MOTION — ruling option (a) confirmed —
     BUT THE DELIVERED CLIP IS HELD on a measured defect: the bell lands at
     2.7s at −3.5dB, AFTER the lockup is out (2.4s) and directly over the
     carrier VO onset (2.6s), mid-dissolve. A window slide does not rescue it
     (the bolt is already forming at 0.7s of the master, so a later window
     breaks the smoke-only open) and fading the bell deletes the concept.
     The pin is WIRED with the shared rotation as fallback; the registration
     carries `held:` with the reason, so no routine stinger:build can re-arm
     the defective clip. REGENERATION ASK: ring sealed and bell STRUCK by
     ~2.0s of the clip, decay under the smoke return. §3, §7.
  B. RESULTS chip / RECEIPTS stinger on verify is DELIBERATE, ruled 2026-07-30
     — identification in plain language at 0.4s, brand framing at 5.6s,
     escalation not repetition. Recorded so nobody "corrects" it. §3.
  C. VERIFY HAS A SHORT SIGN-OFF SLOT: `verif_carrier_signoff_short.mp4`
     (~3.0s, budget-gated — first take DELIVERED 7/31 and REJECTED on the
     2.3s line, see v2.1 note A; the slot is empty again). The resolver walks full pt2 →
     short → part 1 alone, each playing WHOLE or not at all. ⚠ The name is a
     SLOT, deliberately not `_ptN` — that namespace belongs to the joiner and
     its orphan scan. Fit math: part 1 is fixed at 10.005s + 0.35s breath, so
     ≤2.3s of speech covers every day with a featured row; ~3.0s starts
     losing the 1-box days. §3, §7.
     Also corrected: the 0-straight drop is a pure DURATION consequence — pt2
     was written badge-agnostic on purpose. v1.9's "fits worst" rationale was
     wrong and is retired.
  D. [CLOSED in v2.1 — the 11 neutral slots landed and serve in the public
     cut; see v2.1 note D] MKT-28 COPY DECISIONS CLOSED (operator-delegated): canonical HOT wins —
     HOT SIGNAL is retired on the detail surfaces; Heat Check keeps its
     verdict WORDING on renested thresholds (80/65/45). The reconciled state
     the public relabel writes against is now: ONE temperature ladder
     (ON FIRE/BLAZING/HOT/WARM/COOL at 90/80/65/45), ONE verdict set
     (BLAZING SIGNAL/STRONG SIGNAL/MODERATE/OVERDUE/LOW), one banner phrase.
     ⏳ The content agent owes the neutral public vocabulary for those 11
     slots — the LAST copy input the public cut needs.
  E. [CLOSED in v2.1 — MKT-16's five registration points landed 7/30;
     allday_public assembles and publishes daily with its stored Q1/Q2 gate
     record; heat neutrals are the real set] MKT-15 P2 PUBLIC CAPTURE MODE IS BUILT (`--redact --relabel`): the eight
     delivered copy slots injected at capture, session labels dropped, state
     codes suppressed, and the ruled look — masked combinations become a
     blurred PLACEHOLDER block with the gold bolt (60% height, 85% opacity,
     one per masked number; blur never derived from real digits). ⚠ Q1 IS
     ABSOLUTE at tier 1, probed against the real lint engine: "100", "100%"
     and "102×" ALL block, so remaining 3-digit runs are structurally HIDDEN
     — the free cut's kept-methodology carve-outs do not carry to public.
     Heat neutrals ship as PROVISIONAL config strings until D's set lands.
     Not registered anywhere (MKT-16 stays held) — a validated capture
     awaiting the five registration points. §10.
  F. REGENERATED powerup + deadpan VALIDATED AND SERVING (both trimmed
     IN 0.0 / OUT 6.3 with a 0.4s tail fade to silence; masters kept).
     deadpan's onset — its one job — measures −46.6dB mean / −18.0dB max
     over the first 0.3s (old: −56.2; public reference: −37.9): mid-fleet
     now, standing ask #4 CLOSED. powerup's device exits by ~5.4s against
     the redesign's 5.0s spec — recorded overshoot, ruled a PASS because
     the OUT-6.3 dissolve bed (5.8-6.3) is clean smoke with ~0.9s margin.
     Its REJECTED status is lifted; the rotation is 6 with all six serving.
     ⚠ SLATE REELS NOW RUN 33.83-34.53s (intros 5.63-6.33s).
  G. lattice IS BED-VIABLE (−31.4dB) — its bed was respec'd +10dB locally
     (0-3.6s, ramp to unity before the 4.2s crack) so the residual sits
     inside the ±6dB clamp; endcard:build re-derived the verdict on its own.
     The pro tier now has THREE bed-viable motions (std, steady, lattice).
     v1.9 note F is retired.
  H. REDACTION HARDENED: the mask and assert now cover MIXED CONTENT (a run
     split across text nodes and elements), rule 7's pair replace is global,
     and a pass-3 backstop asserts that post-mask NO text mentioning "pair"
     still carries a two-digit run. The two blind spots the MKT-26 addendum
     named are closed, fail-closed.
  I. BUDGET STATE: generation balance is ZERO — the balance is GEMINI OMNI
     (corrected 2026-07-31: earlier notes said "Higgsfield"; Omni is the
     video generator for EVERY asset in this project — carriers, intros,
     stinger/endcard motions, forthcoming ad bodies. ChatGPT is the IMAGE
     generator — panels, covers, banner — and is not budget-gated the same
     way). Queue when it returns, in
     order: ① seal regeneration (note A) · ② verif_carrier_signoff_short
     (note C) · ③ evening_pro carrier rotation pair. The 1080×1920 upscale
     pilot (standing ask #1) is also blocked on the same balance.

WHAT CHANGED IN v1.9 (MKT-22/23 — asset wave + the intro chip)
  A. THE INTRO NOW CARRIES AN IDENTITY CHIP over the anchor's right shoulder —
     ALL-DAY / MIDDAY / EVENING / YESTERDAY'S RESULTS, and THE FULL BOARD on
     public. 0.40-2.65s. Reels are now identifiable from their first second
     rather than from the stinger headline at 2.7s. §2.
  B. INTRO ROTATION IS 6 (standard, deadpan, arrival, monitors, powerup, board).
     powerup is the REGENERATION of the clip MKT-17 rejected and is now clean.
     board is the first STAGING change in the set — the Anchor standing at a
     wall-sized display. Its wall was audited for glyphs and numerals: clean.
  C. ⚠ VERIFY NO LONGER SHARES AN INTRO. It was pinned to anchor_intro.mp4,
     which a slate kind also drew every single day — a permanent collision that
     a label-based count hid (it read 6/6 while the FILE count was 5/6). Verify
     now takes anchor_intro_verify.mp4, trimmed to 5.625s so its timeline is
     unchanged. It still does NOT rotate; the tonal ruling is unchanged.
  D. PRO ENDCARD COPY IS NOW PER KIND — standing ask #2 CLOSED. Midday, Evening
     and Verify no longer read All-Day's close. Verify's now describes what it
     IS rather than promising verification on the verification reel.
  E. STINGER MOTIONS 3 -> 5 (+ fracture, imprint), PRO ENDCARD MOTIONS 2 -> 4
     (+ steady, lattice). Standing ask #8 CLOSED — the pro tier now has two
     bed-viable motions instead of one.
  F. ⚠ lattice is BED-INELIGIBLE and that is deliberate. Its bed exists but sits
     at -41.1dB, needing +13.5dB against a ±6dB clamp, so it would ship audibly
     quiet. It plays normally on wall-to-wall days and drops on hum-bed ones.
     Respec its audio ~13dB hotter and it re-qualifies itself automatically.
  G. MIDDAY CARRIER is now scope-named VO. ⚠ Its margin is +0.184s — roughly
     HALF the next tightest (free/public +0.331, pro +0.449). It ships, but
     there is no room for a re-cut that runs longer.

WHAT CHANGED IN v1.8 (MKT-20 — the All-Day carrier OPENING now rotates)
  A. ALL-DAY PART 1 IS A SET, PART 2 IS NOT. Three alternate openings per tier
     rotate by date; the methodology sign-off repeats every day the way a
     broadcast segment ends the same way every night. This is deliberate — do
     NOT propose generating six part 2s to "finish" it. §2, §7.
  B. THE PAIRING IS NOW EXPLICIT, not derived from part 1's filename. You do not
     need to name an alternate anything in particular for it to find its part 2;
     it is declared in scripts/carrier-config.ts. What you DO need is to say
     which kind and tier a delivery is for.
  C. ⚠ EVERY PART 1 IN A SET MUST BE THE SAME LENGTH — all six alternates are
     10.005s, matching the incumbents. This is load-bearing, not tidiness: part
     2's offset in the join is part 1's FILE duration, so an alternate delivered
     at a different length moves the VO ceiling on the one morning in four that
     it plays and looks perfect on the other three. Preflight now FAILS on it.
  D. FOUR THINGS NOW ROTATE INDEPENDENTLY — intro, stinger motion, endcard
     motion, carrier opening. They are uncorrelated on purpose; the spread is
     the point. Nothing you script needs to account for the combination.
  E. THE SEAM BREATH IS NOT A CONSTANT. The pipeline inserts 0.35s, but what a
     listener hears is 0.35s plus whatever tail your part 1 already carries —
     measured 0.586s to 0.777s across the current set. Do not treat "0.35s" as
     the audible pause when scripting a hand-off between parts.
  F. SCOPE PAIRING IS ENFORCED IN CODE. An All-Day carrier cannot reach the
     Midday or Evening reel — those name their own scope aloud and have their
     own carriers. Verified across 60 date probes, not asserted.

WHAT CHANGED IN v1.7 (MKT-19 — the brand motion is now a SET)
  A. THE STINGER AND THE ENDCARD ROTATE, like the intro already did. Motion is
     a set of files; copy stays per-variant config. Three stinger motions, two
     endcard motions PER TIER. §2, §7.
  B. TIER PAIRING IS ENFORCED IN CODE, not by convention. Pro kinds (All-Day
     Pro, Midday, Evening, Verify) can only draw pro endcard motions; free and
     public kinds only free ones. You cannot propose a "pro motion on the free
     close" — there is no path for it.
  C. v1.6 §2 WAS WRONG ABOUT THE FREE ENDCARD. It said Free cannot supply a hum
     bed and therefore REQUIRES a wall-to-wall carrier. That described the old
     BAKED endcard, which cracked at ~1.0s; the motion-built one cracks at 4.3s
     and beds cleanly at −29.3 dB. Free has no such restriction. Corrected in
     §2 — if you shelved a ~10s Free carrier idea because of that line, it is
     back on the table.
  D. THE REAL BED RESTRICTION IS PER MOTION, not per tier, and it is handled
     automatically. A build-up motion (one that swells continuously into the
     crack) has no level-steady stretch to loop, so it simply drops out of the
     rotation on short-carrier days and plays normally on wall-to-wall ones.
     Today that is `endcard_motion_pro_alt` (vault rings). Nothing for you to
     track — but it is why a delivered motion may not appear on a given day.
  E. BED LEVEL IS NOW MATCHED ACROSS MOTIONS. Beds were mixed raw at a flat
     gain, so hum-bed days varied by up to 4.8 dB purely on which motion the
     date drew. They are now level-matched to a reference. Audible consistency
     only; no spec of yours changes.
  F. NEW DELIVERY SPEC FOR MOTIONS in §7, and a review command in
     REEL_COMMANDS.txt (`--stinger-motion=` / `--endcard-motion=`) for watching
     one specific combination end to end.
  G. TWO NEW INTROS TRIMMED AND ADDED — the rotation is 4, not 2 (standard,
     deadpan, arrival, monitors). Both arrived at 10.005s as always. Note
     their trims DIFFER (IN 2.0 and IN 0.0): the trim rule is a rule about
     the footage, not a fixed window. §2, §7.

WHAT CHANGED IN v1.6 (six corrections; v1.5 is wrong on all six)
  1. THE MULTI-PART CARRIER CEILING IN v1.5 IS ARITHMETICALLY WRONG. It said
     "10.0 + 7.0 = the 20.0s carrier ceiling", which is 17.0. Corrected in §7 —
     part 2's last word may land as late as ~9.65s. Read that entry before
     writing another carrier; you have ~2.6s more room than v1.5 implied.
  2. REEL DURATION IS NO LONGER A CONSTANT. 33.83s or 34.20s depending on which
     intro the day's rotation draws. §1, §2.
  3. THE INTRO IS NO LONGER ONE SHARED FILE. It is resolved PER REEL KIND, and
     slate reels rotate over a set. §2, §7 (MKT-17).
  4. NEW INTRO DELIVERY SPEC — the generator (GEMINI OMNI — every video asset
     in this project) only emits at 4/6/8/10s presets, so
     EVERY intro delivered to date has arrived at 10.005s against a 6.5s
     ceiling. Trimming is now the expected path, not an incident. §7.
  5. PUBLIC REEL KINDS are in flight and NOT built. §10 states exactly what
     exists so nothing gets proposed against them prematurely.
  6. `reel:check` now runs a STRAY-ASSET SCAN — a delivered file that no
     registry reads is reported instead of sitting silently unused. §7.


--------------------------------------------------------------------------------
1. WHAT THE SYSTEM IS
--------------------------------------------------------------------------------
A fully automated daily video pipeline. Two commands produce, assemble, upload,
and stage everything; the operator posts from their phone with two taps.

  npm run reel:allday   → THREE reels for TODAY (Pro + Free + PUBLIC — the
                          public cut since MKT-16, 7/30): Anchor intro +
                          branded stinger + a 10.0s still of the COFFEE-MODE
                          HOME (six ranked tiles — MKT-57, 8/17; ~~the All-Day
                          signal board~~ HISTORICAL) + all six pick-detail
                          modals, captured from the LIVE app with real data.

  npm run reel:verify   → TWO reels for YESTERDAY (~23-29s — VARIABLE, see
                          §3): "Receipts" — yesterday's verification
                          breakdown, group cut + verify_public (MKT-40).
                          Intentionally refuses to build on zero-match days
                          (no receipts, no reel).
  npm run reel:daily    → all of the above in order (verify → allday → midday
                          → evening), detached; NINE reels. Operator-triggered
                          only — never scheduled.

  npm run reel:midday   → TWO reels each: the PRO variant, plus a FREE variant
  npm run reel:evening    cut from a separate REDACTED capture (digits masked).
                          Same coffee-Home body as All-Day (MKT-57).
                          Added 2026-07-28 (MKT-13), free half 2026-07-29
                          (MKT-26). ⚠ v1.7's "PRO ONLY" is retired — but the
                          bar it recorded still stands: a FULL-FIDELITY free
                          session reel remains barred. What ships is the
                          masked one.

  ⚠ SLATE REEL LENGTH IS NOT A CONSTANT (changed 2026-07-28, MKT-17). It is
  33.83-34.53s depending on which intro the day's rotation draws — the pool
  spans 5.63s (standard) to 6.33s (deadpan, powerup — the two 2026-07-30
  regenerations). v1.5's flat "33.8s each" is retired. Nothing you write is
  affected: the usable VO window is bodyDur+0.4 and is INDEPENDENT of intro
  length (see §2), so carrier specs do not move. Only the finished file's
  duration does.

SESSION WAVE (MKT-13) — read before proposing session content. Midday and
Evening are NOT a different reel: they are the same reel parameterized by
scope. Same anatomy (§2), same caption engine (§5), same publish chain (§4),
same constraints (§6). What differs is which board is captured, the endcard
and stinger copy, and the slate-stamp scope tag.

⚠ THEY WERE PRO ONLY UNTIL 2026-07-29. MKT-26 shipped the free half, and what
replaced the old ruling is NARROWER than "sessions are free now" — the
distinction is load-bearing, so the original reasoning is kept here rather than
deleted.

Still true: the free group gets the All-Day drop in FULL but Midday/Evening
REDACTED — that gap is the Pro conversion frame. What changed is that the
REDACTED CAPTURE now exists (digits masked by DOM injection at capture time, so
the app itself is never modified and there is no flag a subscriber can reach).
Free session reels therefore ship MASKED, keeping everything that is not a
digit: energy, ZK6 confidence, all four signal percentages, heat badges,
jurisdiction counts and the full prose structure. The methodology is the gift;
the digits are the gap Pro closes.

A FULL-FIDELITY free session reel remains barred — that part never lifted, and
the assembler enforces it: each body carries an `hm_reel_redacted` tag and a
redacted kind REFUSES an untagged body (absence means full-fidelity).

Verified 2026-07-29 against the stronger claim — not "no digits visible" but
"the combination is NOT RECOVERABLE by any route" — across all seven
digit-bearing surfaces on the grid and all six modals, both scopes, checked on
pixels against the unredacted capture. The routes that matter are the BOX SET
chip, the grid set chips, the position boxes, the hero row, the three pair
labels, THE PAIR PROSE beneath each row, and arithmetic across any two. All
closed. Note for anyone writing copy about this: the pair route is the one that
reassembles the combination exactly (front 09 + back 94 → 0-9-4), and it is the
one the automated assert cannot see, because a two-digit run trips neither of
its passes.

Adding a further session/variant is config: an endcard-config entry, a
stinger-config entry, a caption kind with a unique offset, a scope entry in
scripts/reel-scopes.ts — plus widening the marketing_reels kind CHECK
constraint and adding a KIND_UI entry, or the publish fails at upsert and the
admin Reels tab cannot render the row.

Each run ends by auto-uploading the finals to the app backend. They appear in
the operator's Admin → Reels screen seconds later with a fresh, auto-written
caption (see §5). NOTHING auto-posts to Facebook — a human always completes
the post (platform rule + brand-safety rule).

Everything is captured from the real product UI — no mockups, no comps. The
renderer drives the actual app, verifies each signal modal is the right one,
and runs an in-frame vocabulary guard; any violation aborts the render.


--------------------------------------------------------------------------------
2. REEL ANATOMY — SLATE DROPS (33.83-34.53s @ 1080×1920 60fps)
--------------------------------------------------------------------------------
Covers All-Day (Pro + Free) AND the Midday/Evening session drops (Pro + Free as
of MKT-26) — they are the SAME reel built from a different board, so this one
anatomy is the whole spec for all six. Variants differ in carrier (voiceover)
and endcard/stinger copy.

⚠ CHANGED 2026-07-29 (MKT-26) — VARIANTS NO LONGER ALWAYS SHARE ONE BODY.
v1.9 said "variants share one UI body render per scope". True for All-Day, where
Pro and Free legitimately show the same board. NOT true for the sessions: their
FREE variant is cut from a separate REDACTED capture with every digit masked, so
each session scope renders TWO bodies.

  ui_<scope>_<stamp>.mp4           full-fidelity — Pro (unchanged name)
  ui_<scope>_redacted_<stamp>.mp4  digits masked — Free sessions only

WHY THIS IS ENFORCED AND NOT LEFT TO CONVENTION. Before MKT-26 the assembler
resolved ONE body per scope, outside the variant loop. Switching the free
session variant on without changing that would have handed the full-fidelity
board to the free group — and nothing would have caught it, because the carrier
resolves, the endcard resolves, the stamp renders, the contact sheet generates
and preflight reports 0 fail. Every one of those validates an ASSET; the defect
was inside the capture. So each body now carries an `hm_reel_redacted` tag in
its own container metadata (same mechanism as MKT-18's date tag) and the
assembler asserts it against the kind consuming it. A body with no tag is
treated as full-fidelity, which means a renamed copy cannot pose as redacted —
a stray copy of the Pro body was in fact caught this way on 2026-07-29.

WHAT THE REDACTION MASKS (structural detection — no marker text, no class
selector, so it cannot be defeated by a surface nobody enumerated): the hero
row, bare 3-digit leaves, the {•••} brace set chip on BOTH grid and modal, the
position boxes, pair LABELS and pair PROSE. Verified non-recoverable across all
seven digit-bearing surfaces at ten settling frames.
KEPT: energy, ZK6 confidence, all four signal percentages, heat badges,
jurisdiction counts, panels, and the full prose structure.

Times below are quoted for the 5.6s standard intro. On a longer rotation
member every timestamp after the intro shifts by the difference (up to 0.7s
on the 6.33s members) and the reel runs up to 34.53s.

  TIMELINE (t in seconds)
  0.0 – 5.6    INTRO — "The Anchor". Opens ON the gesture — frame 1 is the
               Anchor already holding the phone up, smoke live on its screen.
               Internal beats: 0.0–1.1 presents the phone to camera →
               1.1–2.1 camera dives into the screen → 2.1–5.6 full-frame
               purple smoke. Its own audio is a single whoosh sting, present
               from frame 1, peaking ~2.0, decayed to silence by 5.0.

               ⚠ CHANGED 2026-07-28 (MKT-17) — THE INTRO IS NO LONGER ONE
               SHARED FILE. v1.5 said "anchor_intro.mp4, shared by every reel
               kind". It is now resolved PER KIND:
                 slate reels  — ROTATE over a set, currently [standard,
                                deadpan, arrival, monitors, powerup, board,
                                guest, window, arrive, juggle] (guest/window/
                                arrive added 2026-08-13, MKT-54 — v3.0 notes
                                A/B; juggle 2026-08-19, MKT-61 — v3.9;
                                `arrive`'s frame-one letterform hold was
                                cleared by operator ruling the same day),
                                picked by a date-derived offset — ⚠ v2.9:
                                the offset walk is now RESHUFFLED per cycle
                                (v2.9 note B); even usage and per-kind
                                distinctness hold, but the ORDER is no
                                longer fixed and past dates are not
                                furniture-reproducible. (arrival +
                                monitors added 2026-07-29; powerup + board
                                completed the six the same day; powerup and
                                deadpan REGENERATED and revalidated
                                2026-07-30.)
                                ⚠ CORRECTED IN v2.1 (observed 7/31, confirmed
                                against laneRotate's contract): pro and free
                                draw DIFFERENT members on the same day — the
                                rotation is lane-spread per kind so every reel
                                gets a DISTINCT intro where the pool allows.
                                An earlier "both draw the SAME intro" line
                                described the pre-lane behaviour and is
                                retired. Re-running a past date still
                                reproduces that date's intros exactly.
                 verify       — ~~always standard~~ its own fixed file,
                                anchor_intro_verify.mp4 since MKT-23 (the
                                "always standard" here predated it). A
                                deadpan gag ahead of yesterday's receipts
                                is a tonal mismatch — unchanged.
                 public kinds — anchor_intro_public.mp4, and they NEVER
                                fall through into the rotation: a public
                                cut must not silently acquire the deadpan
                                gag. ⚠ v2.9: "always ONE file" is retired —
                                these are now SETS (VERIFY_INTROS /
                                PUBLIC_INTROS); a second CLEARED public
                                intro registers as one line and the public
                                kinds rotate + spread across it. The
                                never-into-the-slate-rotation bar is
                                unchanged and structural. See v2.9 note E.
               Growing the rotation is one line of config plus a delivered
               file — see the delivery spec in §7 before proposing one.
  5.3 – 8.3    STINGER — the intro's smoke crossfades (0.3s) into a branded
               beat: smoke condenses into the bolt, the wordmark + variant
               headline resolve natively, then the smoke sweeps back and
               refills the frame. It both OPENS and CLOSES on smoke, which is
               why the existing dissolve below needs no change.
               Per-variant copy; prebuilt by npm run stinger:build.
               ⚠ v1.7 — THE MOTION ROTATES. SIX shared motions as of
               MKT-58, 8/19 (bolt condense, strike, circuit, fracture, etch,
               facet; `imprint` retired 7/31; ~~FOUR~~ HISTORICAL), picked by date on the same clock-free basis
               as the intro and panels, shared across tiers because a
               stinger carries no tier signal — plus the SEAL set, which
               serves verify/verify_public ONLY and replaces the rotation
               for those kinds (§3). Copy is unaffected: the headline is
               still per-variant config, rendered over whichever motion
               the day draws.
  7.5 – 8.3    0.8s eased dissolve from the stinger's smoke into the UI body
               (overlaps the tail — not extra time).
  8.3 – 27.3   BODY (19.0s) — 10.0s STATIC still of the COFFEE-MODE HOME
               (v3.4/MKT-57: scope switcher · NEXT DRAW · 2×3 rank tiles ·
               black space with the notation band + date chip; was the Slates
               2×3 signal grid until 2026-08-16; NO zoom/pan — v3.3/MKT-56,
               members notate the picks off this frame), then each signal tapped in
               rank order 1→6: modal opens, 1.5s hold on digits/energy/
               confidence/breakdown, close. Hard cuts between modals.
               (Was 4.0s push-in + 6×2.5s until 2026-08-16.)
  18.3 – 27.3  PANELS — a rotating branded panel sits below the "RESOLVED IN"
               card in each of the six pick-detail modals. It is NOT added to
               the video: it is a real element of the app screen, so it is
               captured along with everything else. A different panel appears
               on each modal; which one leads rotates by date. Panels appear
               in the modals only — the still carries the notation band and
               date chip in its black space by ruling (v3.4), not a panel.
               ⚠ CHANGED 2026-07-28 — PANELS ARE APP-WIDE. An earlier version
               of this doc said the panel was visible only while the renderer
               was driving the app and that "subscribers never see it". That
               capture gate was REMOVED by operator decision: every user now
               sees these panels, free and paying, and the slot is destined to
               become a monetised placement (ads for free users, in-house
               panels for subscribers). Treat panel copy as PRODUCT surface,
               not marketing overlay — a paying subscriber reads it, so it
               must survive that room, not just a reel frame.
  27.3 – 33.8  OUTRO (6.5s) — the endcard's FIRST 6.5s, hard cut in:
               smoke rise → bolt snap → lockup resolves → calm hold.
               THE SNAP TIME DIFFERS PER ENDCARD — it is a property of the
               asset, not of the timeline. Measured 2026-07-27:
                 Pro  — crack at 4.4s of the endcard → ~31.7s of the reel
                 Free — crack at 4.3s of the endcard → ~31.6s of the reel
               ⚠ v1.7 — THE ENDCARD MOTION ROTATES TOO, per tier. Measured
               across the full built set 2026-07-29:
                 Pro  std (converging threads) 4.4s  ·  alt (vault rings) 4.2s
                 Free std (blooming eddies)    4.3s  ·  alt (rising tide)  4.5s
               So the snap lands in a 4.2–4.5s band whichever motion the day
               draws — a 0.3s spread, which nothing in a carrier script is
               tight enough to notice. Do NOT treat any of these as a fixed
               number: the assembler measures each endcard it uses, and the
               band is here so you know the size of the variance, not so you
               can script to a value.
               (Both rebuilt from motion files 7/28; the old baked Free
                endcard cracked at ~1.0s, which is why it could not bed.)
               (An earlier version of this doc quoted a single "~22.8s" for
               both. That was never measured for Pro and was wrong by 3s.)

  DEGRADATION, IN ORDER (the daily run can never block on the intro):
  a missing or defective rotation member drops out of THAT DAY with a logged
  warning and the next candidate is used; if every candidate is unusable the
  assembler falls back to the legacy 1.2s lockup open and the reel is 26.7s.
  Note the asymmetry, which is deliberate: preflight treats a MISSING intro as
  a warning (the fallback handles it) but a DEFECTIVE one as a hard FAIL — a
  bad variant must fail before assembly rather than quietly degrading a run.

  AUDIO (two automatic modes, loudness −14 LUFS integrated):
  The intro carries its own sting, and the stinger its own impact. The
  carrier VO enters 0.4s before the dissolve completes — at 7.9 on the 5.6s
  intro, 8.3 on a 6.0s one. Carrier time 0 sits at whichever of those it is,
  and every VO length below is measured in CARRIER-LOCAL seconds, which is
  exactly why the rotation does not touch your specs.
  The usable VO window is bodyDur+0.4 = 19.4s and is INDEPENDENT of intro
  length, so retiming the intro never re-opens the carrier spec.
  - LONG CARRIER (VO ≥ ~19.4s) — "overlap mode": the voice plays wall-to-
    wall over the body and may ride up to 1.1s into the smoke rise, but
    must finish before the endcard's bolt snap (see the timeline above —
    Pro ~31.7s, Free ~31.6s). The endcard's own synced outro audio is
    mixed in from 27.3s. Carrier content past the VO is DISCARDED
    (generated tracks are unreliable after their voice ends).
    In practice the 20.5s carrier fade bites first, so that is the number
    to script to — not the snap.
  - SHORT CARRIER (VO ~10s) — the voice covers the grid + first modals;
    the endcard's PRE-crack tension hum is looped as a palindrome and laid
    under the remaining modals, then the endcard plays its outro natively.
    ⚠ CORRECTED IN v1.7 — the "Free cannot bed" rule is GONE. v1.6 said the
    Free endcard's crack lands at ~1.0s with the audio decaying to silence,
    so Free REQUIRED a wall-to-wall carrier. That was true of the old BAKED
    endcard and stopped being true when it was rebuilt from its motion file
    on 7/28: the built Free endcard cracks at 4.3s and beds at −29.3 dB.
    Both tiers bed fine. A ~10s Free carrier is a legitimate option again.
    The bed is DERIVED per endcard at build time, never hardcoded — which
    is exactly why this correction happened automatically in the code and
    only the prose was stale.
    The restriction that DOES exist is per MOTION, not per tier: a motion
    that swells continuously into its crack has no level-steady stretch to
    loop, so on short-carrier days it drops from the rotation and another
    motion is used. Today that is endcard_motion_pro_alt. Handled
    automatically; preflight reports how many motions per tier can bed, and
    FAILS only if a whole tier has none.

  ⚠ THE ALL-DAY OPENING ROTATES (MKT-20, 2026-07-29). Part 1 is a SET of
  four per tier (incumbent + three alternates) picked by date; part 2 is
  SHARED and plays every day. That split is deliberate and is not an
  unfinished job: part 1 is the greeting and rotates, part 2 is the
  methodology sign-off and repeats, the way a broadcast segment ends the
  same way every night. Do NOT propose generating six part 2s to
  "complete" it. ⚠ UPDATED 2026-08-03 (MKT-48): EVENING PRO NOW ROTATES
  TOO — a set of four on the same All-Day shape (incumbent = entry 0 and
  fallback; landed / dusk / last as alternates), all joined to the SAME
  shared pt2. Midday and Verify still do not rotate — one carrier each —
  and cross-scope reach remains impossible, since every one of these says
  its scope aloud.

  WHAT THIS MEANS FOR CARRIER PRODUCTION (your main deliverable):
  - Target VO length: ~19.4s for wall-to-wall narration (preferred), or
    ~10s accepting the hum-bed remainder. Anything between works; the
    assembler adapts automatically and prints which mode it used.
  - Write VO to the visual beat map, in CARRIER-LOCAL time (⚠ re-timed
    by MKT-56/57, 8/16-17): 0–0.4 lands under the last of the dissolve →
    0.4–10.4 the SIX TILES of the coffee Home, a static still members
    notate from (the VO may read the board; do not say "tap in" or
    describe a modal before ~10.4) → 10.4–19.4 one pick-detail modal every
    1.5s → if long-form, final line can land over the smoke rise.
    ~~0.4–4.4 the full board → 4.4 onward, one signal detail roughly every
    2s~~ HISTORICAL (the 4.0s push-in + 6×2.5s body, retired 2026-08-16).
  - Do NOT write VO for the intro. The Anchor beat is deliberately
    voiceless — it carries its own sting and the reel's first words land
    as the grid appears. The sting is present from frame 1 by design: the
    reel must never open on silence (autoplay retention).
  - Never script the VO to reference the bolt snap or anything after it —
    that space belongs to the endcard's synced sound.
  - Two variants every time: PRO (first-access framing, never pricing) and
    FREE. ⚠ THE FREE BRIEF IS NOT ONE BRIEF — it splits by scope, and the
    two halves are near-opposites:
      ALL-DAY FREE  — pure value, NO Pro pitch at all (SOCIAL-13 depth rule).
                      This is the value gift; do not sell against it.
      SESSION FREE  — exists TO sell the gap (MKT-26). The board is shown and
                      the digits are withheld, so the VO must NAME the
                      withholding: a redacted reel that doesn't say why it is
                      redacted reads as broken, not as a teaser. Pricing is
                      permitted here and nowhere else — the free group is
                      tier 2, the only tier brandLint §6 allows it on.
      Session free carriers must NOT reuse the Pro ones: first-access framing
      is barred below Pro.

  ENDCARD CONTRACT (full spec in the endcard revision brief):
  - Final frame must be the settled lockup (it is reused as the reel's
    opening frame — the loop is intentional).
  - First 6.5s must be the formation sequence (used as the outro).
  - Audio must be front-loaded and synced to the visual snap; the
    assembler hard-aborts if endcard audio is too short for the outro/bed.


--------------------------------------------------------------------------------
3. REEL ANATOMY — VERIFY / "RECEIPTS" (~23.4-28.8s @ 1080×1920 60fps — envelope updated 2026-07-30, MKT-31 addendum)
--------------------------------------------------------------------------------
  ⚠ SINCE MKT-40 THIS PIPELINE EMITS TWO CUTS PER DAY. Everything below
  describes the GROUP cut (real digits, tier 2/4). The PUBLIC cut
  (verify_public) shares the same beat map, carrier, seal stinger and gold
  ribbon, and differs in exactly five ways: (1) body is the MASKED capture —
  digits dotted upstream, EXACT ORDER / ANY ORDER vocabulary, sessions +
  states dropped, rollup reframed to structural stats, fail-closed sweep
  audit + PUBLIC_TAG assertion; (2) intro is the cold-open public file, not
  verify's fixed group intro; (3) chip reads YESTERDAY'S RECORD (gold,
  dated); (4) endcard is its own free-motion matrix — PUBLISHED FIRST.
  CHECKED AFTER. / SEE THE FULL RECORD FREE — on the SHARED band (as of
  MKT-45 EVERY kind is, verify included; the parenthetical that used to
  read "the 880 tuck fails on the free motions" is now moot — there is no
  tuck anywhere); (5) captions/YT set are its
  own tier-1 deliveries. v2.6 has the summary; the audit entry the detail.
  ⚠ THE VERIFY BODY IS NO LONGER A FIXED LENGTH (changed 2026-07-29, MKT-27).
  v1.7 documented a 6.3s body, a 14.4s total and a flat 9.2s carrier need. All
  three are retired. The operator ruled that a viewer must be able to read a
  matched row completely — digits, badge and "Drew X in Y" — with no pause, and
  1.0s holds were under a reading pass. The body is now built from the row
  selection, so it is DATA-ADAPTIVE the way the caption engine is:

    slate 2.50s + summary band 2.00s + pan 2.00s
      + per featured row: 2.00s if it is a STRAIGHT, 1.60s if a box
      + 0.60s travel between holds + 0.30s settle

  Up to THREE featured rows, straights first, boxes backfilling. Straights get
  the longest holds by ruling — they are the strongest proof in the reel.

  0.0 – 5.6    INTRO — the STANDARD Anchor intro, always. Verify draws a FIXED
               intro and never a rotation member (MKT-17), so the OPEN is stable.
               ⚠ Its STINGER is ruled the same way as of 2026-07-30 (MKT-29):
               PINNED to the closing-bell seal — "session closed, results
               final" is true of receipts and false of every 8:30am drop.
               ✅ CLEARED 2026-07-30 (MKT-31 item 5): the re-delivery measures
               clean (peak −17.4dB at 1.10s, nothing after 2.4s above
               −19.2dB) and the seal SERVES as verify's motion REPLACING
               rotation — a missing/defective build degrades to NO stinger,
               never a slate motion; a re-armed `held:` restores rotation.
               ⚠ v2.9: "FIXED" is now a SET (VERIFY_SEAL_MOTIONS) of one —
               seal VARIANTS (same bell concept, different cuts) register
               as one line and verify + verify_public spread across them.
               Everything else in this paragraph holds per member,
               including per-member `held:`. See v2.9 note E. The stinger renders headline-dominant (MKT-31 item
               1): "YESTERDAY'S RECEIPTS" ~62px on top, wordmark small in
               gold beneath — verify only.
               ⚠ THE CHIP SAYS "YESTERDAY'S RESULTS", THE STINGER SAYS
               "YESTERDAY'S RECEIPTS" — DELIBERATE, ruled 2026-07-30. The chip
               identifies the reel in cold-viewer language FROM FRAME ONE
               (MKT-35: full opacity, no fade-in — frame one is the
               camera-roll thumbnail; it also carries yesterday's date in
               gold); the stinger frames it in brand language at 5.6s.
               Escalation, not repetition. Do not "correct" either to match
               the other, and do not reintroduce a chip fade-in.
  5.6 – 8.3    STINGER (MKT-12, ENABLED for verify 2026-07-29) —
               "HITMASTER ZK6 / YESTERDAY'S RECEIPTS", 3.0s crossfaded 0.3s
               into the intro's smoke, so it ADDS 2.7s. Barred until MKT-27
               because 11.1s of branding against a 6.3s body was 64%; the
               longer body brought that to 44%.
  8.3 – …      BODY — 8.6s to 14.0s. Reference render 2026-07-28: 11 matched
               rows, 2 straights → 3 featured → 13.60s body, 24.43s reel.
               In practice any day with 3+ matched rows lands 12.80-14.00s.
  … – +6.5     CLOSE — ⚠ CHANGED 2026-07-30 (MKT-31 addendum, blocking
               postability): the FIRST 6.5s of the endcard, All-Day's proven
               window (was: last 2.5s, a hard cut to a static stranded
               lockup). Formation fills the frame, text fades in 4.5-5.2s
               under living motion, the reel ends at endcard t≈6.4s — never
               reaching the barren 10s tail. Endcard audio stays unused
               (verify's ruling: carrier, then silence). Cost as ordered:
               +4.0s to the reel.
               ⛔ THE 880 LOCKUP RULING IS STRUCK — REVERSED 2026-08-01 BY
               MKT-45. Verify's endcard text sits on the SHARED 1240 band,
               identical to every other kind. The tuck (lockupTop 880,
               block y880-1040) shipped twice and was rejected on sight
               both times: it lifts the mark+text group into the top half
               and empties the bottom. **Frame balance wins.** There is no
               longer a tuck zone, so a NEW verify endcard motion no longer
               needs to re-measure y880-1040 — it only has to be bed-viable
               under text from 4.5s, like every other kind. See v2.7 A/B.
               A persistent gold DATE RIBBON (MKT-31 item 2) rides EVERY body
               frame — "YESTERDAY'S RECEIPTS · <day> · <date>", real date
               from reel_date — and fades out AT the endcard cut.
               The BOARD segment (MKT-35, 2026-07-31) renders straights GOLD
               with "DREW · STRAIGHT"; box matches stay green. Posted==drawn
               must carry the straight annotation or the render aborts.
  AUDIO        The intro's own sting, then the stinger's own impact, then the
               carrier VO 0.4s before the dissolve completes. −14 LUFS.
               TWO PARTS since 2026-07-29: verif_carrier (10.005s, the day's
               read) + verif_carrier_pt2 (an EVERGREEN sign-off over the
               last hold and the close, joined with a 0.35s breath).
               ⚠ BOTH FILES WERE REPLACED 2026-08-01 (MKT-43) as a designed
               SAME-SESSION pair — pt2 is now 6.542s, NOT 5.667s, and the
               join is 16.897s, NOT 16.02s. pt2 got LONGER (slower read,
               wider gaps), which cuts both ways: coverage improves to a
               body of 13.9s, while short-body days fall back to
               signoff_short more often. v2.7 note F has the spoken lines.
               Measured against a need of bodyDur+6.9 (the 6.5s close moved
               the window — MKT-31 addendum: carrierNeed for the reference
               body 13.50→17.50s, so the full pt2 fits-whole again; VO
               covers 23.94s of the 25.43s reference cut, silence at the
               close 1.49s).
               ⚠ THE WINDOW MOVES, so the close resolves BY WINDOW (updated
               2026-07-30): full pt2 when it fits → the SHORT sign-off
               (verif_carrier_signoff_short.mp4, ~2.2s of speech — slot
               FILLED 2026-07-31 by the MKT-32 addendum splice) when it does
               not → part 1 alone (silence) only if neither fits. Every step plays WHOLE or not at all — never cut
               mid-word.
               ⚠ CORRECTED 2026-07-30 (content agent): the 0-straight drop is
               a pure DURATION consequence, NOT a content mismatch — pt2's
               copy is badge-agnostic by design ("that one's on the record"
               works over a box MATCH exactly as well). v1.9's "fits worst"
               rationale was wrong, which is exactly why a short variant
               closes the gap cleanly.
               The audio stream is padded to the full container length, which
               it was not before (legal mp4, but transcoder-hostile).
               ⚠ The stinger does NOT widen the gap — need derives from the
               body, and the stinger carries its own audio over the intro.

  Without a usable intro this reverts to the legacy 1.2s bolt open; the body
  and carrier arithmetic above are unchanged, only the open shortens.

  - Builds for YESTERDAY by default. If yesterday had zero verified
    matches, the pipeline aborts on purpose — we never fake receipts.
  - With 0 DETECTABLE rows (a DOM change, not a data condition) it degrades to
    board + summary + pan and no holds, rather than aborting.
  - ⚠ NOTHING MAY HARDCODE THE FRAME COUNT. The renderer prints its beat plan
    ("beat plan: 816 frames = 13.60s …") and the assembler derives total, stamp
    windows and contact-sheet stamps from the measured body. A constant 378
    silently truncated the reel mid-hold, which is how this was found.


--------------------------------------------------------------------------------
  ── 3b. THE SAME-DAY MIDDAY VERIFY (`verify_midday`, MKT-62, 2026-08-19) ──
  A THIRD cut from this pipeline, on a DIFFERENT trigger: manual, rare,
  never reel:daily (`npm run reel:verify-midday`). Same rig, same beat map
  shape, and EVERY STRING IS ITS OWN — verify's say YESTERDAY and all of them
  are wrong here. Differences from the group cut, exhaustively: (1) date =
  TODAY, rows = MIDDAY scope only (capture-gated `?scope=midday`, the
  sanctioned MKT-51 exception); (2) intro = `anchor_intro_rise` PINNED (the
  Anchor stands); (3) NO STINGER by ruling; (4) the board beat is the COVER-
  LIFT — covered-as-posted 2.0s → lift 0.5s → graded 2.5s; (5) the summary
  band is the TIMESTAMP PAIR (PUBLISHED / GRADED / ELAPSED / MIDDAY MATCHES)
  from provenance — midday_free.posted_at and the hit-detection clock — with
  three fail-closed aborts; (6) chip TODAY'S MIDDAY · GRADED, ribbon TODAY'S
  MIDDAY · GRADED; (7) carrier `verif_carrier_sameday` single-part, set of
  one; (8) endcard `endcard_motion_sameday` PINNED, lines POSTED THIS
  MORNING · GRADED THIS AFTERNOON / ZK PRO READS EVERY BOARD FIRST · $2.49/MO
  (pricing legal — FREE GROUP ONLY, no public variant, no cross-post);
  (9) retention-exempt; (10) contact sheet 5 frames. Body ≈ 10.9s (1 box) /
  ~13.3s (median); total ≈ 23–25s; furniture 12.1s → ~53% floor / ~48%
  median. Every one of the three assets is PINNED, NOT POOLED — the standing
  beat and the "same day" lines are false on any routine reel.

4. AFTER ASSEMBLY — THE AUTOMATIC PUBLISH CHAIN
--------------------------------------------------------------------------------
Per reel, three finals are produced and uploaded (working files stay local):
  - 9:16 master        e.g. allday_pro_20260727.mp4 / verify_reel_20260726.mp4
  - 1:1 feed cut       RETIRED (MKT-39 addendum, operator, 2026-07-31): zero
                       logged use across 48 assisted posts; was 37% of bucket
                       storage + 8 encodes/run. No registered platform
                       declares a 1:1 asset — platform apps letterbox 9:16
                       themselves. Resurrect recipe commented in
                       assemble-allday-reels.ts.
  - contact sheet PNG  …_contact.png (the storyboard strip)

Upload target: public storage (bucket "marketing-reels"), registered in the
app's database one row per (date, kind). kind ∈ allday_pro | allday_free |
allday_public | verify | verify_public | midday_pro | evening_pro |
midday_free | evening_free.
⚠ RETENTION IS KEEP-CURRENT-ONLY (MKT-38, operator directive 2026-07-31):
when a kind's new-day version publishes, every OLDER version of that kind is
DELETED — storage objects removed, row archived (posted history kept), local
finals/bodies swept. Per-kind and relative to the new row's own reel_date
(verify is D−1 by design — a today-based window would delete it the morning
it ships; do not refactor it into one). The 30-day prune stays as backstop.
Do not propose keeping N days, and do not expect yesterday's mp4 to exist
anywhere but git history. The kind list is enforced by
a database CHECK constraint AND needs a matching entry in the admin Reels
screen — see §8 before proposing a new reel kind.

⚠ PUBLISHING ONE VARIANT OF A SCOPE NEEDS `--variant` (added 2026-07-29). The
upsert key is (reel_date, kind) and it DELIBERATELY resets status/posted_at — a
re-render is a new reel, so a stale posted flag must not survive it. Correct per
kind, destructive across siblings: publishing the free session reel after the
pro one has already gone out re-upserts BOTH rows and wipes the pro row's
posted stamp. Use `publish-reels midday --variant=free`.

The operator's Admin → Reels screen then offers, per reel:
  - contact-sheet storyboard preview + a button to watch the 9:16 (the 1:1
    button hides itself now that the cut is retired)
  - target selector: Free Group / Pro Group / Cross-Post
  - the auto-written caption in an editor, gated by the full brand-lint
    engine (a blocking vocabulary violation disables the send buttons)
  - Two-Question NO/NO checkboxes, required for cross-posts (reels show
    real digits, so cross-posts honestly fail Q1 — that is the filter
    working; reels live in the groups)
  - posting flows: phone = Save to Photos + Open Group (attach from camera
    roll inside Facebook — the only reliable FB video path); web =
    Prepare Video, then Save/Share (native sheet) or Download
  - every handoff is logged centrally with same-day duplicate-caption
    detection across all surfaces

There is deliberately NO public-page (API) lane for reels and NO auto-posting.


--------------------------------------------------------------------------------
5. THE CAPTION SYSTEM (matters for any copy you propose)
--------------------------------------------------------------------------------
Captions are generated at publish time by a template engine — not written
per-day by hand, not AI-freestyled.

  ROTATION — each content kind has 12 template families (allday_public 8;
  grown from 8 by MKT-37, 7/31). The day's date picks
  the template deterministically: a different style every day, never the
  same style on two kinds the same day (protects the duplicate-caption
  rule), and re-running the same date reproduces the same caption exactly.
  Kinds today: allday_pro, allday_free, allday_public, verify (free draft),
  verify_pro, verify_public, midday_pro, evening_pro, midday_free,
  evening_free. YouTube title/description/tags are a per-platform TRANSFORM
  of the caption (MKT-37), not a separate pool. The three PRO slate kinds
  are spaced so they never
  draw the same template family on the same day — a subscriber who sees the
  Midday, Evening and All-Day posts reads three different messages.

  THE NUMBERS POLICY (operator ruling, absolute):
  - PRO captions ONLY may contain real performance data: verified counts,
    state attributions, STRAIGHT MATCH callouts, 30-day totals. These are
    computed fresh from the verified results record (yesterday's board
    checked against actual observed draws — never cached flags).
  - FREE captions are QUALITATIVE. They may DESCRIBE matches creatively,
    but only in descriptor language — scale words ("a verified MATCH" /
    "multiple verified MATCHES" / "a stack of verified MATCHES"), spread
    words ("across several state boards" / "from coast to coast"), and
    STRAIGHT MATCH presence ("one of them a dead-on STRAIGHT MATCH").
    Never counts, never state names. The video shows the receipts; the
    caption text a classifier reads stays clean. Gambling-adjacency
    protection — do not ask for exceptions.
  - THE VERIFY REEL CARRIES TWO DRAFTS (one reel, two rooms): a free-group
    draft (qualitative description, above) and a pro-group draft (full
    precision). In the operator's Reels screen, switching the target
    between Free and Pro loads the matching draft automatically.
    Example, same day (7/24):
      FREE: "Grade day: 7/24 closed with a stack of verified MATCHES from
             coast to coast — one of them a dead-on STRAIGHT MATCH. Pull
             your state's results and compare."
      PRO:  "Graded in full: 7 MATCHES across 7 states on 7/24 — including
             a STRAIGHT MATCH in PA. 🧾"

  VOICE RULES for any template you propose:
  - The product noun is "signals". Always. (Never picks/plays/numbers.)
  - Match vocabulary: MATCH / BOX MATCH / STRAIGHT MATCH. Never "hit(s)",
    never "partial match", never win/won/winning.
  - No guarantees, no urgency hype ("don't miss out", "act now"), max ~2
    emoji, viewer-friendly plain language — receipts-and-record energy,
    not hype.
  - Free All-Day captions never pitch Pro. Pro captions never mention
    pricing.
  - Approved coverage claims: "40+ states & provinces" or "39 tracked
    feeds" (never "40+ jurisdictions").

  PROPOSING NEW TEMPLATES: deliver lines as text following the rules above,
  tagged by kind (pro / free / verify). Engineering adds them to the
  registry — template count per kind can grow beyond 8; rotation adapts.

  New content types (future reels/posts) get their own registry entry with
  the same contract. The realNumbers flag is structural: a non-pro template
  physically cannot access performance data.


--------------------------------------------------------------------------------
6. HARD CONSTRAINTS (non-negotiable, from the brand brief)
--------------------------------------------------------------------------------
  - The Facebook page is under algorithmic probation (de-recommended twice,
    appeals denied). Public page = brand-only; reels and their captions are
    GROUP content (free = conversion funnel, pro = paid room).
  - Two-Question filter is mandatory before anything leaves surfaces we
    control (cross-posts): Q1 no 3-digit numbers visible, Q2 no forbidden
    vocabulary rendered/spoken. Both must be NO.
  - Pro group price is $2.49/mo where pricing is allowed (free-group copy);
    NO app subscription price in any ad or caption.
  - The in-frame vocabulary guard and the caption lint are enforcement, not
    suggestions — plan assets so they pass, don't plan appeals.


--------------------------------------------------------------------------------
7. CURRENT ASSET INVENTORY (what you own / can replace)
--------------------------------------------------------------------------------
  assets/marketing/
    anchor_intro.mp4              THE ANCHOR intro, "standard" — rotation
                                  member AND the fallback every kind
                                  degrades to. 5.6s, trimmed 1.9→7.5s.
    anchor_intro_deadpan.mp4      Rotation member, 6.3s (IN 0.0 / OUT 6.3 +
                                  0.4s tail fade). REGENERATED 2026-07-30 to
                                  fix its quiet open — its one job, and it
                                  did it: first 0.3s now −46.6dB mean /
                                  −18.0dB max (old −56.2; public reference
                                  −37.9) — mid-fleet, hotter max than public.
                                  New beat map: gag open, phone raise
                                  2.5-3.3s, dive 3.3s, device out by 5.7s,
                                  bed 5.8-6.3 clean. Chip zone clear (the
                                  note sits center-low through 0.40-2.65s).
    anchor_intro_arrival.mp4      Rotation member, 6.0s (IN 2.0 / OUT 8.0).
                                  Added 2026-07-29. THE SHOT `powerup` WAS
                                  SUPPOSED TO BE: the camera pushes fully
                                  into the handset and it leaves frame
                                  entirely by 5.0s, so the close is real
                                  full-frame smoke (tail spread 71, edge
                                  0.93). Its master opened on ~1.7s of
                                  near-static desk over DIGITAL SILENCE, so
                                  the in-point is the AUDIO onset (2.0s),
                                  not the picture cut (1.667s) — cutting on
                                  the picture would have opened the reel on
                                  a third of a second of nothing.
    anchor_intro_monitors.mp4     Rotation member, 6.0s (IN 0.0 / OUT 6.0).
                                  Added 2026-07-29. Audio from frame 1
                                  (-34.3 dB), so no slide was needed. Opens
                                  on the MONITOR WALL rather than the
                                  Anchor — a deliberate variation, and the
                                  only rotation member that does. ⚠ its
                                  master pulls back OUT to the newsroom from
                                  ~7.0s (the MKT-08 tail failure), so the
                                  out-point must stay ahead of that; there
                                  is no usable bed anywhere after 7.0s.
    anchor_intro_guest.mp4        Rotation member, 6.21s (IN 0.0 / OUT 6.2).
                                  Added 2026-08-13 (MKT-54). The pool's
                                  first TWO-SHOT: bronze, rounder, older
                                  domestic-model robot — amber visor slab,
                                  burgundy robe, mug — beside the Anchor;
                                  reads as a different MAKE, not a recolour
                                  (operator may overrule). Nod exchange,
                                  phone raise ~4.0s — ⚠ raised by the
                                  ANCHOR, not the guest as briefed. v3.1:
                                  recorded as a MEANING CHANGE, not merely
                                  cosmetic — the shot reads as the Anchor
                                  presenting TO a guest, not a guest
                                  ENDORSING the product. A different beat.
                                  Accepted for the slate rotation;
                                  re-evaluate before any public or ad use
                                  of this member. Dive
                                  complete 5.6s, bed 5.7-6.2 clean (spread
                                  80, edge 0.96). Audio from frame 1
                                  (first 0.3s −50.3 mean / −38.5 max —
                                  quiet open, deadpan-class). Both figures
                                  + phone verified inside y420-1500. Wall
                                  glyph-swept at 2x: chart smudges only.
    anchor_intro_window.mp4       Rotation member, 6.21s (IN 0.0 / OUT 6.2).
                                  Added 2026-08-13 (MKT-54). Back to camera
                                  at a pre-dawn city, turn ~2.3s, phone
                                  raise ~2.6s, dive complete ~5.5s, bed
                                  5.7-6.2 clean (spread 60, edge 0.90).
                                  Audio from frame 1 (−51.3 mean / −35.6
                                  max). ⚠ Its master GHOSTS THE PULL-BACK
                                  through the smoke from ~6.6s (fully back
                                  in the room by ~7.5s) — any re-trim must
                                  keep OUT ≤ ~6.3s. Skyline glyph-swept at
                                  2x across four timestamps: bokeh lit
                                  windows only, no signage or numerals.
                                  Chip sits on the dark sky — the best
                                  chip contrast in the pool.
    anchor_intro_arrive.mp4       Rotation member, 6.25s (IN 0.310 / OUT
                                  6.510). Added 2026-08-13 (MKT-54). IN is
                                  the AUDIO ONSET — the master opens on
                                  0.31s of silence over the empty desk.
                                  Empty-desk beat, walk-in 0.5-2.0s, seated
                                  2.5s, dive complete 5.6s, bed clean
                                  (spread 69, edge 0.94); master pull-back
                                  starts ~7.9s. ⚠ Its desk-front display
                                  shows a legible white "S" letterform
                                  frame 0 → ~1.2s INCLUDING FRAME ONE (the
                                  thumbnail) — held for a day, then
                                  ACCEPTED by operator ruling 2026-08-13
                                  as set dressing (news-desk bug, not a
                                  digit board). Acceptance covers group
                                  tiers only: a future public use of this
                                  rotation re-opens the Q1/Q2 question for
                                  this member specifically. Master at
                                  anchor_intro_arrive_master_10s.mp4.
    anchor_intro_juggle.mp4       ✅ REGISTERED 2026-08-19 (MKT-61) — the no-
                                  look toss. Master kept as
                                  anchor_intro_juggle_master_10s.mp4 (10.000/
                                  10.005s, 720×1280). Trim IN 0.450 (= AUDIO
                                  ONSET; picture is continuous) / OUT 6.650 →
                                  6.208s v / 6.200s a. Full-frame smoke from
                                  ~5.9s of the master (bezel gone by 5.875);
                                  smoke holds to ≥9.5s, so any re-trim moves
                                  OUT later, never before ~6.4. Visor NEVER
                                  tracks the phone (eye-checked 0.25–3.3s at
                                  1/8s steps). Phone apex y≈497 — under the
                                  visor top, 77px inside the 1:1 band. Chip
                                  zone (x520–1008 / y500–700) ≤1.06% phone
                                  pixels through 2.65s. Open RMS −33.5dB
                                  (first 0.3s after IN; mid-fleet). Wall =
                                  standard screen bank, chart smudges only at
                                  2x. Frame one = the first toss in flight
                                  (motion-blurred phone at desk height).
    anchor_intro_rise.mp4         ✅ REGISTERED 2026-08-19 (MKT-62) — PINNED
                                  to `verify_midday` ONLY (VERIFY_MIDDAY_
                                  INTROS, a set of one; NEVER the slate
                                  rotation, VERIFY_INTROS or PUBLIC_INTROS —
                                  the Anchor STANDS, the only beat where he
                                  leaves the chair, and that is false on a
                                  routine reel). Master kept as anchor_intro_
                                  rise_master_10s.mp4 (10.000/10.005s,
                                  720×1280, 24fps). Trim IN 0.56 (= AUDIO
                                  ONSET) / OUT 6.45 (full-frame smoke from
                                  ~5.75s of the master; phone bezel gone by
                                  5.75) → 5.958s v / 5.89s a. Seated at 0.0,
                                  standing by ~1.5s, phone raised centre at
                                  ~4.5s. Chip composited over frames 0–2.6:
                                  the seated head sits where verify/juggle/
                                  board's do (jaw/collar line) — same class,
                                  PASS. Sting beats 0.56/1.10/2.0/3.34.
    anchor_intro_coffee.mp4       ⏸ HELD 2026-08-19 (MKT-61) — delivered
                                  10.000/10.005s, NOT trimmed, NOT registered
                                  (UNREFERENCED_OK carries the reason). Two
                                  rulings owed: (1) the master opens on 1.94s
                                  of SILENCE — the audio-from-frame-one rule
                                  (MKT-08) and the in-point rule (later of
                                  picture cut / audio onset) would put IN at
                                  1.94, INSIDE the mug-at-face hold that is
                                  the joke; (2) the hold itself measures
                                  ~0.65–0.8s (mug reaches the face ~1.6–1.75,
                                  lowering by ~2.45) vs the brief's ~1.0s
                                  floor — "materially under" per the order.
                                  Everything else passes: mug plain white and
                                  unmarked at 2x, wall clean, chip zone clear
                                  (steam stays centre-left, ≤4.6% bright
                                  pixels in the zone = the screen bank), full-
                                  frame smoke from ~5.5s through ≥9.5s, open
                                  RMS −44.4dB at the onset (deadpan class).
                                  Options: IN 0.0/OUT 6.2 (silent head, joke
                                  intact, 6.2s) · IN 1.94/OUT 7.6 (audio from
                                  frame one, opens mid-hold, lift beat lost,
                                  5.66s) · regenerate with ambience from frame
                                  one and a longer hold. Operator chooses.

    anchor_intro_public.mp4       Fixed intro for the PUBLIC kinds, 6.000s.
                                  ⚠ REPLACED 2026-07-31 (MKT-36): the 7/31
                                  regeneration BEAT the incumbent on the
                                  comparative gate and serves. Trim IN 0.4 /
                                  OUT 6.4 of the 10.005s master (smoke fills
                                  frame 5.8s + 0.6; anchor ghosts back
                                  through the smoke from ~6.6s so the out
                                  cannot slide later; in = out − 6.0 lands on
                                  the ignition flare — opens on motion).
                                  Measured vs incumbent: onset first 0.3s
                                  −20.2dB mean / −10.4 max (incumbent −37.9
                                  — the new open is the fleet's hottest);
                                  bed 5.9-6.4 clean full smoke; edge/centre
                                  93-96% (incumbent 90-91%); tail last 0.2s
                                  −45.7/−31.8 (incumbent −34.7/−25.0);
                                  double tail fade 5.2/0.8 + 5.6/0.4.
                                  720×1280 24fps — NOT the requested native
                                  1080×1920; §9 ask #1 stays open, ceiling
                                  confirmed a 4th time. CHIP ZONE measured
                                  clean AT 0.0s (frame one, per MKT-35's
                                  no-fade chip) through 2.65s — seated
                                  mid-shot leaves the upper-right clear;
                                  better than the old helmet-close-up open.
                                  Incumbent + old master parked as
                                  *_incumbent_backup.mp4 (rollback). Opens
                                  on the visor igniting in darkness — the
                                  cold-audience hook — audio present from
                                  frame 1. The only intro cleared for a
                                  public surface; never swapped for a
                                  rotation member.
    anchor_intro_powerup.mp4      ✅ CLEARED 2026-07-30 — the third generation
                                  (redesigned: 3.0s dive) passed the eye
                                  check the first two failed. 6.3s (IN 0.0 /
                                  OUT 6.3 + 0.4s tail fade). One recorded
                                  overshoot: the device fully exits at ~5.4s
                                  against the redesign's 5.0s spec (left rail
                                  visible at 5.25) — ruled a PASS because
                                  every frame from 5.5s is edge-to-edge smoke
                                  and the dissolve bed (5.8-6.3) is clean
                                  with ~0.9s margin. Onset −35.6dB mean, the
                                  hottest open in the fleet. Serving in the
                                  rotation (the live count lives in §2 —
                                  nine as of v3.1; this entry stops
                                  hardcoding it).
    anchor_intro_*_master_10s.mp4 Delivered 10s masters, kept so the trims
                                  stay reproducible. NOT read by the
                                  pipeline.
    anchor_reference.png          Character reference still for the Anchor
                                  (regeneration/continuity — not composited).
    allday_pro_carrier.mp4        Pro VO carrier part 1 — the INCUMBENT, and
                                  the fallback the rotation lands on.
    allday_pro_carrier_stamp.mp4  MKT-20 rotation members (part 1 only):
    allday_pro_carrier_room.mp4   "board's up and it's stamped" / "the room
    allday_pro_carrier_overnight  where it lands first" / "engine ran all
                                  night". All 10.005s, all joined to the SAME
                                  allday_pro_carrier_pt2.mp4.
    allday_free_carrier.mp4       Free VO carrier part 1 — the INCUMBENT.
    allday_free_carrier_open.mp4  MKT-20 rotation members (part 1 only):
    allday_free_carrier_method    "nothin' held back" / "every one shows its
    allday_free_carrier_check     reasonin'" / "come back and check the work".
                                  All 10.005s, all joined to the SAME
                                  allday_free_carrier_pt2.mp4.
    allday_pro_endcard.mp4        Pro endcard (open frame + outro + audio)
    allday_free_endcard.mp4       Free endcard
    verif_carrier.mp4             Verify VO part 1 — 10.005s, ALL of it used.
                                  ⚠ MKT-59 (2026-08-19): now the INCUMBENT +
                                  fallback of a ROTATING set — see the two
                                  entries below and the held third.
    verif_carrier_record.mp4      ✅ MKT-59 members (tier-1 surface — SHARED
    verif_carrier_checked.mp4     with verify_public; enter the same window-
                                  resolved chain pt2 → signoff_short →
                                  silence, boundary UNMOVED because the walk
                                  keys on audio durations, identical across
                                  the set: full pt2 when body ≥ ~9.87s, short
                                  on a 1-box day). "Yesterday's board, back on
                                  the table. Every signal checked against
                                  what actually drew. Right or wrong, it goes
                                  on the record." / "We posted these before
                                  the draw. Here's how every one of them
                                  graded. Same board, no edits." Both
                                  10.000/10.005s; voice-ends 9.15 / 9.08
                                  (incumbent 9.27). Transcribed + linted at
                                  tiers 1/2/4: clean; no direct address, no
                                  time-of-day words, no counts/states. Seam
                                  f0 proxy vs pt2 open 103.2: Δ6.4 / Δ9.5,
                                  IQRs touching/overlapping — in band.
    verif_carrier_sameday.mp4     ✅ REGISTERED 2026-08-19 (MKT-62) — PINNED
                                  to `verify_midday`: set of ONE, single-
                                  part (rest []), NOT in CARRIER_KINDS, and
                                  must stay that way. "Now this is a rare
                                  one." / "This mornin's board — already
                                  checked against what drew." / "Same day.
                                  Not tomorrow." VO phrases 1.10–2.50 · 3.21–
                                  6.44 · 7.33–8.07 · 8.89–9.69 (last word) of
                                  a 10.005s preset. FALSE on a routine reel
                                  by construction — which is why verify's own
                                  carriers were made time-neutral. Tail: the
                                  last ~1–4s of the body's holds play under
                                  silence (verify's MKT-27/41 ruling) until
                                  the endcard audio — a sign-off continuation
                                  for this kind would close it.
    verif_carrier_ledger.mp4      ⏸ HELD (not registered; UNREFERENCED_OK
                                  carries the reason). "Here's the ledger
                                  from yesterday. Every signal we published
                                  checked in the open. Nothing added, nothing
                                  taken away." — tier-1 clean, 10.005s,
                                  voice-end 9.66. Seam proxy Δ12.3 with NON-
                                  overlapping IQR (87–93 vs 96–107): a
                                  falling sentence-final cadence, outside the
                                  accepted band; whole-file median (107.4)
                                  says same man. OPERATOR COLD LISTEN decides
                                  — one line registers it. Not a regen ask.
                                  The day's read: board, summary, first holds.
                                  ⚠ REPLACED 2026-08-01 (MKT-43) — this is a
                                  NEW recording, not the 7/29 one. Head lead
                                  kept at 10.005s deliberately: the signoff
                                  fit math depends on it. Serves BOTH verify
                                  and verify_public, so a defect here reaches
                                  a tier-1 surface.
    verif_carrier_pt2.mp4         ⚠ REPLACED 2026-08-01 (MKT-43) — **6.542s,
                                  NOT the 5.67s this entry read through v2.6.**
                                  Generated in the SAME session as the new
                                  part 1 to fix the seam where the two read as
                                  different men. EVERGREEN sign-off — plays
                                  unchanged every day, like All-Day's shared
                                  part 2. Covers the last hold and the close.
                                  Longer than its predecessor (slower read,
                                  wider inter-line gaps), so the join moved
                                  15.995s → 16.897s. ⛔ THE SEAM ITSELF IS
                                  UNVERIFIED — it is an ear gate, not a
                                  measurement, and it is still owed.
                                  ⚠ TRIMMED FROM ITS 10s MASTER. Delivered at
                                  the generator's fixed 10.005s with ~1.7s of
                                  leading digital silence and a decay tail; the
                                  seam breath would have become ~2s of dead
                                  air. Trimmed to speech, master kept as
                                  verif_carrier_signoff_master_10s.mp4 (same
                                  convention as the 10s anchor masters). If it
                                  is ever regenerated, trim it again — do not
                                  register a raw 10s master.
                                  ⚠ ITS NAME MAY NOT CONTAIN "_pt" ANYWHERE
                                  EXCEPT AS _pt<N>.mp4 — the master was first
                                  called *_pt2_master_10s.mp4 and preflight
                                  correctly FAILED it as invisible to both the
                                  joiner and the orphan scan.
    stinger_verify*.mp4           ✅ BUILT + ENABLED 2026-07-29. Five rotation
                                  variants plus the unversioned fallback.
                                  Verify's stinger ROTATES by reel date even
                                  though its intro does not.
    verif_carrier_signoff_short   ⚠ SLOT FILLED 2026-07-31 by the MKT-32
      .mp4                        addendum splice (this entry read "DECLARED
                                  SLOT, EMPTY" through v2.6 — stale, and it
                                  contradicted §3, which already said
                                  FILLED). 2.47s of audio. Sign-off-only
                                  variant of pt2, used when the day's window
                                  cannot take the now-16.897s full join —
                                  which is MORE often since MKT-43 lengthened
                                  pt2. ⛔ IT IS FROM THE OLD CARRIER SESSION
                                  FAMILY and has NOT been auditioned against
                                  the new pt1; if it reads as a different
                                  man, pull it from the resolver (degrading
                                  to part-1-alone) and regenerate it inside
                                  the new pair's session. ⚠ Deliberately NOT a
                                  `_ptN` name — that namespace belongs to the
                                  joiner and its orphan scan. Delivery: the
                                  usual 10.005s master, trimmed to speech,
                                  master kept (verif_carrier_pt2 convention).
                                  ≤2.3s of speech covers every day with a
                                  featured row; ~3.0s starts losing 1-box
                                  days.
    verif_endcard.mp4             Verify bolt endcard (unversioned last-resort
                                  fallback; refreshed by MKT-31 — the tagged
                                  matrix verif_endcard_<tag>.mp4 is what
                                  serves). ⛔ THE MKT-31 ADDENDUM-2 GEOMETRY
                                  RULING IS STRUCK — REVERSED 2026-08-01 BY
                                  MKT-45. Verify's endcard lockup is on the
                                  SHARED 1240, same as every other kind;
                                  there is no per-variant 880 and no
                                  880-1040 tuck zone. The outro still takes
                                  the endcard's FIRST 6.5s, so a NEW verify
                                  endcard motion must still (a) be
                                  bed-viable under text from 4.5s — but
                                  requirement (b), re-measuring the tuck
                                  zone, is RETIRED with the tuck. See v2.7
                                  notes A/B.
    midday_pro_carrier.mp4        Midday VO carrier part 1 — the INCUMBENT and
                                  fallback (+ shared _pt2, auto-joined).
    midday_pro_carrier_set        ✅ MKT-59 rotation members (2026-08-19, part
    midday_pro_carrier_ahead      1 only, all joined to the SAME pt2): "Midday
    midday_pro_carrier_ready      board's set, partners…" / "Midday's up,
                                  partners, well ahead of the draw…" / "Midday
                                  board's ready, partners…". All 10.000s v /
                                  10.005s a (file law); voice-ends 9.49 /
                                  9.62 / 9.53 (incumbent 9.71) → tails 0.51 /
                                  0.38 / 0.47s before the 0.35s breath; joined
                                  margin set-invariant +0.19s (= incumbent).
                                  Transcribed (faster-whisper) and linted
                                  tier 4: clean; no price; first-access
                                  framing present ("first look's yours" /
                                  "reading it first" / "ahead of every other
                                  room"); "Midday" spoken aloud (tripwire).
                                  Seam f0 proxy vs pt2 open 111.9: Δ2.7 /
                                  Δ8.0 / Δ9.3 — inside the MKT-48 band. No
                                  gap-tighten needed. Cold listen = operator.
    evening_pro_carrier.mp4       Evening VO carrier part 1 — the INCUMBENT
                                  and fallback (+ shared _pt2, auto-joined).
    evening_pro_carrier_landed    MKT-48 rotation members (part 1 only):
    evening_pro_carrier_dusk      "tonight's board just landed" / "engine's
    evening_pro_carrier_last      done its work" / "last board of the day".
                                  All 10.005s, all joined to the SAME
                                  evening_pro_carrier_pt2.mp4. Tail margins
                                  0.453/0.565/0.522s vs the +0.184s floor;
                                  seam f0 within ~3Hz of the incumbent's
                                  accepted delta. Ear confirmation on
                                  assembled reels stays owed (MKT-43 item-7
                                  convention).
    midday_free_carrier.mp4       MKT-26 — FREE-tier session VO (+ _pt2 each).
    evening_free_carrier.mp4      Scripted SEPARATELY from the Pro pair, not
                                  cut down from it: first-access framing is
                                  barred below Pro (SOCIAL-13), and these have
                                  to name the withholding instead. Part 1 is
                                  10.005s like the rest of the fleet, so the
                                  MKT-20 duration invariant holds. Margins:
                                  midday +1.242s (incumbent pt2 — its 7/30
                                  respec REJECTED at +0.114s, parked);
                                  evening +0.359s since MKT-32 (7/30 pt2
                                  REPLACED in place — close: "Tonight's the
                                  one that counts, partner."; old pt2 kept as
                                  *_part2_20260729_backup). ⚠ The free session
                                  pt2s' target frame is now the STRONGER
                                  live-and-running-out conversion framing
                                  ("cover comes off at sunup / Pro ain't
                                  waitin'") vs the flat gap statement they
                                  replaced — the allday_free pt2 respec
                                  ("Grade it against the mornin' receipts",
                                  REJECTED 7/30 at +0.088s, parked as the
                                  _redelivery file) stays PURE VALUE, no Pro
                                  pitch (SOCIAL-13). Floor for any respec:
                                  beat midday_pro's +0.184s.
    midday_pro_endcard.mp4        Midday endcard — built from the SHARED
    evening_pro_endcard.mp4       endcard_motion_pro.mp4 with different copy,
                                  i.e. zero new video generation (MKT-10's
                                  whole payoff). Same for their stingers.
    midday_free_endcard.mp4       MKT-26 — free session closes, built from
    evening_free_endcard.mp4      endcard_motion_free.mp4. Copy: "DIGITS
                                  COVERED HERE" / "FULL SIX IN PRO · $2.49/MO".
                                  ⚠ THE ONLY ENDCARDS CARRYING A PRICE. The
                                  free group is tier 2, the one tier brandLint
                                  §6 permits pricing on — never reuse these
                                  anywhere else. Also note "DIGITS", not
                                  "numbers": §5 bars numbers as a product noun.
    endcard_motion_pro_reveal.mp4 PRO-tier endcard motions — ⚠ REPLACED
    endcard_motion_pro_discharge  WHOLESALE 2026-08-01 (MKT-47). The MKT-19/23
      .mp4                        set of four (std converging threads · alt
    endcard_motion_pro_ignite.mp4 vault rings · steady scrolling waveform ·
    endcard_motion_pro_lock.mp4   lattice node lattice) is RETIRED — those
                                  concepts settled their content at y613-824,
                                  leaving 416-627px between form and lockup
                                  at the shared 1240 band. ⚠ ALL FOUR WERE
                                  REGENERATED FROM CORRECTED PROMPTS AND STILL
                                  LANDED AT 43-49% — the concepts settle high
                                  regardless of prompt. Do not propose
                                  reviving one; it was tried twice and both
                                  attempts are kept as *_incumbent_backup and
                                  *_regen_backup.
                                  The new set settles 50.3-52.5% against
                                  free's 50.4-52.0%, so the shared 1240 reads
                                  as the form's LANDING on every kind — the
                                  first time that is true. THE FIX WAS THE
                                  FOOTAGE, NOT THE GEOMETRY: the band did not
                                  move and ENDCARD_MOTION_LOCKUP stays empty.
                                  reveal = turbulence clearing · discharge =
                                  single strike · ignite = seed ignition ·
                                  lock = sealing ring. Used by All-Day Pro,
                                  Midday, Evening AND Verify.
                                  BED: discharge −27.5dB (−0.1dB correction,
                                  the best bedder in the system) · ignite
                                  −30.9dB · lock −30.8dB — all viable.
                                  ⚠ reveal is bed-INELIGIBLE at −43.2dB: it
                                  needs +15.6dB against the ±6dB clamp, WORSE
                                  than lattice's +13.5dB, so it must NOT be
                                  force-bedded (MKT-19 item E). It plays
                                  wall-to-wall and drops on hum-bed days.
                                  THREE bed-viable, up from two.
                                  ⚠ A fifth delivery, `anvil`, passed
                                  composition (50.3%) and was DROPPED — it
                                  dips to y1192, 48px above the lockup, and
                                  drifting smoke would close that gap on some
                                  frames and not others.
                                  Impacts land 3.55-3.90s, earlier than the
                                  old ~4.3s band; the assembler derives the
                                  bed window per endcard so this is handled.
    endcard_motion_free.mp4       FREE-tier endcard motions (MKT-19 set,
    endcard_motion_free_alt.mp4   widened 2→5 by MKT-48 2026-08-03).
    endcard_motion_free_tide.mp4  std = blooming eddies · alt = rising tide ·
    endcard_motion_sameday.mp4    ✅ REGISTERED 2026-08-19 (MKT-62) — PINNED
                                  (FIXED_ENDCARD_MOTION.verify_midday), in
                                  NEITHER tier pool; delivered as `endcard_
                                  motion_confirmed.mp4` and RENAMED by ruling
                                  ("confirmed" implies an outcome — the reel
                                  must never imply it only runs when the
                                  board landed). Bolt standing from frame 0;
                                  gold ring sweeps in and settles ~4.5s; bolt
                                  goes gold. Built `verify_midday_endcard_
                                  sameday.mp4` on the SHARED 1240 band (bolt
                                  bottoms ~y1100; band 1240–1500 smoke 13–16%
                                  >L40, max L153 — free-motion class; lockup
                                  block ends y=1400). Bed derived pre 0–3.4s
                                  @ −24dB (crack 3.8s) — recorded; verify
                                  kinds never bed.
    endcard_motion_free_bloom.mp4 tide = drifting tide · bloom = radial
                                  bloom · adopen = ad open (see its own
                                  entry below). Serves ALL free-tier kinds
                                  (session frees + publics + verify_public).
                                  All five bed-viable (tide +4.4dB, bloom
                                  post-crack, adopen +4.5dB).
                                  ⚠ tide's form settles ~39% vs the 50-53
                                  family band — registered as an atmospheric
                                  alternate with the deviation recorded. A
                                  third delivery (updraft) was REJECTED:
                                  breaches the 53 ceiling, particles drift
                                  into the text band (the anvil class), and
                                  bed-ineligible −9.9dB. Kept as
                                  *_rejected_20260803.mp4.
    endcard_motion_ad_open.mp4    ✅ REGISTERED (MKT-48, tag adopen) — the
                                  operator tier ruling landed 2026-08-03:
                                  (a) FREE SHARED, the 5th free motion.
                                  Gate-clean (42.6 stable settle, band_max
                                  112 cleanest of family, bed +4.5dB).
    stinger_motion.mp4            Text-free stinger motions (MKT-19 set),
    stinger_motion_strike.mp4     shared across ALL variants and both tiers —
    stinger_motion_circuit.mp4    a stinger carries no tier signal. std =
    stinger_motion_fracture.mp4   bolt condense · strike · circuit ·
    stinger_motion_etch.mp4       fracture (MKT-23) · etch · facet (MKT-58,
    stinger_motion_facet.mp4      2026-08-19). SIX serving.
                                  ✅ etch — "inscribed outline": 4.0s master,
                                  720×1280/24fps. Band y1300-1450 >L40:
                                  2% @1.3 (L22) · 0% @1.6 · 0% @2.0; 15% @2.2
                                  (smoke return under the text-out). Hero
                                  −3.5dB @1.4s (ramp — no 12dB prominence
                                  hit, like strike); late max −5.7 @2.4 on
                                  −14.5dB material; open −19.2dB. Smoke at
                                  0.0 (thinner than std: 87% blocks >L20,
                                  mean L49) and full from ~2.6s with a faint
                                  gold outline ghost to ~2.8. No authored
                                  fade. Build warnings: see MKT-58 entry.
                                  ✅ facet — "hazed planes": 10.0s master
                                  (0-3.0 used), 720×1280/24fps. Band 0% @1.3
                                  (L15) · 0% @1.6 · 4% @2.0 · 37% @2.2 (the
                                  smoke return arriving early, under the
                                  text-out — fleet-normal: std reads 44% at
                                  2.3). Hero −5.0dB @1.0s; a 12dB prominence
                                  hit at 2.4s (−4.0dB on −15.4dB material)
                                  = the smoke-return crest, early and sharp
                                  — NOT the circuit quiet-field class; the
                                  build flags it "uncovered transient at
                                  2.4s", ⚠ OPERATOR EAR OWED. No authored
                                  fade (it would silence the smoke return
                                  the beat belongs to). Open −11.8dB (loud
                                  open — no gate). Mark: glass bolt holds
                                  ~y300-1140, 1.1-2.2s; smoke refills 2.4-3.0.
                                  ⛔ shutter — REJECTED 2026-08-19, NOT
                                  registered (file kept on disk, unreferenced
                                  — the stray scan will name it until the
                                  regen replaces it at the same name). 10.0s
                                  master paced for 10s: smoke only to 2.2s,
                                  three WHITE BANDS 2.4-2.8s (full-frame
                                  L163-172 — and they read as letterforms,
                                  the exact risk the order named), bolt
                                  standing 3.0-5.0s, smoke back ~6s. Inside
                                  the 0-3.0s window: no hero beat at 1.0-1.4,
                                  a white flash where the dissolve source
                                  must be smoke, band 43% @1.3 / 29% @1.6 /
                                  25% @2.0 (smoke, not a mark). Audio: hero
                                  −9.1dB @1.0, −64dB silence at 1.8s, then
                                  a swell to −6.2 @2.9 on −29.3dB material =
                                  the attack-blind LISTEN class. No in-point
                                  trim satisfies both ends (pull the event
                                  to ~1.0 and 2.2-3.0 is bolt-on-dark, not
                                  smoke). REGENERATE AT THE 4s PRESET — the
                                  concept is fine, the pacing is the 10s
                                  preset's.
    stinger_motion_seal.mp4       ✅ SERVING — verify's motion (MKT-29 pin,
                                  MKT-31 item 5 clearance 2026-07-30),
                                  never in the shared set (a slate drop must
                                  never draw the bell). ⚠ v2.9: sole member
                                  of VERIFY_SEAL_MOTIONS — variants welcome,
                                  one line each (v2.9 note E). The 7/30 re-delivery
                                  measures clean: energy peak −17.4dB at
                                  1.10s (bound ~2.0s), smooth decay under
                                  the smoke return, nothing after 2.4s above
                                  −19.2dB; the 7/29 defect (bell 2.7s/−3.5dB
                                  over the VO onset) is gone. `held:` was
                                  cleared LAST, per the standing rule.
                                  ⚠ BINDING (tightened at clearance): seal
                                  REPLACES rotation for verify — a missing or
                                  defective build degrades to NO stinger via
                                  probeStinger's null, never to a slate
                                  motion; re-arming `held:` in
                                  brand-motion.ts returns verify to rotation
                                  (the five rotation verify stingers were
                                  rebuilt with the MKT-31 headline-dominant
                                  layout so a re-arm serves the new
                                  identity).
                                  ⚠ circuit carries a second transient (a
                                  gold pulse at 2.7s) that fires after the
                                  lockup is already out. It is corrected in
                                  the BUILD with an audio tail fade from
                                  2.45s, not by regeneration; the build
                                  re-derives the transient each run and warns
                                  if it moves or a new one appears.
    _motion_meta.json             DERIVED per-motion bed verdicts, written by
                                  `npm run endcard:build`. Not hand-edited —
                                  a regenerated motion clears its own verdict
                                  on the next build.
    stinger_<variant>_<tag>.mp4   Prebuilt stinger matrix: every stinger
                                  kind (9) × every shared motion tag (6) +
                                  verify/verify_public × seal. Generated by
                                  `npm run stinger:build` (rebuilt in full
                                  8/19 for MKT-58).
    <variant>_endcard_<tag>.mp4   Prebuilt endcard matrix: every variant ×
                                  every motion in ITS TIER. Generated by
                                  `npm run endcard:build`. The untagged
                                  `<variant>_endcard.mp4` files remain as the
                                  resolver's last-resort fallback.
    panels/panel_*.png            Rotating promo panels — SEVEN as of MKT-60
                                  (2026-08-19); copy read from pixels, with
                                  the sha256 clearance pins in panel-config:
                                  brand    "HITMASTER ZK6" (7/27)
                                  coverage "EVERY DRAW IN THE COUNTRY / 39
                                           TRACKED FEEDS · 40+ STATES &
                                           PROVINCES" (7/27)
                                  anchor   "THE DATA DESK / HITMASTER ZK6"
                                           (7/27)
                                  app      "SIX SIGNALS. EVERY MORNING. /
                                           RANKED · EXPLAINED · VERIFIED" —
                                           ART REGENERATED 8/19 (ring-bolt +
                                           light waves; was the bars art of
                                           7/29), IDENTICAL copy; sha
                                           d0dfc723…; band y294–732 of
                                           1536×1024, 3.50:1 → 1080×309.
                                           ⚠ FILENAME KEPT BY RULING — the
                                           slot name is HISTORICAL (it once
                                           read "THE APP IS COMING"); do not
                                           infer content from "app".
                                  zk30     "HITMASTER ZK30 / SINGLE-STATE
                                           ENGINE · COMING SOON" (7/27)
                                  receipts "PUBLISHED BEFORE THE DRAW. /
                                           GRADED AFTER · IN THE OPEN" — NEW
                                           8/19; plain gold ring round the
                                           bolt (no marks, 2x); sha 745ce761…;
                                           band y293–730, 3.51:1 → 1080×308.
                                  measures "ENERGY · MOMENTUM / PATTERN ·
                                           CONSISTENCY / FOUR MEASURES BEHIND
                                           EVERY SIGNAL" — NEW 8/19; three
                                           lines, the third reads at the
                                           ~1000px in-modal width; four gold
                                           rules = equal-length plain marks,
                                           not a chart (2x); sha cc518b0d…;
                                           band y285–735, 3.41:1 → 1080×317.
                                  RETIRED 8/19: panel_signals ("FOUR SIGNALS.
                                  ONE VERDICT. / FREQ · MOMO · PATTERN ·
                                  CONSIST") — replaced by measures (same four
                                  components in the DESIGN-03 vocabulary);
                                  source moved to panels/retired/, bucket
                                  object deleted. Rotation at 7: six distinct
                                  per day, no gap, no double-draw (verified
                                  8/19–8/26). ~~Rotating promo panels (6): brand, signals,~~
                                  coverage, anchor, app, zk30. Source art;
                                  panels/built/ is generated, not source.

  PANEL DELIVERY SPEC (assets/marketing/panels/):
  - ~3:2 PNG whose CENTRED horizontal band is the artwork. The band is
    detected and cropped automatically, so the band's own aspect is free
    (delivered set ranges 2.97:1 to 4.40:1) — each panel keeps its native
    height; the app lays it out full-width and derives the height.
  - Anything below the band (e.g. a generator watermark) is discarded.
  - Backing colour does NOT need to match the app: every panel is
    feathered on all four edges, which is what makes full-bleed artwork
    sit natively. Both black-backed and full-bleed purple panels work.
  - ⚠ DUAL-CONTEXT BY DEFAULT (standing rule, 2026-07-28). Every panel is now
    read BOTH by a reel viewer who does not have the app AND by a paying
    subscriber inside it. Write every panel's copy so it is true in both
    rooms. If a panel is ever capture-only again that will be stated
    explicitly; absent that, assume dual-context.
    THE LIVE EXAMPLE: panel_app.png says "THE APP IS COMING / APP STORE ·
    GOOGLE PLAY" — correct for a reel, but it tells a subscriber reading it
    inside the product that the product does not exist. It clears the brand
    lint (no forbidden vocabulary), which is the point: this class of failure
    is invisible to every automated check we have. Approved replacement,
    pending regenerated artwork: "SIX SIGNALS. EVERY MORNING." /
    "RANKED · EXPLAINED · VERIFIED". Not urgent, not blocking.
    Note panel copy is BAKED PIXELS, unlike endcard and stinger copy — MKT-10
    made those config strings, panels were never converted. So a wording fix
    here costs a regeneration, and it is worth getting right the first time.
  - TIER-NEUTRAL, no exceptions: no pricing, no upgrade CTA, no Pro pitch.
    TWO independent reasons now — (1) one body render feeds both the Pro and
    Free reels, and (2) since 2026-07-28 the panels are APP-WIDE, so a paying
    subscriber sees them inside the product. A "subscribe" pitch shown to
    someone who already subscribes is the failure this rule prevents.
  - Artwork is served to the app from the public `app-panels` bucket (not
    bundled), so a swap reaches users without an app release — and equally,
    a panel that is built locally but not republished is invisible to
    everyone. `reel:check` compares the served bytes to the local build.
  - Copy is cleared MANUALLY and pinned to the file's sha256. Changing a
    panel's artwork voids its clearance and `reel:check` fails until it is
    re-reviewed and re-pinned (npm run panel:build -- --print-hashes).
  - After a drop-in: `npm run panel:build`, then `npm run reel:check`.
  - The set is expected to grow past six; rotation adapts with no code
    change. A missing panel is dropped from the day's rotation with a
    warning — it never blocks a run.
  Replacing any of these with a spec-compliant upgrade requires zero code
  changes — drop-in by filename, subject to the filename rule below.
  (v1.5 listed "longer ~19.4s All-Day carriers" as the standing request. That
  is DONE — every slate carrier now ships as a joined ~20.4s pair and runs
  wall-to-wall with nothing discarded. The current standing asks are in §9.)

  MULTI-PART CARRIERS (when the generator caps clips at ~10s):
  Deliver a long carrier as numbered parts and the pipeline joins them
  automatically, in order, before anything else looks at the audio:

      allday_pro_carrier.mp4  +  allday_pro_carrier_pt2.mp4  [+ _pt3 …]

  Works for EVERY carrier (all-day pro/free, midday, evening, verify). Audio
  only is joined — the carriers' video is discarded by design.

  ⚠ CHANGED 2026-07-29 (MKT-20) — THE PAIRING IS NO LONGER DERIVED FROM THE
  FILENAME. It used to be: the joiner read part 1 and went hunting for
  "<that name>_pt2.mp4". It is now declared per kind in
  scripts/carrier-config.ts, because rotating part 1 breaks derivation — an
  alternate opening would send it looking for a part 2 that will never exist.
  What that meant in practice is worth knowing, because it is why the rule
  changed rather than being patched: the unpaired carrier would resolve at
  ~10s, fall below the overlap threshold, silently switch the reel to hum-bed
  mode, and PUBLISH a reel with roughly half its narration replaced by hum.
  Nothing would error and preflight would pass.
  For you this is simpler, not harder: an alternate part 1 can be named
  anything, and it does not need its own part 2. What you must state on
  delivery is which KIND and TIER it belongs to.

  Rules:
  - A 0.35s breath is inserted automatically at each part boundary, on
    top of whatever silence the parts already carry. You do NOT need to
    pad the end of part 1 or the start of part 2 — start part 2's first
    word promptly and the joiner supplies the pause. (The 7/27 delivery
    started part 2 at 0.2s, which left only a 0.4s gap after part 1's
    last word and read as crowded at this voice's pace.)
  - **The ceiling applies to the JOIN, not to each part.**
    ⚠ CORRECTED 2026-07-28 — v1.5 said "part 2's final word must land by
    ~7.0s of part 2 (10.0 + 7.0 = the 20.0s carrier ceiling)". That is
    arithmetically wrong: 10.0 + 7.0 is 17.0, and it cost you ~2.6s of
    usable narration. Measured against the code:

        part 1                 10.005s   (generator's 10s preset)
        + breath                0.350s   (inserted automatically)
        + part 2               10.005s
        = joined file          20.360s
        assembler fade begins  20.010s
        so the LAST WORD must land by ~9.65s OF PART 2.

    In other words a ~10s part 2 may use essentially its full length.
    The 20.0s figure is the target; 20.5s is the hard fade. Preflight
    measures the JOIN, so an overrun is caught before assembly.

  - ⚠ THAT EXTRA ROOM IS NOT SPENDABLE. The correction is real against
    v1.5's arithmetic but NOT against what the generator delivers: this
    voice runs ~35% longer than scripted timings. public_carrier_pt2's
    last word was written for 6.9s and came back at 9.32s, so against the
    corrected ~9.65s ceiling the delivered pair has ~0.33s of ACTUAL and
    ~0.25s of SCRIPTED headroom. The public carrier is effectively FULL.
    Do not plan a longer CTA on the strength of the corrected number.
    Practical rule (content agent, 2026-07-28): script carrier VO to about
    70% of whatever the real ceiling is, and treat any scripted timing as
    advisory until it comes back measured.
  - HOW MUCH ROOM IS ACTUALLY LEFT: on the current public pair the last
    word ends at 19.694s of the join, 0.32s before the fade. That margin
    is set by the CARRIER, not the body — a LONGER body does not consume
    it. The risk runs the other way: if a body render comes in SHORTER
    than ~18.4s the fade moves earlier and the margin inverts. Safe band
    for the delivered pair is a body of 18.4s–20.0s (today: 19.0s). If a
    future capture leaves that band the fix is a shorter part 2, never a
    trimmed audio tail.
  - A continuation the config does not declare is a hard FAIL, not a silent
    skip. Under derivation this was "a _pt3 without a _pt2"; under explicit
    pairing it is any `<part1>_ptN.mp4` on disk that carrier-config.ts does
    not list. Either way the reason is the same — dropping it would leave a
    hole in the narration — and either way it blocks the run rather than
    shipping quietly.
  - ⚠ EVERY PART 1 IN A ROTATION SET MUST BE THE SAME LENGTH (MKT-20).
    Part 2's offset in the join is part 1's FILE duration, not its voice-end,
    so a variant delivered at 9.2s or 11s moves the ceiling on the mornings
    it plays and looks perfect on the others. Preflight FAILS on a mismatch,
    measured against the incumbent rather than a hardcoded 10.005s.
  - ⚠ THE 0.35s BREATH IS NOT THE AUDIBLE PAUSE. It is what the joiner
    INSERTS; a listener hears 0.35s plus whatever tail part 1 already carries.
    Across the current All-Day set that lands between 0.586s and 0.777s. Both
    extremes are bracketed by carriers that already ship, so nothing needs
    fixing — but do not script a hand-off assuming an exact 0.35s gap.
  - The join is generated into assets/marketing/_carrier_joined/ and is
    rebuilt whenever a part changes. It is not source; don't edit it.

  ANCHOR INTRO DELIVERY SPEC (now a SET, not one file — see §2):
  - ⚠ EXACT FILENAME INCLUDES THE EXTENSION, lowercase `.mp4`. The 8/13 drop
    landed as `.mov` — a wrong container extension is invisible to the
    registry exactly as the `.MP4`-uppercase incident was: nothing errors,
    the pool silently stays at its old size until someone notices. If a
    delivery arrives as .mov, PROBE FIRST — when the streams are already
    h264/aac the remux is a lossless `-c copy`; anything else is a real
    transcode and a quality question that must be reported, not absorbed.
  - ⚠ EXPECT TO BE TRIMMED, AND SAY SO IN THE DELIVERY NOTE. The generator
    only emits at 4/6/8/10s presets, so "5.5s" rounds to 10. Every intro
    delivered to date — the 7/27 original, deadpan, powerup and public —
    arrived at 10.005s against a 6.5s ceiling. This is now the expected
    path, not an incident. Masters are preserved as *_master_10s.mp4 so
    the trim stays reproducible; they are NOT read by the pipeline.
    Do NOT state a duration you have not measured: v1.5-era deliveries were
    described as "5.5s, NO TRIM required" while measuring 10.005s, and on a
    rotating set that is worse than it sounds — a rejected member drops
    silently and the set collapses toward one intro while appearing to work.
  - THE TRIM RULE (apply it yourself if you can): find where smoke fills the
    frame, set the out-point 0.6s later, set the in-point 6.0s earlier, and
    slide the window if it runs past either end.
  - ⚠ REFINED 2026-07-29: THE IN-POINT FOLLOWS THE AUDIO, NOT THE PICTURE.
    `arrival`'s master hard-cuts from a static desk to the Anchor at 1.667s,
    but its audio does not start until 2.0s. Cutting on the picture cut —
    the obvious choice, and what the rule literally says — would have opened
    the reel on 0.33s of digital silence, which is the exact autoplay-
    retention failure the head trim exists to prevent. So: find the picture
    cut AND the audio onset, and take the LATER of the two. Check this every
    time; two of the five intros to date have had a silent head, and it is
    invisible unless you measure it (a waveform look is enough).
  - THE TWO TRIMS DONE ON 2026-07-29 WERE DIFFERENT (IN 2.0 vs IN 0.0) from
    the same 10.005s delivery shape. Do not read any previous trim's numbers
    as the spec — they are a result, not a setting.
  - Duration 3.5–6.5s, hard-enforced. Anything outside the window is
    REJECTED by preflight; that member drops from the day's rotation.
  - EVERY member of the set must satisfy this whole spec. Preflight now
    validates all of them, not just the standard file, and a defective
    member is a hard FAIL rather than a silent drop.
  - NOT EVERY CLIP CAN BE TRIMMED INTO COMPLIANCE — and this is the one
    failure mode the numbers do not catch. `anchor_intro_powerup` was
    rejected outright. Swept frame-by-frame across every candidate
    out-point from 5.0s to 10.0s: the smoke is ALWAYS inside the phone,
    with the bezel and notch still in shot, and at ~7.0s the clip hard-cuts
    to a wide newsroom. There is therefore no window anywhere in the file
    that ends on a usable dissolve bed — it is a REGENERATION, not a trim,
    and no choice of in/out point rescues it.
    ⚠ It PASSED the luma-spread check (105 at out 6.0s, inside the ≤120
    band) because that check measures UNIFORMITY, and smoke on a phone
    screen is just as uniform as smoke filling the frame. Dissolving from
    it would have ghosted a handset outline through the UI body. A second
    check now flags a dark border (edge-vs-centre brightness: powerup 41%
    vs standard 117% / public 90% / deadpan 75%) — but it is a WARN asking
    for an eye check, not a substitute for one.
    WHAT TO GENERATE INSTEAD: the camera must push FULLY into the screen so
    the handset leaves frame entirely, as public and deadpan both do at
    4→5s. The delivered ending must be smoke edge to edge with no device
    visible anywhere in the frame.
  - Must END on full-frame smoke. The dissolve reads the final ~0.5s as its
    bed. The 10s master pulled the camera back OUT to the newsroom over its
    last 2.5s — a beautiful shot that is unusable as an ending, and the
    reason the trim exists. Do not "complete" the move; end inside the smoke.
  - Must OPEN on the gesture, with sound. The 10s master spent its first
    ~1.9s on a static Anchor over silence; that head was cut because a reel
    opening on a still frame and no audio bleeds autoplay retention. Start
    on motion, and have the audio up from frame 1.
  - Embedded audio REQUIRED — it is the only sound before the VO enters.
    END it at true silence (the assembler's dissolve fade assumes that); it
    may start hot.
  - Must survive a 1:1 CENTRE CROP (keeps y 420–1500 of 1920). ⚠ The band
    REMAINS BINDING even though the 1:1 output cut was retired (MKT-39
    addendum): the identity chip and every composition clearance are
    measured inside it, and a future 1:1 resurrect must not trigger a
    re-delivery of the whole fleet. Originally specified for the
    square cutdown. Keep the visor, the gesture and the phone inside that
    band; the top of the head may clip.
  - Same resolution/frame-rate spec as everything else (native 1080×1920,
    30–60fps). The current file is 720×1280 24fps and gets upscaled.

  BRAND MOTION DELIVERY SPEC — stinger + endcard motions (MKT-19, new in v1.7):
  - MOTIONS ARE TEXT-FREE. Never bake a wordmark, headline or date into one.
    Copy is rendered fresh at 1080×1920 over the motion at build time, which
    is what makes a headline change a string edit instead of a regeneration.
    A motion arriving with type in it cannot be used at all.
  - EXPECT 10.005s, as with the intros — the generator's 4/6/8/10s presets.
    That is FINE here and needs no trim: endcards contract for 10s, and a
    stinger uses only its first 3.0s. But it means the beat times you were
    briefed for will not be the beat times delivered, so state what you
    MEASURED, never what you intended. Every MKT-19 delivery was described
    as 4.0s/10.0s and every one arrived at 10.005s.
  - ENDCARD MOTIONS: the crack (the bolt snap) should land ~4.3s, and the
    clip must END fully static — the final frame is reused as the reel's
    opening frame. The delivered set lands 4.2–4.5s, which is fine; the
    assembler measures each one rather than assuming.
  - ENDCARD MOTIONS NEED A LEVEL-STEADY STRETCH BEFORE THE CRACK. On a
    short-carrier day the pre-crack audio is looped as a palindrome to bed
    the modals, so the window must not swing more than ~20 dB.
    ⚠ THE SPEC IS ABOUT AUDIO SHAPE ONLY (2026-07-29). `pro_alt` failed on
    its SOUND, not its visuals — "rings align and release" is a fine thing
    to look at. So the visual concept is free to be as interesting as you
    like, provided the audio holds roughly CONSTANT from 0 to ~4.0s and
    then cracks. State the intended audio shape in the delivery note; it is
    the half that decides whether a motion can bed, and it is invisible in
    a preview. A build-up
    concept — "rings align and release", anything that swells continuously
    into the release — has no such stretch BY CONSTRUCTION and will be
    dropped from short-carrier days. That is handled gracefully, not an
    abort, so a build-up is an acceptable delivery; just know it buys a
    motion that only plays on some days. Two build-ups in the same tier
    would be a hard preflight FAIL.
  - STINGER MOTIONS: must open on full-frame smoke and present a usable
    dissolve bed at 3.0s (it is crossfaded into the body). ONE transient
    only, and it must land before ~2.2s. A second beat after the lockup
    has faded out fires on an empty frame and reads as a pop — this is the
    `circuit` defect, corrected in the build with an audio fade rather than
    a regeneration, but do not rely on that: a motion whose hero beat is
    late, or which pulses twice, is fighting the timeline.
  - TIER MATTERS FOR ENDCARDS ONLY. Say which tier a delivered endcard
    motion is for. Pro motions read as vault/precision/institutional; free
    motions as open/tidal/inviting. They can never cross — the code will
    not allow it — so a motion delivered to the wrong tier is simply the
    wrong asset.
  - Embedded audio REQUIRED on both kinds; it is load-bearing (the stinger
    impact, the endcard crack and its bed).
  - Same resolution/frame-rate spec as everything else below.

  ⚠ FILENAME RULE — NAME THE DESTINATION, UNAMBIGUOUSLY (adopted 2026-07-29).
  A delivered file's name must say which SURFACE it is for, using the word this
  handoff already uses for that surface. MKT-21's four bodies were named
  `group_body_*` but meant CROSS-POST; "group" means the free/Pro rooms (tier
  2/4) everywhere else in this document, so the names said the opposite of what
  they meant and the assets were assessed against Pro-room rules for two full
  rounds before the mismatch surfaced.
  This is the FOURTH filename failure in this lane and the first of its kind.
  MKT-06's 2-byte web rename and MKT-16's `_pt_` were MANGLED names; MKT-19's
  `verif_endcard` was a NON-UNIFORM name. This one was well-formed, correctly
  spelled, and simply meant something else in our own vocabulary — so no
  automated check can ever catch it. Only the convention can.
  Use: public · cross · free · pro · verify. Not: group, room, tier, social.
  If a file serves two surfaces, name the requirement they share, not one of
  them — cross-post and public are one requirement, so `public_*` covers both.

  DELIVERY SPEC (every replacement file):
  - Resolution: native 1080×1920 (9:16). Current endcards are 720×1280
    and get upscaled — it works, but costs sharpness. This is the #1
    quality upgrade available.
  - Frame rate: 30-60fps (24fps gets frame-duplicated to 60; slightly
    steppy motion on the energy effects).
  - All-Day carriers: VO must FINISH (hit silence) by ~20.0s of carrier —
    the assembler hard-fades at 20.5s and anything still talking gets cut.
    (History: 17.8/18.3 originally → tightened to 17.0/17.5 when the
    Anchor intro pushed the VO start later → widened to 20.0/20.5 on
    2026-07-27 when the modal hold went 2.0→2.5s. The window was widened
    to fit the narration rather than cutting it a third time; both
    delivered carriers now pass wall-to-wall with nothing discarded.)
    Content after the VO is discarded by design.
  - Endcards: keep audio in the file, front-loaded, crack synced to the
    visual snap; final frame = settled lockup (it opens the reel).
  - After any swap, engineering runs `npm run reel:check` — a preflight
    that verifies all of the above mechanically (corrupt files, missing
    audio, durations, VO-chop risk, weak crack, resolution) before
    anything is assembled. Deliver to pass it clean.

  THE DATE CHIP IS NOW VERIFIED AGAINST THE FOOTAGE (MKT-18, 2026-07-28).
  Previous versions of this doc said the stamp "can never disagree with the
  on-screen data". That was an assumption, not a check: the assembler took the
  date from its argument, confirmed a body file with that name EXISTED, and
  burned the date over whatever those pixels were. Copy a body to another
  day's filename and you got a clean, correct-looking reel of the wrong day's
  board — which is exactly what happened on 7/28 while testing the intro
  rotation. Each renderer now writes the captured date INSIDE the body file
  and both assemblers read it back: a mismatch aborts, naming both dates.
  Nothing changes for you — no new asset, no new step. It does mean a reel can
  no longer be re-stamped to a different day by renaming its body, so if you
  ever want a past day rebuilt, ask for a re-render rather than a rename.
  STATUS: every CURRENT body carries its tag (all four re-rendered 2026-07-28
  evening and verified: allday/midday/evening 2026-07-28, verify 2026-07-27,
  each matching its filename). Bodies from earlier days still carry nothing —
  re-assembling one of those prints a note and proceeds exactly as before,
  which is the intended floor, not a defect.

  FILENAMES ARE LOAD-BEARING — `reel:check` now scans for near-misses.
  Every asset is read by EXACT filename from a registry. A name that is
  almost right is the worst case in this system, because nothing errors:
  the registry finds nothing, the previous week's file stays in place, and
  the reel builds and publishes looking correct. It has happened twice —
  a 2-byte endcard from a GitHub web rename (MKT-06) and
  `public_carrier_pt_.mp4`, which matched neither the joiner's `_ptN`
  pattern nor the orphan scan that exists to catch exactly that (MKT-16).
  Two guards now close the class:
  - a `_pt` name that is not `_pt<N>.mp4` is a hard FAIL;
  - a STRAY SCAN enumerates every filename the registries can produce and
    reports anything on disk that no registry reads. That covers all four
    near-miss shapes — endcard, stinger, carrier, intro — plus panel source
    art, and it closes the one genuinely silent hole: if BOTH the carrier
    and endcard of a kind were misnamed, the kind used to report a healthy
    "dormant, no assets delivered" and say nothing else.
    A stray is a WARN, not a FAIL — it breaks nothing today, it just means
    something you delivered is not reaching a reel. Deliberate exceptions
    (the rejected powerup intro, the not-yet-registered public carrier) are
    listed WITH their reason so the exemption stays visible.
  Practical upshot: if you deliver an asset and the next `reel:check` names
  it as unreferenced, the filename is wrong — not the file.

  Outputs land in assets/marketing/allday_reels/, midday_reels/,
  evening_reels/ and verify_reels/, and are pushed to GitHub after every run.


--------------------------------------------------------------------------------
8. ENHANCEMENT LANES (where new work plugs in cleanly)
--------------------------------------------------------------------------------
  - New/longer carriers and endcards: file drop-in, spec in §2/§3.
  - New caption templates: text delivery, spec in §5.
  - v2.9 — ROTATION MEMBERS, all one-line registrations (see v2.9 notes E/F
    for what each buys and the exact asks): slate intro → INTRO_ROTATION ·
    stinger motion → STINGER_MOTIONS (ask: 4 → 7) · free/pro endcard motion
    → ENDCARD_MOTIONS (free is adequate at 5) · carrier part-1 variant →
    the kind's CARRIERS set (midday/evening_free/public are single-entry) ·
    seal variant → VERIFY_SEAL_MOTIONS · public intro → PUBLIC_INTROS
    (clearance FIRST). After any drop: npm run reel:check validates it,
    npm run reel:rotation shows it entering the schedule. ⚠ Spread-axis
    lists follow LIVE-KINDS-FIRST (v2.9 note D) — never insert a dormant
    kind ahead of a live one.
  - A new SCOPE/variant of the existing slate reel is nearly free: an endcard
    config entry, a stinger config entry, a caption kind, a scope registry
    entry — plus TWO things that fail late if forgotten: the database kind
    CHECK constraint (publish fails at upsert AFTER a successful render and
    upload) and the admin Reels KIND_UI entry (an unknown kind used to crash
    the whole Reels tab; there is a fallback now, but add the entry).
    Endcards and stingers for a new session cost NO new video generation —
    they reuse the shared motion files with different copy.
  - A genuinely new reel FORMAT still needs (a) a render/assemble script
    pair, (b) a caption registry entry, (c) the kind additions above — the
    storage/app/posting chain then works unchanged.
  - Do NOT propose: auto-posting, public-page reel lanes, captions that
    move numbers outside pro, bypassing the lint/Two-Question gates, or a
    full-fidelity FREE Midday/Evening reel (see §9 — it would defeat the
    redaction rule that makes the sessions a Pro benefit).


--------------------------------------------------------------------------------
9. WHERE THE SYSTEM STANDS (2026-08-19, resynced by v3.5 — the top-of-file
   STATE SNAPSHOT is the short form; this section is the closure record)
--------------------------------------------------------------------------------
BUILT AND RUNNING DAILY:
  MKT-01  UI ad-clip render rig + 30s ad assembly
  MKT-02  Verify / "Receipts" reel pipeline
  MKT-03  All-Day dual reels (Pro + Free)
  MKT-04  In-app publishing → storage → Admin → Reels handoff
  MKT-05  Data-driven caption engine (+05b: verify carries free & pro drafts)
  MKT-06  Preflight (npm run reel:check) + daily-post QC audit
  MKT-07  Slate stamp — burned-in day · scope · purpose chip on every cut
  MKT-08  "The Anchor" branded intro (superseded by MKT-17 — see §2)
  MKT-09  Multi-part carrier VOs (_pt2 …) auto-joined with a 0.35s breath
  MKT-10  Endcards: motion and text split — COPY IS NO LONGER AN ASSET
  MKT-11  Rotating promo panels — now APP-WIDE (see §2 warning)
  MKT-12  Branded stinger between the intro and the UI body
  MKT-13  Session wave — Midday + Evening reels (2026-07-28)
  MKT-17  Per-kind intro resolution + rotation (2026-07-28) — see §2
  MKT-18  Body provenance — the capture date travels inside the file
  MKT-19  Brand motion rotation: stinger + endcard (2026-07-29) — see §2
  MKT-20  Carrier rotation — All-Day part 1, pro + free (2026-07-29) — §2, §7
  MKT-22  Intro identity chip (2026-07-29) — §2
  MKT-23  Asset wave: 6 intros, 5 stinger + 4 endcard motions, per-kind copy
  MKT-25  Verify restaging — the board it posted, then the ledger (2026-07-29)
  MKT-26  FREE-GROUP SESSION REELS (2026-07-29) — redacted Midday/Evening drops
          for the free group. Supersedes the pro-only ruling; see below and §2.
  MKT-27  VERIFY READABLE HOLDS + PT2 + STINGER (2026-07-29) — the verify body
          stops being a fixed 6.3s and becomes 8.6-14.0s built from the row
          selection, so a matched row can actually be read. Straights hold
          longest. Retires the 14.4s total and the flat 9.2s carrier need.
          verif_carrier_pt2 (5.67s as delivered by MKT-27; REPLACED at 6.542s
          by MKT-43 on 8/01 — see §7) cut the resulting silence
          from 6.5s to ~0.5s, and the stinger was unblocked and enabled once the
          branding ratio fell from 64% to 44%. See §3, §7, §9.
  MKT-24  TELEGRAM IS THE PRO ROOM (2026-07-29) — tier-4 captions are now
          RESHAPED (pricing / upgrade / pinned-post / CTA sentences dropped,
          plainer register) and free-written drafts are REFUSED outright rather
          than transformed. Real performance data survives untouched; it is
          sanctioned at tier 4. Affects nothing you write for Facebook.
  MKT-28  HEAT COPY DECISIONS CLOSED (2026-07-30) — canonical HOT wins on the
          detail surfaces; Heat Check keeps verdict wording on renested
          thresholds (80/65/45). ✅ FULLY CLOSED same day (v2.1 note D): the
          11 neutral slots were DELIVERED and landed verbatim in
          HEAT_RELABEL; the shipped table is in the MKT-28 audit entry.
          Nothing remains owed.
  MKT-29  VERIFY STINGER PINNED TO SEAL (2026-07-30) — ✅ RESOLVED: the hold
          CLEARED on the 7/30 re-delivery (v2.3 note B); seal is verify's
          FIXED motion REPLACING rotation. §7 has the measurements.
  MKT-30  PUBLIC CAPTURE MODE BUILT (2026-07-30) — `--redact --relabel`
          renders the tier-1-clean public body with the bolt look, audited
          per modal by the real lint engine. ✅ REGISTERED same day: MKT-16's
          five points landed 7/30 (v2.1 note D); allday_public is LIVE.
  MKT-31  VERIFY IDENTITY PACKAGE (2026-07-30) ✅ SHIPPED + 2 ADDENDA,
          operator-accepted — headline-dominant stinger, persistent gold
          date ribbon, gold accents, seal cleared, FIRST-6.5s endcard
          window. v2.3 notes A-C; audit entry has the full detail. Was
          absent from this doc until v2.3. ⛔ ITS lockupTop 880 RULING IS
          REVERSED by MKT-45 (2026-08-01) — everything else in the package
          stands.
  MKT-35  CLASSIFIER + RESYNC + DATED CHIP + RIBBON (2026-07-31) ✅ SHIPPED
          IN FULL — board straight treatment (v2.3 note E: data layer was
          CORRECT, the board renderer was the defect; no backfill existed),
          the v2.3 doc resync, the frame-one dated identity chip, the
          per-segment ribbon, slot 9 LONG GAP. v2.4 note A.
  MKT-36  anchor_intro_public REPLACED (2026-07-31) ✅ — regeneration won
          the comparative gate and serves; two more binaries eaten by
          GitHub-web renames on the way in (total 6). v2.4 note B; §7 has
          the measurements. Still 720×1280 (ask #1 open).
  MKT-37  YOUTUBE SET + TEMPLATES 8→12 (2026-07-31) ✅ SHIPPED — YT titles/
          descriptions/tags as a per-platform transform; caption pools
          grown to 12 everywhere (verify-free's #9 replacement arrived
          later same day); handoff kind guard. v2.4 note C.
  MKT-38  KEEP-CURRENT-ONLY RETENTION (2026-07-31) ✅ — supersession per
          kind on publish; 674MB local + 20 bucket rows retro-freed.
          v2.5 note A; §4 has the rule.
  MKT-39  COST AUDIT (2026-07-31) 📊 + ADDENDUM — numbers only, then the
          operator retired the 1:1 cut (zero logged use; 37% of bucket).
          Reports in repo root. The crop BAND stays binding (§ delivery
          spec).
  MKT-40  verify_public (2026-07-31) ✅ BUILT & LIVE same day as its
          Phase 0 — the masked public grading half; two public reels/day.
          v2.6 notes A-D; audit entry has the full build detail.
  MKT-41  VERIFY STINGER/ENDCARD PLACEMENT + MISSING ENDCARD AUDIO
          (2026-07-31) — MIXED OUTCOME. Item 3 (the endcard close was
          SILENT — the verify assembler mixed 3 inputs where the slate
          assembler mixes 4, so up to 4.5s of dead air) ✅ FIXED AND
          STANDS. Item 2 (withdrawing verify's 880 endcard tuck to the
          shared band) ✅ VINDICATED — MKT-44 briefly overrode it and
          reproduced the exact defect item 2 named. Item 1 (stinger
          lockupTop 820) ⛔ REVERSED by MKT-45.
  MKT-42  STINGER PER-MOTION LOCKUP + STRAND GATE (2026-07-31) ⛔ REVERSED
          by MKT-45 next morning. Its durable findings survive: lockup
          behaviour is a property of the MOTION, not the reel kind
          (circuit measured identically across three kinds); `imprint`
          stays RETIRED for a TIMING defect, not placement (its bloom is
          full-frame through the fade-in) — do not restore it assuming
          the clip was bad.
  MKT-43  VERIF_CARRIER SAME-SESSION PAIR (2026-08-01) ✅ SHIPPED ON
          MEASURABLE GATES — new pt1+pt2 generated as one designed pair to
          fix the seam where they read as different men. Transcript-clean,
          no time-of-day words in pt1 by design. ⚠ TWO EAR GATES STILL
          OWED (the seam itself; whether signoff_short still matches). See
          v2.7 note F for the spoken lines and the new durations.
  MKT-44  ENDCARD PER-MOTION LOCKUP (2026-08-01) ⛔ REVERSED by MKT-45 the
          same day. Durable finding worth keeping: endcard motion TAGS
          REPEAT ACROSS TIERS (`std` is a different file for pro and
          free), so any endcard-motion map must key on the FILE, never the
          tag — tag-keying silently applies a pro measurement to a free
          motion.
  MKT-45  THE TUCKS REVERSED (2026-08-01) ✅ SHIPPED — frame balance beats
          distance-to-mark; all kinds back on the shared bands; both
          override maps kept EMPTY with do-not-re-add warnings; the 380px
          strand gate DELETED rather than retuned. 45 stingers + 16
          endcards + 7 reels rebuilt. v2.7 notes A-E. ⚠ Read v2.7 A before
          acting on ANY geometry note elsewhere in this file.
  MKT-46  AD REEL LANE (2026-08-01) — Phase 0 answered, Phase 1 authorised,
          14 files probed clean; then PARKED: the "Covered vs Uncovered"
          pair was pair-rejected on Q1 (F3 serif O/zero glyphs, 8/03) and
          the head trim is IMPOSSIBLE — never re-attempt. ad_pro = 1 body.
  MKT-47  PRO ENDCARD SET REPLACED WHOLESALE (2026-08-01) ✅ — reveal /
          discharge / ignite / lock; three bed-viable, reveal bed-INELIGIBLE
          (never force-bed). std/steady/lattice retired. v2.7/§7.
  MKT-48  NINE-ASSET DROP (2026-08-03) — 5 registered (free endcards tide +
          bloom + adopen → free pool 5; evening_pro carrier rotation landed/
          dusk/last), 3 rejected (updraft; the ad pair), 1 tier ruling
          (adopen free-shared). v2.9 note G.
  MKT-49  ROTATION DE-CORRELATED (2026-08-06) ✅ — carrier lockstep fixed,
          schedule reshuffled per cycle, `npm run reel:rotation` added,
          reel:check warns on inside-a-week repeats; pinned lanes became
          SETS (VERIFY_INTROS / PUBLIC_INTROS / VERIFY_SEAL_MOTIONS). v2.9.
  MKT-50–53  SOCIAL-BRIEF / TRACK-RECORD lanes (8/07–8/09) — not reel
          assets; MKT-51's track-record rig contract IS reel-adjacent
          (verify live set). v3.1 note A ledger; MKT-53 audit entry owed.
  MKT-54  SLATE INTRO POOL 6 → 9 (2026-08-13) ✅ — guest / window / arrive
          registered; arrive's frame-one letterform ACCEPTED for group
          tiers only; .mov = seventh delivery failure class. v3.0/v3.1.
  MKT-55  RELABELER PROSE SWAP (2026-08-14) ✅ — DESIGN-03 modal prose
          leaked "pick" into the tier-1 audit and the public render aborted
          fail-closed; prose-form pick→signal swap added at the capture
          layer (consumer UI untouched). Standing lesson: any Home-tile or
          modal copy change must clear tier-1 lint before the next run.
  MKT-56  BODY GRID = STATIC STILL (2026-08-16) ✅ — zoom removed, still
          10.0s, modals 1.5s, body stays 19.0s; 56b member-legibility layer
          (emboss · notation strip · hold timer) on full-fidelity bodies
          only; 56c stamp/band positions — RETIRED with the surface by 57.
          v3.3.
  MKT-57  BODY CAPTURES THE COFFEE-MODE HOME (2026-08-16, first production
          8/17) ✅ — scope switcher · NEXT DRAW · 2×3 tiles; notation band at
          the bottom of the black space, stamp chip y=1000, DOM-read tile
          centres, "ENERGY N" relabel carve-out. 57a (8/17): Home modal now
          renders the MKT-11 panel (app fix, both Home mounts). v3.4/v3.5.

IN FLIGHT, NOT BUILT — do not propose against these yet; §10 has the detail:
  MKT-14  ✅ SHIPPED 2026-07-31 — no longer parked. The slate stamp carries
          the brand line "HITMASTER ZK6" BELOW the date line (both gate
          rulings given: that exact string, and board clearance solved by
          padding 328→375 rather than an offset shift). Moved out of this
          list by v2.7.
  MKT-15  Social handoff buttons ....... Phase 1 SHIPPED · Phase 2 BLOCKED
  MKT-16  ✅ SHIPPED 2026-07-30 (Phase 1+3) — allday_public LIVE end to end;
          moved out of this list by v2.3. Platforms unblockable, none
          flipped (operator-only, per-platform).

DAILY OUTPUT TODAY (2026-08-19): NINE reels, every run since 7/31 — verify +
verify_public (~23-29s each, variable by row count since MKT-27; the public
half is the masked grading cut — reel:verify builds both), All-Day Pro + Free
+ PUBLIC (allday_public), and Midday and Evening, each Pro + Free (redacted).
Slate reels are 33.83-34.53s depending on the day's intro; every slate body
is the coffee-Home still + six modals (MKT-56/57). All auto-publish to Admin
→ Reels with drafted captions. Nothing auto-posts, ever. Three production
days on the current body (8/17-8/19) with no aborts and no corrections.

⚠ THE PRO-ONLY SESSION RULING IS SUPERSEDED (MKT-26, 2026-07-29) — BUT NOT
REPEALED. Read the distinction before proposing anything against it:
  The gap it protected is unchanged. The free group still gets the All-Day
  drop in FULL and Midday/Evening WITHHELD — that gap is the Pro conversion
  frame, and the publish pipeline still enforces it for image kits.
  What changed is that MKT-13 named the precondition that would change the
  answer — "a free session reel needs a REDACTED CAPTURE, not just another
  variant entry" — and MKT-15 P2 built exactly that. So the free session reels
  now ship REDACTED: every digit of the combination masked, on the grid and in
  all six modals.
  A FULL-FIDELITY FREE SESSION REEL REMAINS BARRED. That half of the ruling
  never lifted, and the pipeline now refuses it structurally rather than by
  convention (§2, the redaction tag).
  WHAT THE FREE CUT KEEPS, deliberately: energy, ZK6 confidence, all four
  signal percentages, heat badges, jurisdiction counts and the entire prose
  structure. Only the values are withheld. The methodology is the gift; the
  digits are the gap Pro closes.

WHAT WOULD MOVE QUALITY MOST, IN ORDER (the standing asks — ⚠ v3.5: the
CURRENT open list is the STATE SNAPSHOT §G at the top of the file; this list
is kept as the closure record, most of it is ✅):
  1. NATIVE 1080×1920 SOURCE ASSETS. Every delivered motion file to date is
     720×1280 24fps and gets upscaled + frame-duplicated. ⚠ CONFIRMED A
     FOURTH TIME 2026-07-31: the MKT-36 public-intro regeneration was
     explicitly requested at native 1080×1920 and still came back 720×1280 —
     the generator ceiling is real, and the upscale pilot (blocked on
     Gemini Omni balance) is the only remaining path; stop spending
     deliveries chasing native. This is the single
     biggest quality gap left and the only remaining preflight warning class —
     ALL 49 current warnings are this one issue (plus the four rejected MKT-21
     bodies carrying printed reasons), now across the intro pool, the motion
     sets and the built matrix. MKT-19 did not make the problem worse; it made
     its size visible, since every rotation member is now checked, and MKT-26
     added four more by building two new endcards from the same 24fps sources.
  2. ✅ CLOSED 2026-07-29 (MKT-23). Midday, Evening and Verify now carry their
     own endcard closes; a Pro subscriber no longer reads the same words three
     times a day. Original entry kept below for the reasoning.
     MIDDAY + EVENING ENDCARD COPY — 2 lines each, the smallest real gap on
     this list. Their captions and stingers are genuinely distinct, but both
     endcards still carry allday_pro's lines verbatim ("FIRST ACCESS.
     ALWAYS." / "VERIFIED TOMORROW MORNING"), so a Pro subscriber reads the
     same close three times a day. Config strings over the existing motion
     file — zero generation, a text edit and a re-run.
  3. ✅ CLOSED 2026-07-29 (MKT-23 + MKT-26). Both sessions carry their own
     scripted VO, and the free tier now has its own pair on top — scripted
     separately from Pro, because first-access framing is barred below Pro
     (SOCIAL-13). Original entry:
     Session carriers are currently pro-only and generic. Session-specific VO
     ("the midday board", "tonight's board") would read better than reusing
     All-Day framing.
     ✅ EVENING HALF CLOSED 2026-08-03 (MKT-48): `evening_pro_carrier` now
     rotates — a set of four (incumbent + landed/dusk/last), all 10.005s,
     all joined to the existing shared pt2, margins 0.453-0.565s vs the
     +0.184s floor, seam f0 within ~3Hz of the incumbent's accepted delta.
     ⏳ STILL OPEN in this family: the verify carrier has no rotation set.
     Budget-gated.
  4. ✅ CLOSED 2026-07-30 — deadpan's regeneration fixed its open (−46.6dB
     mean / −18.0dB max over the first 0.3s; the max is now the hottest in
     the fleet). Original entry kept for the reasoning:
     A LOUDER OPEN ON ROTATION MEMBERS. Deadpan opens at −56.2 dB mean over
     its first 0.3s against the public intro's −37.9. MKT-08's "audio up
     from frame 1" rule is about autoplay retention, and a near-silent open
     spends it.
  5. More panels. The rotation adapts with no code change; six is the current
     set and the slot is now a product surface, so the copy bar is higher.
  6. ✅ GREW 2026-07-31 (MKT-37): 12 per kind (allday_public stays 8;
     verify-free's #9 replacement arrived same day and closed its set at
     12). Rotation adapted with no code change, spacing re-verified at 12.
     Can grow further.
  7. ✅ LARGELY CLOSED 2026-07-29 (MKT-23) — the set is now 6 and powerup's
     regeneration landed clean. Original entry:
     More intro rotation members. The set is 9 as of 2026-08-13 (standard,
     deadpan, arrival, monitors, powerup, board, guest, window, arrive —
     MKT-54; arrive's one-day hold cleared by ruling, v3.0 note B) — this
     ask is served; the regen queue's coffee/juggle/reveal would take it
     to 12.
     ⚠ ROBOT GUESTS ARE AN OPEN LANE, HUMAN GUESTS ARE CLOSED: three attempts
     at the guest shot with a HUMAN second figure were refused by the
     generator; the all-robot two-shot cleared first try. The block is the
     second human, not the composition — write two-hander prompts with every
     figure a machine (and keep the no-negatives phrasing below).
     Same delivery spec as §7; read the trim rule there before generating.
     ~~The outstanding one is still `powerup`, which needs REGENERATION, not
     a trim — and `arrival` now shows exactly what that shot should look
     like. ⏳ Its regeneration prompt is written and held with the operator,
     ready to run when budget returns.~~ ⚠ HISTORICAL (struck v3.1): the
     powerup regeneration RAN, was validated, and has SERVED in the rotation
     since 2026-07-30 (v2.0 note F; §7 entry). Nothing about powerup is
     outstanding. Note for anyone writing prompts in this
     asset class: the earlier attempt was REFUSED by the generator, traced to
     a CRITICAL block enumerating what must not appear in frame. Removing the
     negatives and describing the final beat as a STATE rather than a
     prohibition cleared it. Third refusal in this class traced to the same
     cause — negatives trigger them, scene descriptions do not.
  8. ✅ RE-CLOSED AGAIN 2026-08-03 (MKT-48) — THE FREE TIER IS NO LONGER
     THIN: the free pool widened 2→5 (tide +4.4dB bedable, bloom bedable
     post-crack, ad_open +4.5dB after the same-day tier ruling — all five
     members bed-viable), so the free-tier kinds no longer run three-plus
     daily reels on a two-motion rotation. A third endcard delivery
     (updraft) was rejected on composition + bed-ineligibility; see the
     MKT-48 audit entry. Prior re-closure kept for the reasoning:
     ✅ RE-CLOSED 2026-08-01 (MKT-47) AT A HIGHER COUNT — the pro tier was
     replaced wholesale and now runs FOUR motions with THREE bed-viable
     (discharge −27.5dB, ignite −30.9dB, lock −30.8dB); `reveal` is
     bed-INELIGIBLE at −43.2dB and must not be force-bedded (+15.6dB against a
     ±6dB clamp, worse than lattice's +13.5dB). The motions this entry closed
     on — std/steady/lattice — are all retired. ⚠ THE FREE TIER IS NOW THE
     THIN ONE: two motions (free +1.7dB, free_alt −3.1dB — both viable) serving
     All-Day Free AND both public kinds, i.e. three daily reels on a two-motion
     rotation, and the public cuts are the ones reaching cold audiences. When
     generation budget next allows, FREE-tier motions are the better spend than
     a fifth pro motion. Prior closure kept for the reasoning:
     ✅ CLOSED 2026-07-29 (MKT-23) — `endcard_motion_pro_steady` beds cleanly at
     -29.1dB, so the pro tier has two bed-viable motions and a short-carrier Pro
     day no longer collapses to one. `lattice` also landed but is bed-INELIGIBLE
     (see v1.9 note F). Original entry kept for the reasoning:
     A PRO ENDCARD MOTION THAT CAN BED — ⏳ PROMPT WRITTEN, AWAITING BUDGET.
     The pro set is 2 but only 1 (std) has a usable hum bed, because pro_alt
     is a build-up, so on a short-carrier Pro day the rotation collapses to a
     single motion. Not broken and not urgent — every current carrier is
     wall-to-wall — but the pro tier has no redundancy the day that changes.
     A prompt targeting a LEVEL-STEADY 0-4.0s window was delivered to the
     operator 2026-07-29 and runs when generation budget returns.

WHAT IS DELIBERATELY NOT BUILT:
  - Auto-posting to Facebook (platform + brand-safety rule).
  - A public-page reel lane (page is brand-only, under probation).
  - FULL-FIDELITY free session reels. The REDACTED ones ship as of MKT-26;
    an unmasked one is still barred and the assembler refuses it (§2).
  - (CLEARED 2026-07-29) A stinger on the verify reel — now LIVE. Kept here for
    the reasoning: it was barred on ratio grounds (64% branding against a 6.3s
    body), and the operator's condition was "the holds first, then the stinger".
    MKT-27 delivered the holds, branding fell to 44%, the clips were built, and
    only then was the flag flipped — that order is the durable lesson, since
    probeStinger degrades SILENTLY on a missing build.


--------------------------------------------------------------------------------
10. IN FLIGHT — WHAT EXISTS AND WHAT DOES NOT (2026-08-19, resynced by v3.5;
    everything here is SHIPPED except the MKT-15 device test + platform flips)
--------------------------------------------------------------------------------
Added in v1.6 because three lanes are now partly delivered, and "an asset is in
the repo" has been mistaken for "the pipeline reads it". Nothing in this section
is running daily.

MKT-15 — SOCIAL HANDOFF. Phase 1 SHIPPED.
  A platform registry + a caption transform + one row per platform in Admin →
  Reels: shaped-caption preview, char budget, per-platform lint, Two-Question
  checkboxes, one "Save + Copy + Open" tap. `npm run social:dryrun` is the
  off-device gate.
  ENABLED: Telegram only — a room we own, so no classifier decides whether we
  are gambling-adjacent there. It is tier 4 (the Pro room), not a cross-post.
  REGISTERED BUT DISABLED: YouTube, TikTok, Reddit, X — all four blocked on
  Phase 2's public cut, each declaring asset:'redacted' so the dependency
  lives in the registry rather than a comment. Instagram is disabled by
  STANDING RULING, not a gate: it needs a linked FB Page and Meta review
  against a page de-recommended twice, and Meta enforces across linked
  accounts. Phase 2 does not unblock it.
  THE MECHANICAL CEILING, worth internalising before proposing anything here:
  no URL scheme on any platform can attach a video file. Every path is
  save-to-camera-roll then pick-in-app; deep links only choose which screen
  opens. The CLIPBOARD is the real value, because retyping a platform-shaped
  caption is the actual friction.
  ⚠ STILL OWED: an operator DEVICE test. The deep links are documented-scheme
  research, not observed behaviour — this is a Linux codespace with no phone.
  X / Reddit / Telegram carry templates; YouTube / TikTok / Instagram are
  deliberately plain app-open, because an undocumented scheme that silently
  fails is worse than a link that honestly just opens the app.

MKT-15 — PHASE 2, THE PUBLIC CUT. ⚠ UPDATED 2026-07-30 (MKT-30): THE CAPTURE
MODE IS BUILT. `render-allday-body --redact --relabel` produces the public
body: eight copy slots swapped at capture, session labels dropped, state codes
suppressed (counts kept), heat vocabulary swapped (PROVISIONAL neutrals — see
below), masked combinations rendered as blurred placeholder + gold bolt (60%
height, 85% opacity, one per masked number), and EVERY rendered string run
through the real tier-1 lint engine per modal with an EMPTY acceptance bar.
Q1 is absolute at tier 1 — "100", "100%" and "102×" all block, so remaining
3-digit runs are structurally hidden. Bodies carry an `hm_reel_public` tag for
the eventual assembler to assert. The bolt decisions are made (gold gradient
per palette §7, centered, 60%/85%). [v2.3: BOTH remaining items CLOSED
2026-07-30 — ① the neutral heat vocabulary was delivered and serves in
HEAT_RELABEL (11-slot table in the MKT-28 audit entry), ② MKT-16's five
registration points landed and allday_public is LIVE. Nothing remains here.]
The original scoping below is kept for the record.

MKT-15 — PHASE 2, THE PUBLIC CUT. BLOCKED, and bigger than first scoped.
  The headline finding: MASKING DIGITS IS NOT SUFFICIENT. The body is a screen
  recording of the real app, and the live audit (every on-screen string run
  through the real lint engine at tier 1) found four BLOCKING vocabulary terms
  structurally present on all six modals — PICK, STRAIGHT, BOX, PLAY — plus
  session labels and state-code attributions. A public cut needs a RELABELLED
  capture mode, not merely a redacted one.
  This is a TIER MISMATCH, NOT A BUG. Those words are the sanctioned
  vocabulary for the paying subscribers the app is built for. The fix is a
  capture-mode override and NEVER an edit to the consumer UI.
  COPY IS SETTLED — the eight-slot public set is delivered and approved; see
  docs/mkt15_phase2_copy_brief.md. Notably slot 3 ships as "ANY ORDER", not
  "SET": the brief's character-count rule was a bad proxy for pixel width, and
  measured on the live modal the chip has 86px of content width against
  "ANY ORDER"'s 55px. Paired with "BEST ORDER" it teaches itself.
  THE REDACTION IS NOT A BLUR OF THE DIGITS. The look is extreme blur + the
  bolt, as ruled — but capture mode never RENDERS the real digits: it draws a
  placeholder block, blurs that, and lays the bolt over it. A blur or mosaic
  derived from the true pixels is recoverable by sharpening across only 1,000
  combinations, and a high-contrast blurred 3-digit block is often readable at
  a glance anyway. Drawing a placeholder makes the frame-by-frame Q1 pass a
  fact rather than a judgement about how much blur is enough. Nothing changes
  in what you supply.
  ✅ CLOSED 2026-07-31 (MKT-14 Phase 0): the three bolt decisions shipped with
  MKT-30 (gold gradient per palette §7, centred, 60% height / 85% opacity),
  and bolt_mark.svg was NEVER actually outstanding — it has been on disk and
  TRACKED IN GIT since 2026-07-28 (commit 407a914, MKT-17), same geometry and
  white fill as the copy re-delivered with the MKT-14 order. This line was
  wrong from the day it was written.
  ⚠ ALSO WORTH AUDITING: the PLAN tab CONTENTS are unaudited. The renderer
  captures each modal at its DEFAULT tab and never clicks through, so only the
  tab label is exposed today. The day a tab-switch beat is added to the
  capture, an un-audited surface enters frame. Better to have the list first.

MKT-16 — PUBLIC ASSET INTEGRATION. ✅ PHASE 1+3 SHIPPED 2026-07-30 (v2.3).
  allday_public is LIVE END TO END: registered at all five points + the DB
  kind CHECK (migration landed FIRST, operator ruling), assembled
  (34.20s: 6.0 public intro + 2.7 stinger + 19.0 public body + 6.5 endcard)
  and published daily — reel:daily builds EIGHT reels. The stored Q1/Q2 gate
  record (_public_gate_records.json) is REQUIRED before the kind publishes;
  reel:check FAILs a public kind without it. YouTube/TikTok/Reddit/X have
  their asset:'redacted' condition MET and are UNBLOCKABLE — none flipped;
  enabling is operator-only, one platform at a time, Two-Question checkboxes
  on every handoff. Instagram stays do-not-build (Meta cross-enforcement).
  midday_public / evening_public stay UNREGISTERED by ruling (no session
  carriers; three daily public reels is a cadence risk).
  [Original Phase 0 scoping kept below for the record — superseded:]
  DELIVERED AND USABLE: anchor_intro_public.mp4 (trimmed, passing preflight),
  public_carrier.mp4 + _pt2 (joined, 20.360s, passing), bolt_mark_2048.png.

MKT-14 — SLATE-STAMP BRAND ATTRIBUTION. ✅ SHIPPED 2026-07-31 (v2.7 note G;
  this block was the stale half of a struck pair until v3.1 — §9's in-flight
  list already said so). The brand line rides the slate stamp BELOW the date
  line; the earlier bottom-right watermark version is SUPERSEDED and must not
  be built — every short-form platform stacks its own chrome bottom-right, so
  a mark there is buried on all of them. `watermark_source.mp4` is delivery
  evidence only, permanently unreferenced, deletable.

~~FREE-GROUP MIDDAY / EVENING REEL — not a lane, and nothing is assumed.
  It is the second payoff of the masked capture, but it is not registered
  anywhere (§9's PRO-ONLY ruling still stands until it is). When it is built
  it needs its OWN endcard and carrier — Pro CTA and $2.49 pricing, not the
  free-community CTA, and NOT inherited from the public set. Those have not
  been written and will not be invented.~~
  ⚠ HISTORICAL (struck v3.5 — this block outlived its subject by three
  weeks): midday_free / evening_free SHIPPED 2026-07-29 (MKT-26) as REDACTED
  cuts, with their own endcards (free motion set, $2.49 line sanctioned at
  tier 2) and their own carriers (midday_free_carrier + pt2, evening_free_
  carrier + pt2 — single-entry sets). They run daily. What remains true: a
  FULL-FIDELITY free session reel is barred and the assembler refuses it.
================================================================================
