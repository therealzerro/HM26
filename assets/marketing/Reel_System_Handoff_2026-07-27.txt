================================================================================
HITMASTER ZK6 — DAILY REEL SYSTEM
Content Agent Handoff · 2026-07-28 · v1.6
================================================================================
Read this before proposing or producing ANY reel-related asset or enhancement.
Companion docs:
  - assets/HitMaster_Brand_Rehab_Skill_Brief_v2.md  (brand law — canonical)
  - assets/marketing/AllDay_Reel_Endcard_Revision_Brief_2026-07-27.md (endcard contract)
  - docs/mkt15_phase2_copy_brief.md (public copy set — DELIVERED + the bolt request)
  - REEL_COMMANDS.txt (repo root — operator command reference)

WHAT CHANGED IN v1.6 (six corrections; v1.5 is wrong on all six)
  1. THE MULTI-PART CARRIER CEILING IN v1.5 IS ARITHMETICALLY WRONG. It said
     "10.0 + 7.0 = the 20.0s carrier ceiling", which is 17.0. Corrected in §7 —
     part 2's last word may land as late as ~9.65s. Read that entry before
     writing another carrier; you have ~2.6s more room than v1.5 implied.
  2. REEL DURATION IS NO LONGER A CONSTANT. 33.83s or 34.20s depending on which
     intro the day's rotation draws. §1, §2.
  3. THE INTRO IS NO LONGER ONE SHARED FILE. It is resolved PER REEL KIND, and
     slate reels rotate over a set. §2, §7 (MKT-17).
  4. NEW INTRO DELIVERY SPEC — the generator only emits at 4/6/8/10s presets, so
     EVERY intro delivered to date has arrived at 10.005s against a 6.5s
     ceiling. Trimming is now the expected path, not an incident. §7.
  5. PUBLIC REEL KINDS are in flight and NOT built. §10 states exactly what
     exists so nothing gets proposed against them prematurely.
  6. `reel:check` now runs a STRAY-ASSET SCAN — a delivered file that no
     registry reads is reported instead of sitting silently unused. §7.


--------------------------------------------------------------------------------
1. WHAT THE SYSTEM IS
--------------------------------------------------------------------------------
A fully automated daily video pipeline. Two commands produce, assemble, upload,
and stage everything; the operator posts from their phone with two taps.

  npm run reel:allday   → TWO reels for TODAY (Pro variant + Free variant):
                          Anchor intro + branded stinger + the All-Day signal
                          board + all six signal detail modals, captured from
                          the LIVE app with real data.

  npm run reel:verify   → ONE reel for YESTERDAY (14.4s): "Receipts" —
                          yesterday's verification breakdown. Intentionally
                          refuses to build on zero-match days (no receipts,
                          no reel).

  npm run reel:midday   → ONE reel each, PRO ONLY: the identical All-Day shape
  npm run reel:evening    built from that SESSION's board. Added 2026-07-28
                          (MKT-13).

  ⚠ SLATE REEL LENGTH IS NOT A CONSTANT (changed 2026-07-28, MKT-17). It is
  33.83s or 34.20s depending on which intro the day's rotation draws — the
  standard intro is 5.625s, the deadpan is 6.000s. v1.5's flat "33.8s each" is
  retired. Nothing you write is affected: the usable VO window is bodyDur+0.4
  and is INDEPENDENT of intro length (see §2), so carrier specs do not move.
  Only the finished file's duration does.

SESSION WAVE (MKT-13) — read before proposing session content. Midday and
Evening are NOT a different reel: they are the same reel parameterized by
scope. Same anatomy (§2), same caption engine (§5), same publish chain (§4),
same constraints (§6). What differs is which board is captured, the endcard
and stinger copy, and the slate-stamp scope tag.

They are PRO ONLY, and that is a ruling, not a missing asset. The free group
gets the All-Day drop in FULL but Midday/Evening REDACTED — that gap is the
Pro conversion frame, and the publish pipeline already enforces it for image
kits. A full-fidelity free session reel would give away exactly what the
redaction withholds. Do not propose one as "just another variant": it would
require a REDACTED CAPTURE (digits masked in the UI), not a config entry.

Adding a further session/variant is config: an endcard-config entry, a
stinger-config entry, a caption kind with a unique offset, a scope entry in
scripts/reel-scopes.ts — plus widening the marketing_reels kind CHECK
constraint and adding a KIND_UI entry, or the publish fails at upsert and the
admin Reels tab cannot render the row.

Each run ends by auto-uploading the finals to the app backend. They appear in
the operator's Admin → Reels screen seconds later with a fresh, auto-written
caption (see §5). NOTHING auto-posts to Facebook — a human always completes
the post (platform rule + brand-safety rule).

Everything is captured from the real product UI — no mockups, no comps. The
renderer drives the actual app, verifies each signal modal is the right one,
and runs an in-frame vocabulary guard; any violation aborts the render.


--------------------------------------------------------------------------------
2. REEL ANATOMY — SLATE DROPS (33.83s or 34.20s @ 1080×1920 60fps)
--------------------------------------------------------------------------------
Covers All-Day (Pro + Free) AND the Midday/Evening session drops (Pro only) —
they are the SAME reel built from a different board, so this one anatomy is the
whole spec for all four. Variants share one UI body render per scope; they
differ in carrier (voiceover) and endcard/stinger copy.

Times below are quoted for the 5.6s standard intro. On a 6.0s rotation member
every timestamp after the intro shifts 0.4s later and the reel is 34.20s.

  TIMELINE (t in seconds)
  0.0 – 5.6    INTRO — "The Anchor". Opens ON the gesture — frame 1 is the
               Anchor already holding the phone up, smoke live on its screen.
               Internal beats: 0.0–1.1 presents the phone to camera →
               1.1–2.1 camera dives into the screen → 2.1–5.6 full-frame
               purple smoke. Its own audio is a single whoosh sting, present
               from frame 1, peaking ~2.0, decayed to silence by 5.0.

               ⚠ CHANGED 2026-07-28 (MKT-17) — THE INTRO IS NO LONGER ONE
               SHARED FILE. v1.5 said "anchor_intro.mp4, shared by every reel
               kind". It is now resolved PER KIND:
                 slate reels  — ROTATE over a set, currently [standard,
                                deadpan], picked by a date-derived offset.
                                Both pro and free draw the SAME intro on a
                                given day (different rooms, so no spacing is
                                needed). Re-running a past date reproduces
                                that date's intro exactly.
                 verify       — always standard. A deadpan gag ahead of
                                yesterday's receipts is a tonal mismatch.
                 public kinds — always anchor_intro_public.mp4, and they
                                NEVER fall through into the rotation: a
                                public cut must not silently acquire the
                                deadpan gag.
               Growing the rotation is one line of config plus a delivered
               file — see the delivery spec in §7 before proposing one.
  5.3 – 8.3    STINGER — the intro's smoke crossfades (0.3s) into a branded
               beat: smoke condenses into the bolt, the wordmark + variant
               headline resolve natively, then the smoke sweeps back and
               refills the frame. It both OPENS and CLOSES on smoke, which is
               why the existing dissolve below needs no change.
               Per-variant copy; prebuilt by npm run stinger:build.
  7.5 – 8.3    0.8s eased dissolve from the stinger's smoke into the UI body
               (overlaps the tail — not extra time).
  8.3 – 27.3   BODY (19.0s) — 4.0s slow push-in over the full 2×3 signal
               grid (all six on screen), then each signal tapped in rank
               order 1→6: modal opens, 2.5s hold on digits/energy/
               confidence/breakdown, close. Hard cuts between modals.
  12.3 – 27.3  PANELS — a rotating branded panel sits below the "RESOLVED IN"
               card in each of the six pick-detail modals. It is NOT added to
               the video: it is a real element of the app screen, so it is
               captured along with everything else. A different panel appears
               on each modal; which one leads rotates by date. Panels appear
               in the modals only — the grid push-in has no room for one.
               ⚠ CHANGED 2026-07-28 — PANELS ARE APP-WIDE. An earlier version
               of this doc said the panel was visible only while the renderer
               was driving the app and that "subscribers never see it". That
               capture gate was REMOVED by operator decision: every user now
               sees these panels, free and paying, and the slot is destined to
               become a monetised placement (ads for free users, in-house
               panels for subscribers). Treat panel copy as PRODUCT surface,
               not marketing overlay — a paying subscriber reads it, so it
               must survive that room, not just a reel frame.
  27.3 – 33.8  OUTRO (6.5s) — the endcard's FIRST 6.5s, hard cut in:
               smoke rise → bolt snap → lockup resolves → calm hold.
               THE SNAP TIME DIFFERS PER ENDCARD — it is a property of the
               asset, not of the timeline. Measured 2026-07-27:
                 Pro  — crack at 4.4s of the endcard → ~31.7s of the reel
                 Free — crack at 4.3s of the endcard → ~31.6s of the reel
               (Both rebuilt from motion files 7/28; the old baked Free
                endcard cracked at ~1.0s, which is why it could not bed.)
               (An earlier version of this doc quoted a single "~22.8s" for
               both. That was never measured for Pro and was wrong by 3s.)

  DEGRADATION, IN ORDER (the daily run can never block on the intro):
  a missing or defective rotation member drops out of THAT DAY with a logged
  warning and the next candidate is used; if every candidate is unusable the
  assembler falls back to the legacy 1.2s lockup open and the reel is 26.7s.
  Note the asymmetry, which is deliberate: preflight treats a MISSING intro as
  a warning (the fallback handles it) but a DEFECTIVE one as a hard FAIL — a
  bad variant must fail before assembly rather than quietly degrading a run.

  AUDIO (two automatic modes, loudness −14 LUFS integrated):
  The intro carries its own sting, and the stinger its own impact. The
  carrier VO enters 0.4s before the dissolve completes — at 7.9 on the 5.6s
  intro, 8.3 on a 6.0s one. Carrier time 0 sits at whichever of those it is,
  and every VO length below is measured in CARRIER-LOCAL seconds, which is
  exactly why the rotation does not touch your specs.
  The usable VO window is bodyDur+0.4 = 19.4s and is INDEPENDENT of intro
  length, so retiming the intro never re-opens the carrier spec.
  - LONG CARRIER (VO ≥ ~19.4s) — "overlap mode": the voice plays wall-to-
    wall over the body and may ride up to 1.1s into the smoke rise, but
    must finish before the endcard's bolt snap (see the timeline above —
    Pro ~31.7s, Free ~31.6s). The endcard's own synced outro audio is
    mixed in from 27.3s. Carrier content past the VO is DISCARDED
    (generated tracks are unreliable after their voice ends).
    In practice the 20.5s carrier fade bites first, so that is the number
    to script to — not the snap.
  - SHORT CARRIER (VO ~10s) — the voice covers the grid + first modals;
    the endcard's PRE-crack tension hum is looped as a palindrome and laid
    under the remaining modals, then the endcard plays its outro natively.
    ⚠ THE FREE ENDCARD CANNOT SUPPLY A BED. Its crack lands at ~1.0s and
    its audio then decays monotonically to silence by 8.0s, so there is no
    crack-free stretch with usable level. Free therefore REQUIRES a long
    (wall-to-wall) carrier; on a short one, preflight fails rather than
    replaying the bolt crack under the modals. Pro is unaffected — its
    crack is at 4.25s, leaving 0-3.9s clean.

  WHAT THIS MEANS FOR CARRIER PRODUCTION (your main deliverable):
  - Target VO length: ~19.4s for wall-to-wall narration (preferred), or
    ~10s accepting the hum-bed remainder. Anything between works; the
    assembler adapts automatically and prints which mode it used.
  - Write VO to the visual beat map, in CARRIER-LOCAL time: 0–0.4 lands
    under the last of the dissolve → 0.4–4.4 the full board (six signals
    on screen) → 4.4 onward, one signal detail roughly every 2s → if
    long-form, final line can land over the smoke rise.
  - Do NOT write VO for the intro. The Anchor beat is deliberately
    voiceless — it carries its own sting and the reel's first words land
    as the grid appears. The sting is present from frame 1 by design: the
    reel must never open on silence (autoplay retention).
  - Never script the VO to reference the bolt snap or anything after it —
    that space belongs to the endcard's synced sound.
  - Two variants every time: PRO (first-access framing, never pricing) and
    FREE (pure value — the All-Day drop carries NO Pro pitch, per the
    SOCIAL-13 depth rule).

  ENDCARD CONTRACT (full spec in the endcard revision brief):
  - Final frame must be the settled lockup (it is reused as the reel's
    opening frame — the loop is intentional).
  - First 6.5s must be the formation sequence (used as the outro).
  - Audio must be front-loaded and synced to the visual snap; the
    assembler hard-aborts if endcard audio is too short for the outro/bed.


--------------------------------------------------------------------------------
3. REEL ANATOMY — VERIFY / "RECEIPTS" (14.4s @ 1080×1920 60fps)
--------------------------------------------------------------------------------
  0.0 – 5.6    INTRO — the STANDARD Anchor intro, always. Verify is a fixed
               kind and never draws a rotation member (MKT-17); its length is
               therefore stable at 5.6s and so is the 14.4s total. 0.8s
               dissolve from the smoke tail.
  5.6 – 11.9   BODY (6.3s) — yesterday's verification breakdown captured
               from the live app.
  11.9 – 14.4  CLOSE — final 2.5s of verif_endcard (static lockup), hard cut.
  AUDIO        The intro's own sting 0.0–5.6; verif_carrier enters at 5.2
               and covers to the end, −14 LUFS, de-click edge fades. The
               carrier therefore needs 9.2s of usable track — and that
               figure is FLAT regardless of how long the intro is.

  Without a usable intro this reverts to the legacy 1.2s bolt open and a
  10.0s reel needing a 10.0s carrier.

  - Builds for YESTERDAY by default. If yesterday had zero verified
    matches, the pipeline aborts on purpose — we never fake receipts.


--------------------------------------------------------------------------------
4. AFTER ASSEMBLY — THE AUTOMATIC PUBLISH CHAIN
--------------------------------------------------------------------------------
Per reel, three finals are produced and uploaded (working files stay local):
  - 9:16 master        e.g. allday_pro_20260727.mp4 / verify_reel_20260726.mp4
  - 1:1 feed cut       …_1x1.mp4 (center crop)
  - contact sheet PNG  …_contact.png (the storyboard strip)

Upload target: public storage (bucket "marketing-reels"), registered in the
app's database one row per (date, kind). kind ∈ allday_pro | allday_free |
verify | midday_pro | evening_pro. Storage self-prunes after 30 days (the log
rows are kept). The kind list is enforced by a database CHECK constraint AND
needs a matching entry in the admin Reels screen — see §8 before proposing a
new reel kind.

The operator's Admin → Reels screen then offers, per reel:
  - contact-sheet storyboard preview + buttons to watch the 9:16 / 1:1
  - target selector: Free Group / Pro Group / Cross-Post
  - the auto-written caption in an editor, gated by the full brand-lint
    engine (a blocking vocabulary violation disables the send buttons)
  - Two-Question NO/NO checkboxes, required for cross-posts (reels show
    real digits, so cross-posts honestly fail Q1 — that is the filter
    working; reels live in the groups)
  - posting flows: phone = Save to Photos + Open Group (attach from camera
    roll inside Facebook — the only reliable FB video path); web =
    Prepare Video, then Save/Share (native sheet) or Download
  - every handoff is logged centrally with same-day duplicate-caption
    detection across all surfaces

There is deliberately NO public-page (API) lane for reels and NO auto-posting.


--------------------------------------------------------------------------------
5. THE CAPTION SYSTEM (matters for any copy you propose)
--------------------------------------------------------------------------------
Captions are generated at publish time by a template engine — not written
per-day by hand, not AI-freestyled.

  ROTATION — each content kind has 8 template families. The day's date picks
  the template deterministically: a different style every day, never the
  same style on two kinds the same day (protects the duplicate-caption
  rule), and re-running the same date reproduces the same caption exactly.
  Kinds today: allday_pro, allday_free, verify (free draft), verify_pro,
  midday_pro, evening_pro. The three PRO slate kinds are spaced so they never
  draw the same template family on the same day — a subscriber who sees the
  Midday, Evening and All-Day posts reads three different messages.

  THE NUMBERS POLICY (operator ruling, absolute):
  - PRO captions ONLY may contain real performance data: verified counts,
    state attributions, STRAIGHT MATCH callouts, 30-day totals. These are
    computed fresh from the verified results record (yesterday's board
    checked against actual observed draws — never cached flags).
  - FREE captions are QUALITATIVE. They may DESCRIBE matches creatively,
    but only in descriptor language — scale words ("a verified MATCH" /
    "multiple verified MATCHES" / "a stack of verified MATCHES"), spread
    words ("across several state boards" / "from coast to coast"), and
    STRAIGHT MATCH presence ("one of them a dead-on STRAIGHT MATCH").
    Never counts, never state names. The video shows the receipts; the
    caption text a classifier reads stays clean. Gambling-adjacency
    protection — do not ask for exceptions.
  - THE VERIFY REEL CARRIES TWO DRAFTS (one reel, two rooms): a free-group
    draft (qualitative description, above) and a pro-group draft (full
    precision). In the operator's Reels screen, switching the target
    between Free and Pro loads the matching draft automatically.
    Example, same day (7/24):
      FREE: "Grade day: 7/24 closed with a stack of verified MATCHES from
             coast to coast — one of them a dead-on STRAIGHT MATCH. Pull
             your state's results and compare."
      PRO:  "Graded in full: 7 MATCHES across 7 states on 7/24 — including
             a STRAIGHT MATCH in PA. 🧾"

  VOICE RULES for any template you propose:
  - The product noun is "signals". Always. (Never picks/plays/numbers.)
  - Match vocabulary: MATCH / BOX MATCH / STRAIGHT MATCH. Never "hit(s)",
    never "partial match", never win/won/winning.
  - No guarantees, no urgency hype ("don't miss out", "act now"), max ~2
    emoji, viewer-friendly plain language — receipts-and-record energy,
    not hype.
  - Free All-Day captions never pitch Pro. Pro captions never mention
    pricing.
  - Approved coverage claims: "40+ states & provinces" or "39 tracked
    feeds" (never "40+ jurisdictions").

  PROPOSING NEW TEMPLATES: deliver lines as text following the rules above,
  tagged by kind (pro / free / verify). Engineering adds them to the
  registry — template count per kind can grow beyond 8; rotation adapts.

  New content types (future reels/posts) get their own registry entry with
  the same contract. The realNumbers flag is structural: a non-pro template
  physically cannot access performance data.


--------------------------------------------------------------------------------
6. HARD CONSTRAINTS (non-negotiable, from the brand brief)
--------------------------------------------------------------------------------
  - The Facebook page is under algorithmic probation (de-recommended twice,
    appeals denied). Public page = brand-only; reels and their captions are
    GROUP content (free = conversion funnel, pro = paid room).
  - Two-Question filter is mandatory before anything leaves surfaces we
    control (cross-posts): Q1 no 3-digit numbers visible, Q2 no forbidden
    vocabulary rendered/spoken. Both must be NO.
  - Pro group price is $2.49/mo where pricing is allowed (free-group copy);
    NO app subscription price in any ad or caption.
  - The in-frame vocabulary guard and the caption lint are enforcement, not
    suggestions — plan assets so they pass, don't plan appeals.


--------------------------------------------------------------------------------
7. CURRENT ASSET INVENTORY (what you own / can replace)
--------------------------------------------------------------------------------
  assets/marketing/
    anchor_intro.mp4              THE ANCHOR intro, "standard" — rotation
                                  member AND the fallback every kind
                                  degrades to. 5.6s, trimmed 1.9→7.5s.
    anchor_intro_deadpan.mp4      Rotation member, 6.0s (IN 0.0 / OUT 6.0).
                                  Keeps the whole sheet gag. ⚠ opens quiet
                                  (−56.2 dB mean over the first 0.3s vs
                                  public's −37.9) — flagged, not blocking,
                                  but louder would be better next time.
    anchor_intro_public.mp4       Fixed intro for the PUBLIC kinds, 6.0s
                                  (IN 0.0 / OUT 6.0). Opens on the visor
                                  igniting in darkness — the cold-audience
                                  hook — audio present from frame 1. The
                                  only intro cleared for a public surface;
                                  never swapped for a rotation member.
    anchor_intro_powerup.mp4      REJECTED, see the delivery spec below.
                                  Present but unregistered; awaiting
                                  regeneration.
    anchor_intro_*_master_10s.mp4 Delivered 10s masters, kept so the trims
                                  stay reproducible. NOT read by the
                                  pipeline.
    anchor_reference.png          Character reference still for the Anchor
                                  (regeneration/continuity — not composited).
    allday_pro_carrier.mp4        Pro VO carrier (10s class, backup exists)
    allday_free_carrier.mp4       Free VO carrier (10s class, backup exists)
    allday_pro_endcard.mp4        Pro endcard (open frame + outro + audio)
    allday_free_endcard.mp4       Free endcard
    verif_carrier.mp4             Verify reel audio bed (9.2s used)
    verif_endcard.mp4             Verify bolt endcard
    midday_pro_carrier.mp4        Midday VO carrier (+ _pt2, auto-joined)
    evening_pro_carrier.mp4       Evening VO carrier (+ _pt2, auto-joined)
    midday_pro_endcard.mp4        Midday endcard — built from the SHARED
    evening_pro_endcard.mp4       endcard_motion_pro.mp4 with different copy,
                                  i.e. zero new video generation (MKT-10's
                                  whole payoff). Same for their stingers.
    endcard_motion_pro.mp4        Text-free endcard motion, shared by the pro
    endcard_motion_free.mp4       and free/session endcards.
    stinger_motion.mp4            Text-free stinger motion, shared by all
                                  stinger variants.
    stinger_<variant>.mp4         Prebuilt per-variant stingers (allday_pro,
                                  allday_free, midday_pro, evening_pro).
                                  Generated by `npm run stinger:build`.
    panels/panel_*.png            Rotating promo panels (6): brand, signals,
                                  coverage, anchor, app, zk30. Source art;
                                  panels/built/ is generated, not source.

  PANEL DELIVERY SPEC (assets/marketing/panels/):
  - ~3:2 PNG whose CENTRED horizontal band is the artwork. The band is
    detected and cropped automatically, so the band's own aspect is free
    (delivered set ranges 2.97:1 to 4.40:1) — each panel keeps its native
    height; the app lays it out full-width and derives the height.
  - Anything below the band (e.g. a generator watermark) is discarded.
  - Backing colour does NOT need to match the app: every panel is
    feathered on all four edges, which is what makes full-bleed artwork
    sit natively. Both black-backed and full-bleed purple panels work.
  - ⚠ DUAL-CONTEXT BY DEFAULT (standing rule, 2026-07-28). Every panel is now
    read BOTH by a reel viewer who does not have the app AND by a paying
    subscriber inside it. Write every panel's copy so it is true in both
    rooms. If a panel is ever capture-only again that will be stated
    explicitly; absent that, assume dual-context.
    THE LIVE EXAMPLE: panel_app.png says "THE APP IS COMING / APP STORE ·
    GOOGLE PLAY" — correct for a reel, but it tells a subscriber reading it
    inside the product that the product does not exist. It clears the brand
    lint (no forbidden vocabulary), which is the point: this class of failure
    is invisible to every automated check we have. Approved replacement,
    pending regenerated artwork: "SIX SIGNALS. EVERY MORNING." /
    "RANKED · EXPLAINED · VERIFIED". Not urgent, not blocking.
    Note panel copy is BAKED PIXELS, unlike endcard and stinger copy — MKT-10
    made those config strings, panels were never converted. So a wording fix
    here costs a regeneration, and it is worth getting right the first time.
  - TIER-NEUTRAL, no exceptions: no pricing, no upgrade CTA, no Pro pitch.
    TWO independent reasons now — (1) one body render feeds both the Pro and
    Free reels, and (2) since 2026-07-28 the panels are APP-WIDE, so a paying
    subscriber sees them inside the product. A "subscribe" pitch shown to
    someone who already subscribes is the failure this rule prevents.
  - Artwork is served to the app from the public `app-panels` bucket (not
    bundled), so a swap reaches users without an app release — and equally,
    a panel that is built locally but not republished is invisible to
    everyone. `reel:check` compares the served bytes to the local build.
  - Copy is cleared MANUALLY and pinned to the file's sha256. Changing a
    panel's artwork voids its clearance and `reel:check` fails until it is
    re-reviewed and re-pinned (npm run panel:build -- --print-hashes).
  - After a drop-in: `npm run panel:build`, then `npm run reel:check`.
  - The set is expected to grow past six; rotation adapts with no code
    change. A missing panel is dropped from the day's rotation with a
    warning — it never blocks a run.
  Replacing any of these with a spec-compliant upgrade requires zero code
  changes — drop-in by filename, subject to the filename rule below.
  (v1.5 listed "longer ~19.4s All-Day carriers" as the standing request. That
  is DONE — every slate carrier now ships as a joined ~20.4s pair and runs
  wall-to-wall with nothing discarded. The current standing asks are in §9.)

  MULTI-PART CARRIERS (when the generator caps clips at ~10s):
  Deliver a long carrier as numbered parts and the pipeline joins them
  automatically, in order, before anything else looks at the audio:

      allday_pro_carrier.mp4  +  allday_pro_carrier_pt2.mp4  [+ _pt3 …]

  Works for EVERY carrier (all-day pro/free, midday, evening, verify). Audio
  only is joined — the carriers' video is discarded by design. Rules:
  - A 0.35s breath is inserted automatically at each part boundary, on
    top of whatever silence the parts already carry. You do NOT need to
    pad the end of part 1 or the start of part 2 — start part 2's first
    word promptly and the joiner supplies the pause. (The 7/27 delivery
    started part 2 at 0.2s, which left only a 0.4s gap after part 1's
    last word and read as crowded at this voice's pace.)
  - **The ceiling applies to the JOIN, not to each part.**
    ⚠ CORRECTED 2026-07-28 — v1.5 said "part 2's final word must land by
    ~7.0s of part 2 (10.0 + 7.0 = the 20.0s carrier ceiling)". That is
    arithmetically wrong: 10.0 + 7.0 is 17.0, and it cost you ~2.6s of
    usable narration. Measured against the code:

        part 1                 10.005s   (generator's 10s preset)
        + breath                0.350s   (inserted automatically)
        + part 2               10.005s
        = joined file          20.360s
        assembler fade begins  20.010s
        so the LAST WORD must land by ~9.65s OF PART 2.

    In other words a ~10s part 2 may use essentially its full length.
    The 20.0s figure is the target; 20.5s is the hard fade. Preflight
    measures the JOIN, so an overrun is caught before assembly.

  - ⚠ THAT EXTRA ROOM IS NOT SPENDABLE. The correction is real against
    v1.5's arithmetic but NOT against what the generator delivers: this
    voice runs ~35% longer than scripted timings. public_carrier_pt2's
    last word was written for 6.9s and came back at 9.32s, so against the
    corrected ~9.65s ceiling the delivered pair has ~0.33s of ACTUAL and
    ~0.25s of SCRIPTED headroom. The public carrier is effectively FULL.
    Do not plan a longer CTA on the strength of the corrected number.
    Practical rule (content agent, 2026-07-28): script carrier VO to about
    70% of whatever the real ceiling is, and treat any scripted timing as
    advisory until it comes back measured.
  - HOW MUCH ROOM IS ACTUALLY LEFT: on the current public pair the last
    word ends at 19.694s of the join, 0.32s before the fade. That margin
    is set by the CARRIER, not the body — a LONGER body does not consume
    it. The risk runs the other way: if a body render comes in SHORTER
    than ~18.4s the fade moves earlier and the margin inverts. Safe band
    for the delivered pair is a body of 18.4s–20.0s (today: 19.0s). If a
    future capture leaves that band the fix is a shorter part 2, never a
    trimmed audio tail.
  - A _pt3 delivered without a _pt2 is a hard FAIL, not a silent skip —
    dropping it would leave a hole in the narration.
  - The join is generated into assets/marketing/_carrier_joined/ and is
    rebuilt whenever a part changes. It is not source; don't edit it.

  ANCHOR INTRO DELIVERY SPEC (now a SET, not one file — see §2):
  - ⚠ EXPECT TO BE TRIMMED, AND SAY SO IN THE DELIVERY NOTE. The generator
    only emits at 4/6/8/10s presets, so "5.5s" rounds to 10. Every intro
    delivered to date — the 7/27 original, deadpan, powerup and public —
    arrived at 10.005s against a 6.5s ceiling. This is now the expected
    path, not an incident. Masters are preserved as *_master_10s.mp4 so
    the trim stays reproducible; they are NOT read by the pipeline.
    Do NOT state a duration you have not measured: v1.5-era deliveries were
    described as "5.5s, NO TRIM required" while measuring 10.005s, and on a
    rotating set that is worse than it sounds — a rejected member drops
    silently and the set collapses toward one intro while appearing to work.
  - THE TRIM RULE (apply it yourself if you can): find where smoke fills the
    frame, set the out-point 0.6s later, set the in-point 6.0s earlier, and
    slide the window if it runs past either end.
  - Duration 3.5–6.5s, hard-enforced. Anything outside the window is
    REJECTED by preflight; that member drops from the day's rotation.
  - EVERY member of the set must satisfy this whole spec. Preflight now
    validates all of them, not just the standard file, and a defective
    member is a hard FAIL rather than a silent drop.
  - NOT EVERY CLIP CAN BE TRIMMED INTO COMPLIANCE — and this is the one
    failure mode the numbers do not catch. `anchor_intro_powerup` was
    rejected outright. Swept frame-by-frame across every candidate
    out-point from 5.0s to 10.0s: the smoke is ALWAYS inside the phone,
    with the bezel and notch still in shot, and at ~7.0s the clip hard-cuts
    to a wide newsroom. There is therefore no window anywhere in the file
    that ends on a usable dissolve bed — it is a REGENERATION, not a trim,
    and no choice of in/out point rescues it.
    ⚠ It PASSED the luma-spread check (105 at out 6.0s, inside the ≤120
    band) because that check measures UNIFORMITY, and smoke on a phone
    screen is just as uniform as smoke filling the frame. Dissolving from
    it would have ghosted a handset outline through the UI body. A second
    check now flags a dark border (edge-vs-centre brightness: powerup 41%
    vs standard 117% / public 90% / deadpan 75%) — but it is a WARN asking
    for an eye check, not a substitute for one.
    WHAT TO GENERATE INSTEAD: the camera must push FULLY into the screen so
    the handset leaves frame entirely, as public and deadpan both do at
    4→5s. The delivered ending must be smoke edge to edge with no device
    visible anywhere in the frame.
  - Must END on full-frame smoke. The dissolve reads the final ~0.5s as its
    bed. The 10s master pulled the camera back OUT to the newsroom over its
    last 2.5s — a beautiful shot that is unusable as an ending, and the
    reason the trim exists. Do not "complete" the move; end inside the smoke.
  - Must OPEN on the gesture, with sound. The 10s master spent its first
    ~1.9s on a static Anchor over silence; that head was cut because a reel
    opening on a still frame and no audio bleeds autoplay retention. Start
    on motion, and have the audio up from frame 1.
  - Embedded audio REQUIRED — it is the only sound before the VO enters.
    END it at true silence (the assembler's dissolve fade assumes that); it
    may start hot.
  - Must survive a 1:1 CENTRE CROP (keeps y 420–1500 of 1920) for the
    square cutdown. Keep the visor, the gesture and the phone inside that
    band; the top of the head may clip.
  - Same resolution/frame-rate spec as everything else (native 1080×1920,
    30–60fps). The current file is 720×1280 24fps and gets upscaled.

  DELIVERY SPEC (every replacement file):
  - Resolution: native 1080×1920 (9:16). Current endcards are 720×1280
    and get upscaled — it works, but costs sharpness. This is the #1
    quality upgrade available.
  - Frame rate: 30-60fps (24fps gets frame-duplicated to 60; slightly
    steppy motion on the energy effects).
  - All-Day carriers: VO must FINISH (hit silence) by ~20.0s of carrier —
    the assembler hard-fades at 20.5s and anything still talking gets cut.
    (History: 17.8/18.3 originally → tightened to 17.0/17.5 when the
    Anchor intro pushed the VO start later → widened to 20.0/20.5 on
    2026-07-27 when the modal hold went 2.0→2.5s. The window was widened
    to fit the narration rather than cutting it a third time; both
    delivered carriers now pass wall-to-wall with nothing discarded.)
    Content after the VO is discarded by design.
  - Endcards: keep audio in the file, front-loaded, crack synced to the
    visual snap; final frame = settled lockup (it opens the reel).
  - After any swap, engineering runs `npm run reel:check` — a preflight
    that verifies all of the above mechanically (corrupt files, missing
    audio, durations, VO-chop risk, weak crack, resolution) before
    anything is assembled. Deliver to pass it clean.

  THE DATE CHIP IS NOW VERIFIED AGAINST THE FOOTAGE (MKT-18, 2026-07-28).
  Previous versions of this doc said the stamp "can never disagree with the
  on-screen data". That was an assumption, not a check: the assembler took the
  date from its argument, confirmed a body file with that name EXISTED, and
  burned the date over whatever those pixels were. Copy a body to another
  day's filename and you got a clean, correct-looking reel of the wrong day's
  board — which is exactly what happened on 7/28 while testing the intro
  rotation. Each renderer now writes the captured date INSIDE the body file
  and both assemblers read it back: a mismatch aborts, naming both dates.
  Nothing changes for you — no new asset, no new step. It does mean a reel can
  no longer be re-stamped to a different day by renaming its body, so if you
  ever want a past day rebuilt, ask for a re-render rather than a rename.

  FILENAMES ARE LOAD-BEARING — `reel:check` now scans for near-misses.
  Every asset is read by EXACT filename from a registry. A name that is
  almost right is the worst case in this system, because nothing errors:
  the registry finds nothing, the previous week's file stays in place, and
  the reel builds and publishes looking correct. It has happened twice —
  a 2-byte endcard from a GitHub web rename (MKT-06) and
  `public_carrier_pt_.mp4`, which matched neither the joiner's `_ptN`
  pattern nor the orphan scan that exists to catch exactly that (MKT-16).
  Two guards now close the class:
  - a `_pt` name that is not `_pt<N>.mp4` is a hard FAIL;
  - a STRAY SCAN enumerates every filename the registries can produce and
    reports anything on disk that no registry reads. That covers all four
    near-miss shapes — endcard, stinger, carrier, intro — plus panel source
    art, and it closes the one genuinely silent hole: if BOTH the carrier
    and endcard of a kind were misnamed, the kind used to report a healthy
    "dormant, no assets delivered" and say nothing else.
    A stray is a WARN, not a FAIL — it breaks nothing today, it just means
    something you delivered is not reaching a reel. Deliberate exceptions
    (the rejected powerup intro, the not-yet-registered public carrier) are
    listed WITH their reason so the exemption stays visible.
  Practical upshot: if you deliver an asset and the next `reel:check` names
  it as unreferenced, the filename is wrong — not the file.

  Outputs land in assets/marketing/allday_reels/, midday_reels/,
  evening_reels/ and verify_reels/, and are pushed to GitHub after every run.


--------------------------------------------------------------------------------
8. ENHANCEMENT LANES (where new work plugs in cleanly)
--------------------------------------------------------------------------------
  - New/longer carriers and endcards: file drop-in, spec in §2/§3.
  - New caption templates: text delivery, spec in §5.
  - A new SCOPE/variant of the existing slate reel is nearly free: an endcard
    config entry, a stinger config entry, a caption kind, a scope registry
    entry — plus TWO things that fail late if forgotten: the database kind
    CHECK constraint (publish fails at upsert AFTER a successful render and
    upload) and the admin Reels KIND_UI entry (an unknown kind used to crash
    the whole Reels tab; there is a fallback now, but add the entry).
    Endcards and stingers for a new session cost NO new video generation —
    they reuse the shared motion files with different copy.
  - A genuinely new reel FORMAT still needs (a) a render/assemble script
    pair, (b) a caption registry entry, (c) the kind additions above — the
    storage/app/posting chain then works unchanged.
  - Do NOT propose: auto-posting, public-page reel lanes, captions that
    move numbers outside pro, bypassing the lint/Two-Question gates, or a
    full-fidelity FREE Midday/Evening reel (see §9 — it would defeat the
    redaction rule that makes the sessions a Pro benefit).


--------------------------------------------------------------------------------
9. WHERE THE SYSTEM STANDS (2026-07-28) — READ THIS FOR CURRENT STATE
--------------------------------------------------------------------------------
BUILT AND RUNNING DAILY:
  MKT-01  UI ad-clip render rig + 30s ad assembly
  MKT-02  Verify / "Receipts" reel pipeline
  MKT-03  All-Day dual reels (Pro + Free)
  MKT-04  In-app publishing → storage → Admin → Reels handoff
  MKT-05  Data-driven caption engine (+05b: verify carries free & pro drafts)
  MKT-06  Preflight (npm run reel:check) + daily-post QC audit
  MKT-07  Slate stamp — burned-in day · scope · purpose chip on every cut
  MKT-08  "The Anchor" branded intro (superseded by MKT-17 — see §2)
  MKT-09  Multi-part carrier VOs (_pt2 …) auto-joined with a 0.35s breath
  MKT-10  Endcards: motion and text split — COPY IS NO LONGER AN ASSET
  MKT-11  Rotating promo panels — now APP-WIDE (see §2 warning)
  MKT-12  Branded stinger between the intro and the UI body
  MKT-13  Session wave — Midday + Evening reels (2026-07-28)
  MKT-17  Per-kind intro resolution + rotation (2026-07-28) — see §2

IN FLIGHT, NOT BUILT — do not propose against these yet; §10 has the detail:
  MKT-14  Brand attribution in the slate stamp .......... PARKED (untouched)
  MKT-15  Social handoff buttons ....... Phase 1 SHIPPED · Phase 2 BLOCKED
  MKT-16  Public asset integration + redaction ....... Phase 0, build held

DAILY OUTPUT TODAY: up to four reels — verify (14.4s), All-Day Pro + Free, and
optionally Midday and Evening (Pro only). Slate reels are 33.83s or 34.20s
depending on the day's intro. All four auto-publish to Admin → Reels with
drafted captions. Nothing auto-posts, ever.

THE SESSION WAVE IS PRO-ONLY, AND THAT IS A RULING:
  The free group gets the All-Day drop in FULL but Midday/Evening REDACTED —
  that gap is the Pro conversion frame, and the publish pipeline already
  enforces it for image kits. A full-fidelity free session reel would hand
  away exactly what the redaction withholds. If a free session reel is ever
  wanted it requires a REDACTED CAPTURE (digits masked in the UI), which is a
  real build — not a config entry. Do not propose it as "just another
  variant".

WHAT WOULD MOVE QUALITY MOST, IN ORDER (the standing asks):
  1. NATIVE 1080×1920 SOURCE ASSETS. Every delivered motion file to date is
     720×1280 24fps and gets upscaled + frame-duplicated. This is the single
     biggest quality gap left and the only remaining preflight warning class
     (10 of 10 current warnings are this, across 3 intros and 4 endcards).
  2. MIDDAY + EVENING ENDCARD COPY — 2 lines each, the smallest real gap on
     this list. Their captions and stingers are genuinely distinct, but both
     endcards still carry allday_pro's lines verbatim ("FIRST ACCESS.
     ALWAYS." / "VERIFIED TOMORROW MORNING"), so a Pro subscriber reads the
     same close three times a day. Config strings over the existing motion
     file — zero generation, a text edit and a re-run.
  3. Session carriers are currently pro-only and generic. Session-specific VO
     ("the midday board", "tonight's board") would read better than reusing
     All-Day framing.
  4. A LOUDER OPEN ON ROTATION MEMBERS. Deadpan opens at −56.2 dB mean over
     its first 0.3s against the standard intro's −37.9. MKT-08's "audio up
     from frame 1" rule is about autoplay retention, and a near-silent open
     spends it.
  5. More panels. The rotation adapts with no code change; six is the current
     set and the slot is now a product surface, so the copy bar is higher.
  6. More caption templates per kind (8 today, can grow; rotation adapts).
  7. More intro rotation members. The set is 2. Same delivery spec as §7 —
     and read the trim rule there before generating.

WHAT IS DELIBERATELY NOT BUILT:
  - Auto-posting to Facebook (platform + brand-safety rule).
  - A public-page reel lane (page is brand-only, under probation).
  - Free session reels (above).
  - A stinger on the verify reel — its body is 6.3s, so intro + stinger would
    put more branding on screen than receipts. Flag exists, set to off.


--------------------------------------------------------------------------------
10. IN FLIGHT — WHAT EXISTS AND WHAT DOES NOT (2026-07-28)
--------------------------------------------------------------------------------
Added in v1.6 because three lanes are now partly delivered, and "an asset is in
the repo" has been mistaken for "the pipeline reads it". Nothing in this section
is running daily.

MKT-15 — SOCIAL HANDOFF. Phase 1 SHIPPED.
  A platform registry + a caption transform + one row per platform in Admin →
  Reels: shaped-caption preview, char budget, per-platform lint, Two-Question
  checkboxes, one "Save + Copy + Open" tap. `npm run social:dryrun` is the
  off-device gate.
  ENABLED: Telegram only — a room we own, so no classifier decides whether we
  are gambling-adjacent there. It is tier 4 (the Pro room), not a cross-post.
  REGISTERED BUT DISABLED: YouTube, TikTok, Reddit, X — all four blocked on
  Phase 2's public cut, each declaring asset:'redacted' so the dependency
  lives in the registry rather than a comment. Instagram is disabled by
  STANDING RULING, not a gate: it needs a linked FB Page and Meta review
  against a page de-recommended twice, and Meta enforces across linked
  accounts. Phase 2 does not unblock it.
  THE MECHANICAL CEILING, worth internalising before proposing anything here:
  no URL scheme on any platform can attach a video file. Every path is
  save-to-camera-roll then pick-in-app; deep links only choose which screen
  opens. The CLIPBOARD is the real value, because retyping a platform-shaped
  caption is the actual friction.
  ⚠ STILL OWED: an operator DEVICE test. The deep links are documented-scheme
  research, not observed behaviour — this is a Linux codespace with no phone.
  X / Reddit / Telegram carry templates; YouTube / TikTok / Instagram are
  deliberately plain app-open, because an undocumented scheme that silently
  fails is worse than a link that honestly just opens the app.

MKT-15 — PHASE 2, THE PUBLIC CUT. BLOCKED, and bigger than first scoped.
  The headline finding: MASKING DIGITS IS NOT SUFFICIENT. The body is a screen
  recording of the real app, and the live audit (every on-screen string run
  through the real lint engine at tier 1) found four BLOCKING vocabulary terms
  structurally present on all six modals — PICK, STRAIGHT, BOX, PLAY — plus
  session labels and state-code attributions. A public cut needs a RELABELLED
  capture mode, not merely a redacted one.
  This is a TIER MISMATCH, NOT A BUG. Those words are the sanctioned
  vocabulary for the paying subscribers the app is built for. The fix is a
  capture-mode override and NEVER an edit to the consumer UI.
  COPY IS SETTLED — the eight-slot public set is delivered and approved; see
  docs/mkt15_phase2_copy_brief.md. Notably slot 3 ships as "ANY ORDER", not
  "SET": the brief's character-count rule was a bad proxy for pixel width, and
  measured on the live modal the chip has 86px of content width against
  "ANY ORDER"'s 55px. Paired with "BEST ORDER" it teaches itself.
  THE REDACTION IS NOT A BLUR OF THE DIGITS. The look is extreme blur + the
  bolt, as ruled — but capture mode never RENDERS the real digits: it draws a
  placeholder block, blurs that, and lays the bolt over it. A blur or mosaic
  derived from the true pixels is recoverable by sharpening across only 1,000
  combinations, and a high-contrast blurred 3-digit block is often readable at
  a glance anyway. Drawing a placeholder makes the frame-by-frame Q1 pass a
  fact rather than a judgement about how much blur is enough. Nothing changes
  in what you supply.
  ⚠ STILL OWED FROM CONTENT: the three bolt decisions (fill, placement,
  scale+opacity) and bolt_mark.svg if it exists — only the 2048 PNG arrived.
  ⚠ ALSO WORTH AUDITING: the PLAN tab CONTENTS are unaudited. The renderer
  captures each modal at its DEFAULT tab and never clicks through, so only the
  tab label is exposed today. The day a tab-switch beat is added to the
  capture, an un-audited surface enters frame. Better to have the list first.

MKT-16 — PUBLIC ASSET INTEGRATION. Phase 0 only; build held.
  DELIVERED AND USABLE: anchor_intro_public.mp4 (trimmed, passing preflight),
  public_carrier.mp4 + _pt2 (joined, 20.360s, passing), bolt_mark_2048.png.
  NOT BUILT: the public reel KINDS themselves. allday_public / midday_public /
  evening_public exist at exactly ONE of five registration points — the intro
  registry (MKT-17 pre-registered them). They are absent from the endcard
  config, the stinger config, the scope registry, the caption registry and the
  database kind CHECK constraint. So: the public assets are real, and no
  command builds a public reel today. Do not write public captions or public
  endcard copy against a kind that cannot yet be published.

MKT-14 — SLATE-STAMP BRAND ATTRIBUTION. Parked, cleanly.
  Nothing built, no code touched; the work order is parked verbatim at
  docs/mkt14_work_order_parked.md and resumes only on explicit go-ahead. The
  earlier bottom-right watermark version is SUPERSEDED and must not be built —
  every short-form platform stacks its own chrome bottom-right, so a mark
  there is buried on all of them.

FREE-GROUP MIDDAY / EVENING REEL — not a lane, and nothing is assumed.
  It is the second payoff of the masked capture, but it is not registered
  anywhere (§9's PRO-ONLY ruling still stands until it is). When it is built
  it needs its OWN endcard and carrier — Pro CTA and $2.49 pricing, not the
  free-community CTA, and NOT inherited from the public set. Those have not
  been written and will not be invented.
================================================================================
