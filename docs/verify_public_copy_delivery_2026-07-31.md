# verify_public — staged copy delivery (content agent, 2026-07-31)

Stored verbatim so registration never recovers copy from a transcript (the
MKT-24 lesson). **Kind NOT yet registered** — build gated on the Phase-0
report (see `VERIFY_PUBLIC_PHASE0_2026-07-31.txt`). Every string below ran
through the real `lintCaption(…, 1)` on delivery day: **21 items, 0 blocking
trips, 0 warnings**; all YouTube titles < 60 chars.

## Endcard (free-tier motion set)
```
line 1   HITMASTER ZK6
line 2   PUBLISHED FIRST. CHECKED AFTER.
line 3   SEE THE FULL RECORD FREE
```

## Stinger + chip (escalation split)
```
chip (frame one)   YESTERDAY'S RECORD   + the dated line
stinger            HITMASTER ZK6 / YESTERDAY'S RECEIPTS
```
⚠ Chip is deliberately NOT "YESTERDAY'S RESULTS" — that stays tier-2/4;
"results" reads gambling-adjacent to a cold viewer/classifier. RECORD is the
analytics register. Stinger keeps RECEIPTS so MKT-31's escalation logic
(plain identification frame one → brand framing 5.6s) survives.

**Config answers (delivery asked, answered 2026-07-31):**
- (a) Headline-dominant stinger layout: RULED YES for verify_public — same
  reel's identity; the headline matters more on a cold surface. Recorded
  here; wire at registration.
- (b) verify_public chips YESTERDAY's date: TRUE BY CONSTRUCTION — it will
  assemble via the verification assembler whose stamp IS the content date
  (D−1), same as verify.
- Open at registration (flagged in Phase-0 report): seal inheritance and
  chip accent (gold vs public cyan).

## Caption templates — 8, tier 1
(no counts, no states, no digits, no match-type vocabulary; ritual + audit
invite; `{free_group_url}` wired; #5 is the ONE that names the masking — the
deliberate 1-in-8 dose)
1. Yesterday's board, graded against the official results — every signal we published before the draw, checked after it. The full record is free: {free_group_url}
2. We publish six ranked signals every morning and check every one of them the next day, in public. Come read the record yourself: {free_group_url}
3. This is what accountability looks like in pattern analysis: published first, graded after, nothing edited in between. Free to check: {free_group_url}
4. Anyone can claim a method. We post ours before the draw and grade it after — same time, every day, across 40+ states & provinces. {free_group_url}
5. Yesterday's signals, checked against what actually drew. The values are covered here — they're open in the community: {free_group_url}
6. No edits, no retroactive claims. The board goes up before the draw and gets graded the next morning where anyone can see it. {free_group_url}
7. Every morning we grade the day before. That's the whole product — the analysis, then the receipts. Read the record free: {free_group_url}
8. Published before. Checked after. That order is the only thing that makes a record worth reading. See it in full: {free_group_url}

## YouTube transform — verify_public OWN set (never inherit allday_public's)
TITLES (6): 1. We grade yesterday's signals in public · 2. Published before
the draw. Checked after. · 3. What happens the morning after we post ·
4. Our record, checked against official results · 5. How we grade our own
pattern analysis · 6. The receipts, every single morning

DESCRIPTIONS (3):
A. Every morning we check the previous day's six signals against the official results and publish what happened. Nothing edited, nothing retroactive.\nThe full record is free in our community: {free_group_url}
B. We publish pattern analysis before the draw and grade it after. This is the grading half — the same board, checked in the open, across 40+ states & provinces.\nSee the full record free: {free_group_url}
C. Most methods never show what happened next. Ours gets checked every morning and the record stays public.\nFree to read: {free_group_url}

TAGS: #Shorts #DataAnalysis #PatternAnalysis — description only (MKT-37 ruling).

## Note
The delivery's final section re-supplied the verify-FREE #9 replacement
("pool sits at 11") — STALE: that line was already registered earlier on
7/31; the verify-free pool is complete at 12.
